# Wordocious — Project Bible

*Last updated: 2026-07-02. **(80) iOS build 90 — bot plays multi-board by the REAL rules + honest recap badges (2026-07-02).** User audit of an OctoWord bot defeat exposed the root: BotEngine planned multi-board modes as N independent mini-games (43 'guesses' in a 13-max OctoWord), inflating scores unfairly and turning the replay recap into nonsense (bot 'Solved' above 6 red boards; player 'Not solved' above 7 purple). Rewrite: shared-guess modes get one legal submission sequence (fillers → shuffled solutions, per-board latestGuesses on every submission); Succession sequential within one budget; Gauntlet per-stage segments using each stage's maxGuesses. Recap badges now show the per-board tally ('7/8 boards') from the replayed boards. Web/Android port in flight via agent. **(79) Web + Android parity port of builds 87–89 (2026-07-02).** Ported to BOTH platforms: (1) finished multi-board solo games (Quad/Octo/Succession/Deliverance) render the compact completed-board recap grid instead of the zoomed in-play layout; (2) VS result final boards — multi-board matches rebuild each player's FULL board set via engine replay from the seed (2-board '+N more' cap removed; single-board always reveals the answer); (3) Gauntlet VS results show the solo-style expandable stage-by-stage review per player (GauntletStageBreakdown shared with the Completed-Today card), reconstructed by replaying the flat guess log with NEXT_STAGE advancement; (4) VS opponent HUD/strip gets the mode's board count from match start (no single-placeholder-board Quad/Octo pre-typing); (5) the local CPU bot's ProperNoundle answer now comes from the PN puzzle set for the seed (web: new getPuzzleForSeed + bot match start now emits puzzleMetadata; Android: BotEngine normalizes puzzleForSeed's answer). tsc clean + :app:compileDebugKotlin BUILD SUCCESSFUL. **(78) iOS builds 88+89 — Gauntlet VS stage review + PN bot wrong-answer fix (2026-07-02).** Build 88: Gauntlet VS results rebuilt from the seed via GauntletReconstruct and shown as the solo-style expandable stage fan-down per player (was a '+19 more' letter wall). Build 89 (CRITICAL): the CPU bot seeded ProperNoundle from the shared engine (dictionary word) while the player plays the PN puzzle set's proper noun — the bot literally played a different answer (user solved ZELDA, match said RELAY, winner shown 'Not solved'). BotEngine now pulls ProperNoundle.puzzle(forSeed:).answer. Web/Android parity for both + the build-87 fixes running via agent. **(77) iOS build 87 — four build-86 feedback fixes (2026-07-02).** (1) ProperNoundle restored to the profile mode picker (its HomeMode has mode:nil so the picker's filter dropped it; chips now derive GameMode from dbKey). (2) Post-game Quad/Deliverance recap uses the compact CompletedBoardLayout per-board grid instead of the zoomed in-play BoardLayout — all multi-board modes now match Octo's clean finished look. (3) VS result final boards: multi-board matches render each player's FULL board set as the solo-style compact recap (engine replay from seed via CompletedBoardReconstruct), replacing the 2-boards+'+N more' letter wall; single-board keeps side-by-side + always reveals the answer. (4) VS opponent strip receives the mode's board count from match start — quad/octo no longer show a single tall placeholder until the opponent types. Uploaded to TestFlight. Web/Android parity for 2–4 pending (task U5). **(76) restat R6 — Android port of the profile/records restructure (2026-07-02).** Android Compose now matches the restat web work end-to-end: shared StatKit (SectionHeader/KitCard/StatCell/StatGrid/ChartCard/ProLockOverlay with blur+gradient Pro pill), StatsDeepService (Kotlin ports of the ten stats-service.ts fetchers; guess logs evaluated client-side via :core evaluateGuess, length-guarded), SnapshotHero + "top X% today" standing strip + Opener Lab + Weekday Form + Daily Points chart (sweep COUNTS single home = Records→You), ProfileScreen restructured to the web page order (Today's Dailies untouched; per-mode panel gets a mode header with read-only play-type chip + 4×2 ModeStatsGrid, replacing the old global STATS BY MODE list), Insights card, Pro deep layer (Skill Radar Canvas pentagon w/ edge-aware labels, Rivalries on the VS tab, per-mode Deep Insights: Opener Yield / Position Accuracy / Gauntlet Stage Breakdown / Hint honesty / Word Almanac — all Pro-gated with blurred static sample previews), Progression banner (medals + achievements in both views), and the Records You tab upgraded to top-3 Record Chase with progress bars + Trophy Shelf. Simulate Pro (is_admin) untouched; compileDebugKotlin green (--rerun-tasks). **(75) iOS build 86 uploaded — restructure + VS polish trio (2026-07-02).** TestFlight build 86 bundles: the full profile/records restructure port (74), FULLY opaque VS countdown backdrop (build-85's 0.92–0.96 vignette still ghosted the queue screen — now solid #1E1B3A→#0A0A12 on all 3 platforms), a new 3-2-1-GO! beat (GO! flashes ~0.6s over the board start; was cutting straight from '1'), and the ProperNoundle VS clue fix (the VS board never rendered vm.clue — Clue burned a row with no visible hint; web/Android already showed it). Android R6 port in flight. **(74) restat R5 — iOS port of the profile/records restructure (2026-07-02).** Native app now matches the restat web work: shared StatKit (SectionHeader/KitCard/StatCell/ChartCard/ProLockOverlay), SnapshotHero (old 4-card summary + This Week strip merged into one card), "top X% today" standing strip, Opener Lab + Weekday Form cards, Daily Points chart split from the sweep-counts card (counts single home = Records→You), Progression banner (Daily Medals + Achievements in both All and per-mode views), and the Pro deep layer — Skill Radar pentagon (Path-drawn, edge-aware labels), Rivalries (VS tab), per-mode Deep Insights (Opener Yield, Position Accuracy, Gauntlet Stage Breakdown, Hint honesty, Word Almanac) — all Pro-gated with blurred sample previews for free users (StatsDeepService ports the stats-service.ts fetchers, guess logs evaluated client-side via evaluateGuess). Records tab split out of ProfileTab.swift into RecordsTab.swift; You tab upgraded to top-3 Record Chase with progress bars + Trophy Shelf. Today's Dailies and the is_admin Simulate Pro toggle untouched. **(73) iOS build 85 — clean VS countdown scene (all platforms, 2026-07-02).** The 3-2-1 countdown floated over the dimmed queue screen ("MATCH FOUND / Matching you with…" bled through behind the number). All 3 platforms now give the countdown the match-intro's near-opaque radial vignette + the gradient mode title; iOS also fades the overlay in/out. Intro → countdown → board reads as one continuous scene. Uploaded via manual-signing pipeline. **(72) iOS build 84 — uniform share-card boards + post-share tiny-board fix (all platforms, 2026-07-02).** VS share cards: both players' boards now render on one shared grid size (max rows/cols across shown boards, short grids padded with empty tiles) so a quick win next to a slow loss no longer produces mismatched card sizes — fixed on iOS/web/Android. iOS: game screens now .ignoresSafeArea(.keyboard) — sharing a card opens iMessage's system keyboard inside the share sheet, and its lingering inset squeezed the next (rematch) match into the top half with a tiny board and keyboard-sized dead zone; applied to VSGameView + solo GameScreen. Uploaded via the new manual-signing pipeline. **(71) iOS build 83 uploaded — P1–P6 batch + home-screen widget live on TestFlight (2026-07-02).** App group `group.com.wordocious.app` registered + assigned to both identifiers via the dev portal (user signed in, Claude drove Chrome; no public API exists). New signing pipeline: profiles minted via ASC API (app 4AVC4A4WPR, widget 8X3LDS85XK), manual-signing export (cloud signing lacks permission for the widget profile), dual re-sign appex→app with apps/ios/signing entitlements, altool upload (Delivery 714ef0ce-ed02-47e2-b2c5-7ccff1c5b25d). Build 83 contains: VS share cards, theme fixes, review prompt, VoiceOver/TalkBack pass, and the widget (small + medium). **(70) Polish batch P1–P6 (all platforms, 2026-07-02).** (P1) VS share cards now match the daily-card aesthetic on all 3 platforms: 1080×1080, WORDOCIOUS gradient wordmark, head-to-head columns w/ crown + scores + solve badges + tinted mini-boards, Victory/Defeat/Draw pill (iOS ImageRenderer, web canvas vs-share-image.ts, Android Bitmap/Canvas renderVs). (P2) Theme audit — new VS surfaces (PN hints/clue box etc.) moved off hardcoded light colors onto theme tokens. (P3) App-review prompt: after a WIN on streak ≥3, once per version, 2.5s delayed (iOS SKStoreReviewController, Android Play review-ktx). (P4) midnight countdown was ALREADY live in the sweep banner on all 3 ('Next in Xh Ym') — no change. (P5) accessibility pass: iOS VoiceOver on board rows (one spoken sentence per guess), keyboard keys w/ letter+state (incl. per-board quadrant summary), victory stats + VS score card combined elements; Android tile/key contentDescription parity; web already had aria labels. (P6) iOS home-screen WidgetKit widget (small: streak + X/9 + 9 dots; medium: full mode-chip grid), rendered from a JSON snapshot the app writes to the group.com.wordocious.app container (WidgetBridge ← DailyCompletionsStore), refreshes per completion + midnight. New app-extension target com.wordocious.app.widgets (registered via ASC API, FN4U457D34) + APP_GROUPS capability on both bundle IDs; release re-sign entitlements moved into apps/ios/signing/. GATE before the widget ships: register + assign group.com.wordocious.app to both identifiers in the developer portal (public ASC API cannot manage app groups), then regenerate App Store profiles + manual-signing export. **(69) VS result redesign — FINAL SCORE card + why-line + board badges (iOS build 82, all platforms, 2026-07-02).** The result screen buried the outcome in three inverted comparison bars (lower-is-better bars read backwards) and never explained WHY you won/lost — losing with fewer guesses + a lower score (because you didn't solve) looked like a bug. Now: (1) prominent FINAL SCORE card right under the headline — big head-to-head totals (winner crowned + highlighted, loser dimmed), the exact calculation under each ('9 guesses + 4.59 time'), elapsed clock, Solved/Not-solved badges, and a 'lowest score wins — but solving always beats not solving' footnote; (2) plain-English why-line under WINNER/DRAW/DEFEAT ('Daily Bot solved it — you didn't' / 'Both solved — you won on score' / 'Dead even'); (3) final boards get per-player solve badges + the answer word revealed for single-board modes. Comparison bars removed from the screen (components kept). Shared logSolved() helper detects solve from the guess log vs solutions. iOS CURRENT_PROJECT_VERSION 81->82, uploaded (Delivery a92dc18f-8fee-4cd9-97cc-193047b4885c). Web tsc + Android compileDebugKotlin green. **(68) Smooth initial-match countdown vs bots (iOS build 81, all platforms, 2026-07-01).** The first game from the chooser showed a clunky one-frame countdown (just '3' then the board): the bot fired match_start at 3s from match_found, but the intro clash auto-plays ~2.5s first and the numeric countdown only starts on the intro's onDone — leaving ~0.5s of visible countdown. Fix (bot-only, all 3): the bot holds match_start until introMs(2500) + countdownMs(3000) both elapse, so the 3-2-1 runs full after the clash; serverStartAt shifts with it so the match clock + bot pacing stay correct. Rematch was already smooth (no intro). Human/socket matches unaffected. iOS CURRENT_PROJECT_VERSION 80->81, uploaded (Delivery 9920e853-2413-466c-8b93-05d0ec427222). **(67) CRITICAL FIX — CPU match stuck on spectator screen (iOS build 80, all platforms, 2026-07-01).** When the player finished a VS-vs-CPU match, the code called service.playerCompleted(...) then set screen=waiting. For a bot that had already finished (fast difficulty solving before the player), playerCompleted ends the match SYNCHRONOUSLY (screen→result), so the subsequent screen=waiting clobbered it back — the match was ended internally (ended=true) but the UI was stranded on the spectator screen: the clock kept ticking and 'Claim your win'/resolveNow no-op'd on its !ended guard. Fix (iOS/Android/web): set the spectator screen BEFORE reporting completion so a synchronous match-end wins. Affects every VS mode vs bots. iOS CURRENT_PROJECT_VERSION 79->80, uploaded (Delivery c2a2aac0-7ee6-4592-85b8-ee21ced9d8b1). All 3 verified green. **(66) VS Gauntlet spectator + fast-forward + smooth countdown + parity (iOS build 79, 2026-07-01).** iOS: (1) Gauntlet spectator revamped — a card PER STAGE (name in gradient, number/checkmark badge, Cleared/Playing/locked status, that stage's boards) instead of a flat wall of 21 mini-boards; cleared stages collapse to solved rows, active shows live with flip-in, locked dim. (2) FAST-FORWARD: on any CPU spectator screen you can skip watching the bot grind — 'Claim your win' when win-locked, else neutral 'Skip to result' (resolveNow() ends via the bot's already-fixed plan, win OR loss). (3) Gauntlet match screen: added the solo 5-node stage stepper + gradient stage TITLE; cleared stage now plays the solo StageTransitionOverlay (auto-advance) not a bare Continue button; opponent strip shows the opponent's stage NAME (+ accent); dropped the confusing per-stage '1/6 guesses' from the top header. (4) Board no longer shifts when the opponent starts/stops typing (row height reserved). (5) Countdown no longer flashes a stale '1' — the 3-2-1 tick starts when the intro clash finishes. Succession/Deliverance Gauntlet stages verified working in VS (shared engine; bot stage boundaries derive from gauntletStages). iOS CURRENT_PROJECT_VERSION 78->79, uploaded (Delivery 9b92ce94-a432-49a5-a36c-d1169f9b0e7d). Android/web parity for fast-forward + smooth countdown + no-typing-shift shipped; the Gauntlet stepper/overlay/spectator revamp is iOS-only so far (deferred). Android compileDebugKotlin + web tsc green. **(65) VS CPU 3rd profile tab + CPU warmup + spectator polish + rematch countdown (iOS build 78 + Android + web, 2026-07-01).** (1) PROFILE: 'VS CPU' is now a THIRD toggle beside Solo/VS (was a static aggregate card under VS). Selecting it filters user_stats to play_type='vs_cpu', so the mode picker + per-mode scoring grid + guess distribution populate per game exactly like Solo/VS. Renamed 'VS CPU · PRACTICE' → 'VS CPU' everywhere. (2) CPU WARMUP: bot matches no longer flash the human matchmaking queue (searching…/position/waiting); a clean branded warmup ('Matching you with Rook 🤖…') shows for the brief spin-up before the intro splash. (3) SPECTATOR POLISH: opponent board enlarged (single 16→34, multi 24/14) so it fills the space; tiles flip in tile-by-tile (staggered 3D rotateX reveal) instead of a flat pop; breathing 'live' ring on the opponent avatar; in-match HUD board bigger + same flip. (4) REMATCH COUNTDOWN: hitting Rematch runs a 3-2-1 'REMATCH STARTING IN' countdown before the board resets (was instant); the bot engine is delayed the same 3s to stay aligned; works for human rematches too. (5) Invite sheet (iOS): styled inline mode dropdown (brand icons + accents) replacing the native Menu, all-caps title. iOS CURRENT_PROJECT_VERSION 77→78, uploaded (Delivery 1c2a10d9-223f-419a-8ffb-f9b97df2c9df). Android compileDebugKotlin + web tsc green. NOTE remaining parity gap: Android invite-sheet dropdown restyle not yet done. **(64) VS UX overhaul — entry chooser + VS hints + intro polish (iOS build 77 + Android + web, 2026-07-01).** Reworked the VS entry flow and added hints on all 3 platforms. (1) ENTRY CHOOSER: tapping a VS mode opens a chooser — Quick Match (joins the live human queue, deferred from mount via joinHumanQueue), Bot Match (Pro-gated difficulty/opponent picker), Invite a Friend (existing InviteModal/InviteSheet) — instead of silently queuing AND offering the CPU at once. Daily VS + accepted invite links still auto-join; the queue-screen CPU panel is now the empty-queue auto-offer only. New entry screen state on all 3 (VSScreen.entry / 'entry'). (2) VS HINTS match solo exactly: Six/Seven get the vowel+consonant reveal bar, ProperNoundle gets clue/vowel/consonant — each reveal is a board row (counts as a guess, the VS cost). Human AND bot matches (per-mode VS component shared by both transports). Web reused useClassicHints/useHints verbatim; native reused the shared game VM hint API + made Android HintPills non-private. (3) INTRO POLISH: iOS avatar+ring rasterized into one drawingGroup layer so the ring can't detach during the slam; all 3 got a deep radial-vignette backdrop instead of flat black. (4) Opponent mini-board visible the whole match (dropped the hasTiles gate) so it never flickers in on the first guess. (5) vs-CPU practice card always shown on the profile VS tab (empty state before first bot match). iOS CURRENT_PROJECT_VERSION 76->77, uploaded (Delivery 842fa182-14f1-4307-bc11-5be36c55adb4). Android compileDebugKotlin + web tsc green. **(63) CPU VS — Gauntlet + Ghost + animated photo-finish, full parity (iOS build 76 + Android, 2026-07-01).** Closed the last CPU gaps on all 3 platforms. (1) GAUNTLET CPU enabled everywhere: the bot engine now maps GAUNTLET to 21 boards, pulls all 21 solutions via generateSolutionsFromSeed (createInitialState only holds the current stage), and emits stage-completed markers at the stage-boundary boards [0,4,8,12,20]; LocalBotMatchService fires onOpponentStageCompleted so the opponent's 5-node stepper advances. cpuSupported un-gated for Gauntlet (web/iOS/Android). So YES — you can now play the CPU on Gauntlet. (2) GHOST RACES on native ('Beat Your Best'): MatchStatsService.ghostBestRun (fewest-guesses then fastest winning solo run from matches) → chooser button, disabled until a win exists, replays that pace (targetGuesses+targetSolveMs+forceSolve). (3) PHOTO-FINISH ANIMATION on native: replaced the static text with an animated spring-in stamp (SwiftUI scaleEffect+spring / Compose graphicsLayer+animateFloatAsState). iOS CURRENT_PROJECT_VERSION 75→76, uploaded (Delivery efdd6b0e-51ea-4b32-81f0-82800aba4bb5). Android compile+assemble green. CPU VS feature is now FULLY at parity across web/iOS/Android with zero remaining gaps. **(62) CPU VS opponents — native ports iOS build 75 + Android (Phases 4-5, 2026-07-01).** Ported the client-side CPU/bot VS feature to BOTH native platforms with the same drop-in architecture as web. Each platform: BotPersonas + BotEngine (difficulty/adaptive/ghost trajectory, real dictionary solver + fabricated PN path) + CpuProgression (UserDefaults / SharedPreferences: streak/ladder/cosmetics/Bot-of-Day) + a VSTransport protocol/interface that the existing socket service now conforms to + a LocalBotMatchService replaying the plan on timers (DispatchWorkItem / Handler) with the exact match_found→+3s match_start timing + composite winner rule. VSMatchViewModel.service became a swappable VSTransport; startCpu(kind) tears down the socket, builds the bot, re-wires handlers, joins. handleMatchFound resolves the CPU persona locally (shows '<Persona> 🤖'); recordResult branches to GameResultsService.recordCpuResult (user_stats vs_cpu ONLY — no XP/matches/daily/achievements/daily-lock) + progression + session tally + photo-finish. UI: VS queue chooser (Easy/Med/Hard/Adaptive/Bot-of-Day) + 15s auto-offer + Pro gate (non-Pro → Pro screen); result-screen photo-finish text flourish + streak/milestone/cosmetic/run-it-back; profile 'vs CPU · Practice' box (best-streak hero); Pro selling-point line. Gauntlet gated out (needs stage relay). iOS CURRENT_PROJECT_VERSION 74→75, uploaded (Delivery ecefdc37-d8d5-469a-a7dd-b9aa996b3359). Android compileDebugKotlin + assembleDebug green. CPU VS now on ALL 3 platforms. FOLLOW-UPS (both native): Ghost 'Beat Your Best' (needs a best-run query) and the full native photo-finish ANIMATION (text flourish for now). Also earlier this session: Android per-mode win-streak parity in profile STATS BY MODE (MatchStatsService.modeWinStreak). **(61) CPU VS — web Phases 2-3 complete (fun layer + multi-board) (2026-07-01).** Built the full fun/addictive layer + extended CPU to multi-board, all client-side & unranked. cpu-progression.ts (localStorage): CPU win streak (current/best), boss-ladder rung, cosmetic unlocks, Bot-of-the-Day day-streak. Result screen: streak line + milestone shout (5/10/25/50/100) + cosmetic-unlock note + run-it-back session tally ('You N · CPU M', rematch button reads 'Run it back'). Profile vs-CPU box shows best CPU streak hero line. photo-finish.tsx = DISTINCT close-win flourish (camera flash + checkered-flag sweep + speed lines + stamp; NOT the win confetti — confetti held until it dismisses; reduced-motion→static stamp) on sub-2s margin ('PHOTO FINISH') or last-guess win ('CLUTCH'). Adaptive difficulty (4th chip, streak-seeded). Ghost races 'Beat Your Best' (ghost-service reads best run from matches → bot replays that pace via engine targetSolveMs+rescale+forceSolve; disabled until first win). Bot of the Day (fixed daily seed + rotating persona + personal day-streak). LocalBotMatchService gained BotConfig (adaptive/ghost/fixedSeed/opponentId); cpuIdentity() resolves persona for any cpu:<kind>. Multi-board: bot loops boards → CPU now offered for QuadWord/OctoWord/Succession/Deliverance; GAUNTLET gated out (needs stage relay, TODO). tsc clean; routes serve 200. **REMAINING: Phase 4 iOS + Phase 5 Android full ports** (engine+service+UX+progression+fun layer). **(60) iOS per-mode Streak/Best Streak fix (build 74, 2026-07-01).** Profile mode-detail grid showed 0 for STREAK + BEST STREAK on every mode (Solo & VS) — it read ModeStats.winStreakCurrent/best which UserStatsService.aggregate never sets. Fixed: ProfileTab now fetches the per-mode win streak from match history via MatchStatsService.modeWinStreak (made non-private) in a .task keyed on selectedMode (reset-then-fetch, no stale flash), matching web mode-detail-panel's fetchModeWinStreak. Not play_type-filtered (same value both tabs = web parity). Android mode-detail doesn't render per-mode streak cells so no visible bug there (parity add TODO). Uploaded (Delivery 55b90c9d-5a8b-49eb-b4eb-77c90d008982). **(59) CPU/bot VS opponents — Phase 1 web core (2026-07-01).** New Pro-only practice feature: play VS against a client-side CPU (Easy/Medium/Hard). Architecture — the bot is a drop-in `IMatchService` (`LocalBotMatchService`, apps/web/lib/adapters/bot-match-service.ts) behind a stable-identity facade (`SwappableMatchService`) that hot-swaps the socket transport without re-registering handlers, so the ENTIRE VS UI (intro splash, tug-of-war header, opponent mini-boards, result) is reused unchanged and the socket server is NEVER touched (server was too socket-coupled — ~20-30 emit sites — for a server bot). `bot-engine.ts` builds a difficulty-driven trajectory (real dictionary solver for standard word modes so revealed words are legit; fabricated converging path for ProperNoundle), mirrors server timing (match_found → +3s match_start) + the composite winner rule. `bot-personas.ts` = Rook/Lexi/Nova roster + banter (banter wired in Phase 2). vs-game.tsx: 'Play the CPU' chooser + auto-offer after 15s empty queue + CPU chip on intro/header + onMatchEnded CPU branch. Pro-gated in UI (non-Pro → /pro); added to Go Pro selling points. `recordCpuResult` writes ONLY user_stats(play_type='vs_cpu') — no XP/matches/daily/achievements → never ranked, never leaks into leaderboard/H2H/streaks/records. Profile VS tab shows a separate dashed 'vs CPU · Practice' box. Migration 20260701000001 (user_stats.play_type += 'vs_cpu') — USER MUST RUN. tsc clean; /practice/vs serves 200. REMAINING per plan: Phase 2 fun layer (photo-finish/ghost races/boss ladder+streak/run-it-back/adaptive/bot-of-day/cosmetics), Phase 3 web multi-board modes, Phase 4 iOS, Phase 5 Android. **(58) Per-mode longest_streak record + win-streak clarification (iOS build 73, 2026-07-01).** Completed the all-time-records port — added the 8th/last check skipped in build 72: per-mode longest_streak. New fetchModeBestWinStreak (iOS RecordsService + Android RecordsService) walks the newest 200 `matches` rows for the mode (web stats-service.ts fetchModeWinStreak parity: OR(player1_id,player2_id) filter, eq game_mode, created_at DESC, limit 200) and computes the best consecutive winner_id==user run; fires only on a win, keyed (longest_streak, gameMode, playType) — distinct from the global (null,null) longest_streak. CLARIFICATION for the record: a WIN-streak stat already existed — profiles.current_streak/best_streak does `won ? current+1 : 0` (resets on any loss), separate from daily_login_streak/best_daily_login_streak (the play/login streak). No new stat needed; this only adds the per-mode all-time record derived from match history. iOS CURRENT_PROJECT_VERSION -> 73, uploaded to TestFlight (Delivery b4accd59-8f48-40d4-aed4-57a28ea5ee0b; supersedes build 72's Delivery c26ac144). Android compiles clean. **(57) All-time records ported to native (iOS build 72, 2026-07-01).** Closed the last VS-6-audit deferral: native games now write the all_time_records "hall of records" rows that were web-only. New iOS RecordsService.checkAndUpdateRecord + updateAfterGame (fire-and-forget from GameResultsService.record after stats/profile updates); new Android RecordsService mirrors it (wired into record()). 7 record types: fastest_win / fewest_guesses / most_games_played (per-mode), longest_streak / highest_level / most_gold_medals / most_daily_completions (global null,null). Per-mode longest_streak skipped (needs game-history streak computation). Fetch-then-update-by-id like web checkAndUpdateRecord so NULL-unique global keys never accumulate duplicate rows; all writes best-effort. iOS CURRENT_PROJECT_VERSION -> 72, uploaded to TestFlight (Delivery c26ac144-87de-4709-9785-21cd8ae8bc8e). Android compiles clean. §7 build log updated. **(56) Cross-platform VS 6-audit sweep (2026-07-01).** Ran 6 parity audits across web/iOS/Android. RESULT: mostly clean, 2 real bugs fixed. (1) SOLUTION AGREEMENT across all 9 VS modes: 8/9 MATCH (byte-identical word lists + shared ×31 djb2 simpleHash + identical multi-board collision-avoidance + identical gauntlet 21-solution staging). BUG: Android ProperNoundle.puzzleForSeed used FNV-1a (ported before the iOS fix) → Android VS raced a DIFFERENT proper noun; fixed to simpleHash (×31). Critical since PN VS was just enabled on Android (#55). (2) RESULT RECORDING: BUG — iOS recordResult never wrote the daily_results VS row on a normal finish (only forfeit) while web+Android do → iOS daily VS missing from leaderboard/already-played/3-wins achievement; added recordVs for dailyVsActive (iOS BUILD 70, Delivery 9864f6fd-9a93-479c-a543-5c1f2c88b7ca). (3) WINNER/DRAW/BOTH-LOSE display: consistent. (4) FORFEIT tag: MATCHES (written+displayed all 3, amber #fef3c7/#b45309). (5) TUG-OF-WAR lead bar: MATCHES (0.7×boards+0.3×greens, clamp 0.1..0.9). (6) VS ACHIEVEMENTS: MATCHES (same keys/thresholds/call-sites). CONSISTENT-not-divergent (noted, unfixed): draws recorded as losses on all 3; all-time-record (fastest_win/fewest_guesses) writes deferred on native per bible §7 (web writes them live). **(55) Android VS: closed ALL documented deferrals (2026-06-30).** The deferral comments (VSMatchViewModel.kt / VSLobbyScreen.kt) were largely STALE — Android already had InviteService.createInvite, AchievementService.checkAchievements (called from GameResultsService.record → so VS achievement writes already fired), and the Gauntlet engine + stage socket handlers. Work was mostly WIRING: (1) GAUNTLET VS — added GAUNTLET+PROPERNOUNDLE to VS_MODES; wired vm.onStageCompleted→service.stageCompleted; render the 5-node GauntletStepper + StageTransitionOverlay in the VS match screen (reused solo composables, made GauntletStepper/ProperNoundleHints non-private). (2) PROPERNOUNDLE VS — same unified GameViewModel path; render the clue text + Clue/Vowel/Consonant hint pills in VS. (3) PRIVATE INVITES — added InviteService.lookupMode + markInviteAccepted; VSLobbyScreen PRIVATE MATCH section (2-col create-invite grid + join-by-code) via a new onEnterInvite→vsInvite→VSGameScreen(inviteCode) path; markInviteAccepted in handleMatchFound. (4) ACHIEVEMENTS — already worked (stale comment). All comments refreshed. :app:assembleDebug green. Android now has full VS mode + feature parity with iOS. NOTE: aesthetic parity of Gauntlet/PN VS + the invite UI is compile-verified only — needs an on-device pass. **(54) ProperNoundle VS desync fix (iOS build 69, 2026-06-30).** AUDIT of ProperNoundle VS found a real correctness bug: iOS ProperNoundle.puzzle(forSeed:) (ProperNoundleEngine.swift) selected the proper noun via FNV-1a, but the server's selectProperNoundlePuzzle — and web, which plays the server's puzzleMetadata — uses the ×31 djb2 hash (JS ((h<<5)-h+char)|0, abs%count, the SAME hash as the shared word-seed simpleHash). Different hash → different index → the iOS player and the web/server opponent were handed DIFFERENT proper nouns and raced different words; the result screen even revealed the server's answer, not the one iOS solved. FIX: iOS now uses the ×31 djb2 hash (the puzzle JSON is byte-identical across platforms — apps/web/data == apps/ios/.../Resources — so matching the hash yields the same index → same puzzle). Consistent now for iOS-vs-web, iOS-vs-iOS, and daily VS. The server-metadata-dropped concern the audit flagged is moot (iOS's local puzzle now agrees with the server). Everything else in PN VS verified SAFE (single-board, no fan-out, reconnect/forfeit shared paths, clue derived from the same display on both). Android PN VS remains DEFERRED (no ProperNoundleVM — mode not offered on Android). **iOS BUILD 69** (Delivery UUID a3dd75e3-e6ee-4054-b24b-6f438bef2af8, TestFlight-only). **(53) iOS VS match-intro polish (build 68, 2026-06-30).** The match-found "clash" splash (VSIntroView) had two issues: (a) the white avatar ring visually DETACHED from the photo during the slam because the async avatar image and its stroke overlay composited as separate GPU layers → fixed with .compositingGroup() so they animate as one; (b) the slam felt rushed (both cards snapped in simultaneously on a stiff spring response .5/damping .6) → now a softer/slower spring (0.72/0.72) with the opponent card landing +0.12s later (staggered duel clash), VS-pop + h2h re-timed to follow the settle; total still 2.5s so the pre-match countdown beat is preserved. iOS-only (web match-intro.tsx unchanged — the ring-detach is a SwiftUI compositing artifact; offer to mirror the softer timing to web if wanted). **iOS BUILD 68** (Delivery UUID b90552ea-94af-46f2-b6b2-6ed7d562d911, TestFlight-only). **(52) VS: all opponent boards for applyToAll modes (server fan-out + 3 clients, build 67, 2026-06-30).** Previously the opponent HUD only ever populated board 0 for quordle/octordle/rescue (one guess hits all boards but the client sent ONE submit_guess → server evaluated only board 0). FIX server (apps/server/src/index.ts): APPLY_TO_ALL_MODES={QUORDLE,OCTORDLE,RESCUE}; submit_guess now evaluates the guess against every still-unsolved board and emits latestGuesses[] (per-board tiles), tracking playerState.solvedBoardSet (populated in board_solved) to skip solved boards. Sequence/single-board keep latestGuess; Gauntlet excluded (stage-based, 21-board count). Clients (web vs-game.tsx, iOS VSMatchViewModel.applyOpponentProgress, Android VSMatchViewModel) apply latestGuesses[] into opponentTiles. UX (user decision): the in-play opponent strip renders per-board mini-grids ONLY for totalBoards<=4 (QuadWord/Deliverance/Succession) — OctoWord(8) stays summary-only during play (8 tiny grids over your own 8 boards are illegible/space-stealing); the spectator "still playing" screen already renders ALL boards at 16px (now filled by the fan-out). Gauntlet falls out of the <=4 gate → shows Stage N. New wire field opponent_progress.latestGuesses (typed). **iOS BUILD 67** (Delivery UUID ba6915e1-2ce1-470e-b0b3-efeec65569f7, TestFlight-only). **(51) VS board-index parity + multi-board VS audit (2026-06-30).** Brought the Sequence/Succession board-index relay to parity across all platforms and audited the other multi-board VS modes. (a) ANDROID: mirrored the iOS build-66 fix — GameViewModel captures the active board before the reducer and relays it via onGuessCommitted(guess, boardIndex); also covers the Gauntlet 'Succession' stage (sequential stage inside GAUNTLET mode) via gauntletStages[currentStage].sequential. (b) WEB: vs-succession.tsx AND vs-gauntlet.tsx (sequential stage) had the SAME latent hardcoded-0 bug — now relay the active board index too, so web is correct AND consistent with native for cross-platform VS. iOS build 66 already covered both standalone Succession and the Gauntlet Succession stage (its isSequence includes currentGauntletStage.sequential). AUDIT of the other modes (QuadWord/quordle, OctoWord/octordle, Deliverance/rescue): these are applyToAll (one guess hits all boards at once) and send ONE submit_guess with boardIndex 0 on EVERY platform incl. web, so the opponent HUD only populates board 0's mini-board (boards 1+ stay empty) while the boards-solved COUNT + attempts are correct. This is a CONSISTENT web=native limitation, NOT the Succession-class wrong-data bug (colors aren't evaluated against the wrong board). Fully showing all opponent boards for applyToAll modes would need a server-side per-board evaluation + an array of per-board tiles in opponent_progress + all clients rendering it (bigger cross-platform change) — deferred pending user call. **(50) VS reconnect grace period (server, 2026-06-30).** A mid-match disconnect (phone call, notification, screen lock — anything that backgrounds the app and drops the socket) instantly forfeited the player: the opponent saw a WIN while the disconnected player kept playing locally, disconnected. FIX (apps/server/src/index.ts): a signed-in player's mid-match drop now holds the match open for 60s (RECONNECT_GRACE_MS) via pendingReconnects (keyed by presence id) instead of forfeiting; Socket.IO auto-reconnects with the same presence id in the handshake, and the connection handler re-binds the new socket to the match (re-points player slot socketId/id + playerToMatch) and resumes. Only on grace expiry does it forfeit. Anonymous players still forfeit immediately. New typed events opponent_disconnected/opponent_reconnected/match_resumed (client handlers are future polish — server-only fix is sufficient). Deploys via Railway. **(49) iOS VS Sequence board-index relay fix (build 66, 2026-06-30).** In Sequence/Succession VS the guess relay (VSMatchViewModel.onGuessCommitted) hardcoded boardIndex:0, so the server evaluated every guess against board 0's solution and told the opponent all tiles belonged to board 0 — board 1+ never populated on the opponent mini-board and post-board-0 tile colors were wrong. (board_solved already carried the right index → the N/4 count was correct; only per-board tiles were broken.) CONFIRMED at runtime via a temporary /debug/vslog ring buffer (a board-1 solve arrived as boardIndex:0 isCorrect:false). FIX: GameViewModel captures the active sequence board index BEFORE the reducer runs (it advances once a board is solved) and passes it through onGuessCommitted(guess, boardIndex); the relay sends that. Single-board / quordle applyToAll modes still send 0. **iOS BUILD 66** (Delivery UUID 6f7db771-6a7a-4c4a-a1ba-1a0c908c920e, TestFlight-only). Diagnostic /debug/vslog + /version endpoints remain temporarily. **(48) VS private-match self-pairing fix (server, 2026-06-30).** BUG: creating + sharing a private VS code instantly matched the CREATOR against himself (JPGolf vs JPGolf). Private lobbies were deduped/guarded by per-socket `playerId` (= socket.id), which changes on every reconnect. When the creator's socket reconnected (the iOS share sheet briefly backgrounds the app → onConnect re-fires join_queue), the server no longer recognized the creator's own parked lobby (keyed by the OLD socket.id) and paired it against the new socket → self-match. FIX (apps/server/src/index.ts join_queue invite branch): dedupe + self-match-guard by the STABLE presence id (`u:<userId>` from the handshake), stored as `lobby.presence`, not playerId. Deploys via Railway. **(47) VS rematch "No guesses" fix (server, 2026-06-30).** BUG: after a VS rematch, the result screen showed "No guesses" for the opponent's board on BOTH sides even though both played. The offer_rematch handler created the new match and seeded `submittedWordsByMatch` but never seeded `guessLogByMatch` for it — so submit_guess's `const log = guessLogByMatch.get(matchId); if (log) ...` found undefined and silently skipped logging every rematch guess; endMatch then emitted an empty opponentGuessLog. (createMatch seeds it, so first matches were fine.) FIX (apps/server/src/index.ts offer_rematch): `guessLogByMatch.set(newMatchId, {p1:[],p2:[]})` + delete the old. Deploys via Railway. **(46) VS forfeit double-loss fix (server, 2026-06-30).** BUG: when a VS opponent tapped "Forfeit & Leave" (emits `abandon_match`), the server's handler called `endMatch(matchId)` WITHOUT `forfeitBy`, so the winner was derived purely from board state — the abandoner is ABANDONED and the remaining player still PLAYING, neither WON, so winner stayed `null` and BOTH players recorded a loss. (The disconnect handler was already correct, passing `forfeitBy`.) FIX (apps/server/src/index.ts abandon_match): pass `endMatch(matchId, playerId)` so the remaining player is credited the win, matching the disconnect path. Surfaced by the build-65 keyboard bug (unplayable VS classic daily → both players forfeited → both saw an L). Deploys via Railway (server.wordocious.com). **(45) iOS VS daily keyboard fix (2026-06-30).** BUG (iOS only): the classic-mode daily VS match showed the iOS SYSTEM keyboard over the custom on-screen KeyboardView (board unplayable). Two causes, both fixed in VSGameView.swift: (a) VSGameView never called `.hidesBottomNav()`, so the custom BottomNav rendered over the pushed game and ate the bottom safe area, clipping the keyboard's bottom row (solo games hide it via fullScreenCover — mirrored here); (b) the game-start interstitial ad leaves a web-view text input as first responder, so iOS kept the system keyboard up — now `.onChange(of: vm.screen)` resigns first responder the instant the playable `.match` screen appears (the VS board uses no UITextField). **iOS BUILD 65** (Delivery UUID d553f7ce-d929-42ad-87d3-60b9a06a0ff5, TestFlight-only). **(44) iOS leaderboard "Play" → solved board for finished dailies + Gauntlet score backfill (2026-06-30).** BUG (iOS only): the Leaderboard tab's Play CTA always launched a fresh GameScreen, which resumes only from LOCAL persistence — so a daily finished on another device (or after cleared local state) re-opened as a blank board instead of the completed review. (Home cards were fine: they use the server-backed SolvedPuzzleView.) FIX (LeaderboardTab in ProfileTab.swift): if you've finished today's daily for the selected mode it opens read-only SolvedPuzzleView (matches-row reconstruction, cross-device); otherwise still launches a fresh playable board. Button reads "View" vs "Play". Completion answered instantly from a cached DailyCompletionsStore (zero flash) + confirmed by the per-mode leaderboard rank. Android needed NO change — its GameScreen already server-reconstructs a played daily on entry (fetchRecordedDailyMatch → installReplayedState → PostGameScreen). This was a pre-existing gap, not from the scoring/guard work. **iOS BUILD 64** (Delivery UUID 4d49b5f2-57c5-4e71-9d4f-68eb9bd8742e, TestFlight-only). DATA: the user retroactively backfilled GAUNTLET-loss composite_scores to the new stage-depth ladder via a single SQL UPDATE in Supabase (derives stagesCompleted + failed-stage boards purely from boards_solved, since sequential play makes the cumulative-board split deterministic; thresholds [1,5,9,13,21] → ladder [0,25,55,95,150] + 6×failedStageBoards). Single-board near-miss losses were NOT backfilled (green-letter count isn't persisted in daily_results) — going-forward only.  **(43) Cross-platform composite-scoring guard (2026-06-30).** Automated parity test so the daily composite score can't silently drift across platforms. Web extracted to a pure lib/composite-scoring.ts (no runtime imports; daily-service.ts re-exports it). One generator (apps/web/scripts/gen-composite-scoring-fixtures.mjs, runs the web source-of-truth fn over 22 canonical cases incl. the new Gauntlet ladder + single-board near-miss) writes an identical composite-scoring-fixtures.json into all 3 test dirs. Guards: web `pnpm --filter @wordle-duel/web exec ... ` → `node scripts/check-composite-scoring.mjs` (npm `test:scoring`); iOS moved DailyScoring.swift app→WordociousCore (public) so the SPM test target asserts it (Tests/CompositeScoringFixtureTests, `swift test`); Android app/src/test CompositeScoringFixtureTest (`:app:testDebugUnitTest`). NOTE: the older scoring-fixtures.json is VS calculateScore — different file. No new iOS TestFlight build needed (DailyScoring relocation is behavior-identical; ships with the next build). All three green.  **(42) Loss-scoring overhaul — Gauntlet stage depth + single-board near-miss (3 platforms, 2026-06-30).** Losses now reward progress in the composite score (web computeScoreBreakdown = source of truth, mirrored 1:1 in iOS DailyScoring.swift + Android DailyScoring.kt). Only the loss-path completionBonus changed; wins + other multi-board modes (Quad/Octo/Succession/Deliverance) untouched. GAUNTLET loss = STAGE_LADDER[stagesCompleted] + 6×boardsInFailedStage (ladder [0,25,55,95,150], stage boards [1,4,4,4,8]); single-board loss = 12×bestCorrectLetters (greens in best guess, hint rows excluded). So stage-4 Gauntlet loss ≈150-186 vs stage-2 ≈55-73; Classic 4-green near-miss = 48 vs whiff 0; a loss stays well under any win (1,200+). Threaded two optional inputs (stagesCompleted, bestCorrectLetters) through the record pipeline + post-game ScoreBreakdown on all 3 platforms, computed from data already at each call site; the post-game card relabels the loss row to "Stage progress · N/5 stages cleared" / "Near miss · N correct letters". NO DB migration (composite_score exists; the inputs aren't persisted) and no retroactive rewrite (records update only when the new score is higher). NOTE: the shared scoring-fixtures.json is VS calculateScore only — NOT the daily composite — so it was correctly left untouched; daily-composite parity is guaranteed by the three identical DailyScoring implementations. Docs synced: FAQ/Help/About/Support via centralized static-content.ts (feeds /api/content for web + native) + landing copy + gauntlet-results comment. **iOS BUILD 63 cut + uploaded (Delivery UUID d51ecee7-6931-48b2-b9c5-95395c23df13, TestFlight-only).** web tsc+build, iOS build, Android compile+:core:test all green.  **(41) Records "You"-tab polish + 6 new achievements (3 platforms, 2026-06-30).** iOS Records "You" tab: stat cells (Daily Sweeps + per-mode board stats) recentred as balanced tiles (icon→value→label, matching the profile global-summary cards) instead of left-hugging; bottom padding 16→80 so the page scrolls fully past the custom bottom nav. Achievements +6, rounding each grouped column to a clean multiple of 3 (consistency 19→21, skill 37→39, social 7→9): streak_14 "Fortnight" (14-day login), flawless_10 "Flawless Ten" (10 flawless days), quick_draw "Quick Draw" (<10s win), sharpshooter "Sharpshooter" (25 lifetime 1-guess wins), vs_centurion (100 VS wins), vs_marathoner (100 VS played) — all non-duplicative of existing keys. Catalog single-sourced in lib/achievement-service.ts (native fetch /api/achievements); detection added to all three per-platform engines by extending existing query blocks. iOS BUILD 60 (Delivery UUID cb4d574d-ec4f-4683-a5b1-ee82d632fee3, TestFlight-only). Web tsc + iOS build + Android compile green.  **(40) Profile & Records expansion — Waves A+B (all 3 platforms, 2026-06-30).** Depth-not-new-charts work, web-first then native, all from existing fetchers (no backend invention). **Records Wave A:** a third "You" tab (personal bests per mode, sweep/flawless totals, medal totals, all-time records held) alongside Daily/All-Time; a rank percentile line ("#N of M · top X%") + yesterday's-podium recap on the Daily tab (Android also gained the user-rank + RankDelta badge it was missing). **Profile Wave B:** P1 achievements glow-up (moved above Recent Matches, always-expanded, grouped by category w/ per-category counts + progress bars on derivable locked ones — streak_7/30, medal_10/50); P2 trophy case (medal history "View all N" gallery; Android previously showed only the 3 count cards); P3 "This Week" recap hero card (games this week, win streak, XP-to-next + a single Pro upsell hook); P4 shareable stats card (web 'profile' layout in lib/share-image.ts → drawProfileCard, a 1080² PNG via the existing share pipeline + shareResult; native P4 share-card **now shipped in build 58** — iOS ProfileShareCardView (SwiftUI→ImageRenderer) + Android ProfileShare (Canvas), 1080² stats PNG via the share-images bucket + /s?m=Profile link, "Share" pill in both profile headers). Web all four shipped + deployed (tsc + next build green); iOS + Android P1-P3 committed (xcodebuild BUILD SUCCEEDED / compileDebugKotlin SUCCESSFUL). **iOS BUILDS 57 (Wave B P1-P3) + 58 (P4 share + WOTD "Baton" definition-cache fix) cut + uploaded (2026-06-30):** Build 58 Delivery UUID 1f99dff4-bafe-4eda-a7ba-5ab8a87acd8e. (57:) CURRENT_PROJECT_VERSION 56→57, usual unsigned-archive→export→re-sign-applesignin→altool recipe, Delivery UUID f288d6e9-b52e-40dc-8e05-4580adc4a4c7; TestFlight-only (not swapped into review), manual release. Profile Wave B is now COMPLETE on all three platforms (P1-P4). Remaining minor follow-up: Android emulator board-fit check; share-card boldness FIXED in build 59 — iOS share renderers now use Brand.font (drives the NSCTFontVariationAttribute wght axis) and Android nunito() selects the real weighted instance via Typeface.create(base, 900/700) on API 28+ (dropped faux-bold); sweep/flawless + single-game + profile cards all render true bold. Build 59 Delivery UUID daa40987-118f-4414-8048-f7169a5903c9. **(39) Edit Profile glow-up + personalization (all 3 platforms, 2026-06-26).** The plain system-Form Edit Profile was restyled to the app chrome (accent bar + gradient `EDIT PROFILE` title + card sections + a LIVE PREVIEW card) and gained four expressive fields: a **Bio** (≤80 chars), a **Featured Title** (wear any *unlocked* achievement as a ★ pill — only the user's `achievements` rows are offered), a **Personal accent color** (8-swatch palette — purple default + blue/teal/green/amber/pink/red/slate — that tints the username + the avatar-fallback gradient + the title pill), a **Favorite-mode** chip, and an optional **avatar emoji**. The 8-swatch palette is single-sourced and byte-identical across web `lib/profile-personalization.ts` ↔ iOS `ProfilePersonalization.swift` ↔ Android `ProfilePersonalization.kt`. One additive, nullable migration `supabase/migrations/20260626000001_profile_personalization.sql` adds `bio`(CHECK ≤80)/`featured_achievement`/`accent_color`/`favorite_mode`/`avatar_emoji` + `NOTIFY pgrst` — **MUST be applied (Supabase SQL editor) before saving works**: the web save payload now always includes these columns, so an un-migrated DB rejects ALL profile saves (incl. username) on the live site. Display: own + public profile headers tint the username + render the title pill + bio + favorite-mode chip under the name (web `app/profile/page.tsx` + `app/profile/[id]/page.tsx`; iOS `ProfilePersonalizationRow` in ProfileTab/PublicProfileView; Android `ProfilePersonalizationRow` in ProfileScreen/PublicProfileScreen). Save encodes nils as null so fields can be cleared. Fields read defensively (web `as any`, native optional decode + `select *`) so the code shipped before the migration. Web deployed (tsc + build green); iOS (`xcodebuild` BUILD SUCCEEDED) + Android (`:app:compileDebugKotlin` SUCCESSFUL) committed. **iOS BUILD 49 cut + uploaded (2026-06-26):** CURRENT_PROJECT_VERSION 48→49, usual recipe (unsigned archive → exportArchive via ASC API key → re-sign applesignin cert E834629E… + ent.plist → repackage → altool), UPLOAD SUCCEEDED Delivery UUID 7976c554-2ad4-464e-abaf-3e90fb21dcef; NOT swapped into App Review (TestFlight first; manual release; build 36 still approved/pending-release). Build 49 carries the profile personalization (push 39) PLUS the batched build-48-era work since the last upload (uppercase menu/nav titles, FAQ-pill removal, Settings info-page flicker fix). Android edits ride the eventual Play build. **(40) Fix — Gauntlet Succession stage played as QuadWord (iOS + Android, 2026-06-26, build 50).** The Succession Gauntlet stage (stageIndex 2, `sequential:true`) rendered as a parallel QuadWord — all 4 boards shown with colors + a quadrant keyboard — instead of sequential play (one active board, future boards masked with bullets, single active-board keyboard). Root cause: the view-layer sequential flags keyed off `mode == .sequence`/`GameMode.SEQUENCE`, but inside Gauntlet the mode is `.gauntlet`, so a sequential stage was never detected (web already keys off the stage's `sequential` flag, gauntlet-game.tsx). Fix: made the flag stage-aware on both natives — `isSequence = mode == .sequence || currentGauntletStage?.sequential == true` (iOS GameViewModel; Android `state.gauntlet.stages[currentStage].sequential` in GameScreen) — which flows into useQuadrantKeyboard (now single keyboard), board masking, and active-only keyboard letter states. `applyToAll` stays true (the intended Succession carry-forward — guesses populate all boards, you just play/view one at a time), so no reducer change. Web already correct. iOS BUILD 50 cut (49→50, usual recipe), UPLOAD SUCCEEDED Delivery UUID 5b49efee-fbc5-49f0-b155-54b2bb82dbd8; NOT in review (TestFlight first). Android rides the Play build. **(41) Fix — Gauntlet win not recorded if you leave the final cleared screen (iOS + Android, build 51).** A fully-cleared Gauntlet only wrote its daily_results row when the player TAPPED through the final "Tap to see your results" overlay — that overlay waited forever for a tap (no auto-advance, unlike the 2.5s between-stage advance), and nextStage()/record only fires on advance. Leaving first (Home/swipe) dropped the row, so a genuine Flawless showed as an 8/9 Daily Sweep (Gauntlet simply absent from the modal grid), self-correcting only after re-entering the cleared screen and tapping through. Diagnosed live with the user (they left the final screen, re-entered → it recorded → W populated → banner flipped to Flawless). Fix: the final-stage overlay now AUTO-ADVANCES after a 4s pause instead of waiting indefinitely (advancing = recording); iOS adds a belt-and-suspenders `GameScreen.onDisappear → vm.finalizeGauntletIfCleared()` (idempotent; records on the way out if they leave within 4s). Android records via a GameScreen `LaunchedEffect(isFinished)` so the auto-advance covers the on-screen case (a VM-level record move would be needed to also cover leaving within 4s — deferred, no live Android channel). iOS BUILD 51 cut (50→51, usual recipe), UPLOAD SUCCEEDED Delivery UUID 67cfceb7-5fd0-4de2-b03d-820d0a24af5c; NOT in review. NOTE: the sweep/flawless math itself was correct + unchanged (verified via git history — the "icon fix" build 43 only touched icon rendering); this was purely the missing Gauntlet daily row. **(42) Fix — flawless mis-count (read-after-write race) + Unlimited Play Again + web sweep-banner clip (iOS + Android + web, build 52).** User saw the Flawless-shows-as-Sweep recur with the just-finished mode (any mode, not just Gauntlet) missing from the modal grid + an N-1/9 count. Root cause: on a daily finish the mode is added to byMode optimistically (completionPosted note), but `.onDailyCompletion`/completionTick immediately calls load(), which re-fetches daily_results and BLIND-REPLACES byMode — and the just-INSERTed row often isn't queryable yet (read-after-write), dropping the last mode (self-heals on a later refresh). Fix: load() now MERGES, keeping any locally-known finish (won OR lost — the all-9 count needs losses) the server response still lacks — iOS `DailyCompletionsService.load`, Android `fetchTodayCompletions`. Together with build 51's Gauntlet auto-record, flawless populates reliably. ALSO (B) iOS Unlimited "Play Again" showed the old finished board — the fullScreenCover seed swap didn't rebuild GameScreen's @StateObject (created once from the old seed); added `.id(g.seed)` to force a fresh view+VM. ALSO (C) web home sweep/flawless banner was vertically clipped — a flex item in a flex-1/min-h-0/overflow-y-auto column compressed under default flex-shrink + its own overflow-hidden clipped the text; added `shrink-0`. iOS BUILD 52 cut (51→52), UPLOAD SUCCEEDED Delivery UUID ff4f30e8-fcfd-4004-9845-91340ad2f32c; NOT in review. Web deployed; Android batched. **(43) Daily Sweep / Flawless share card — real game icons in the badges (iOS + Android, build 53).** The all-dailies share PNG drew a bare letter glyph (C/S/D/G/P + IV/VIII/6/7) in each mode's accent badge; now it draws the actual game icon (same lucide/hand/roman art as the home cards + celebration modal) WHITE on the accent square — Classic=grid, Succession=trending-up, Deliverance=shield, Gauntlet=skull, Proper=crown; QuadWord/OctoWord keep IV/VIII, Six/Seven the hand+numeral. iOS `DailySweepShare.shareGlyph()` switches on the mode's ModeIconKind (homeModes by dbKey); Android draws the lucide vector via ContextCompat.getDrawable tinted white. Covers BOTH the Sweep + Flawless cards (one shared layout). iOS BUILD 53 cut (52→53), UPLOAD SUCCEEDED Delivery UUID 0a693c65-0333-4dc0-9412-90d66fae5e2f; NOT in review. **WEB DONE (same push):** the web share-image (`lib/share-image.ts`) now draws the real game icons too — `drawSweepBadgeIcon()` strokes the exact lucide paths (copied verbatim from lucide-react@0.446: trending-up/shield/skull/crown) via Path2D on the client browser canvas (the share PNG is generated client-side, full Path2D support), plus the 5×6 white grid for Classic; numeral modes (IV/VIII/6/7) keep their glyph. So all three share cards (web + iOS + Android) now show matching game icons. Web `tsc` + `next build` (66 pages) green, deployed. **(44) Word-of-the-Day entry title + launch-skeleton skip (iOS + Android + web, build 54).** (a) Tapping the Word-of-the-Day card now opens the words list titled "WORD OF THE DAY" (was "PAST WORDS") — it leads with today's word + its strategy, so the title now matches: iOS `WordsView(navTitle:)`, Android `pastwords` route navTitle, web `/words` h1; the "?" menu "Words" entry + SEO metadata unchanged. (b) The split-second loading skeleton (iOS) / spinner (Android) on launch is skipped for returning signed-in users: a persisted `had-session` hint (UserDefaults `wordocious.had-session` / SharedPreferences via SettingsPref, set on sign-in, cleared on sign-out) lets the gate render the home IMMEDIATELY while the session quietly restores in the background (home draws from cache/static, fills in as session+profile load); falls back to the auth screen if the restore reveals no session; first-ever launch still shows the loading state. iOS BUILD 54 cut (53→54), UPLOAD SUCCEEDED Delivery UUID 2b37588c-a56c-482b-b074-139b75f9a055; NOT in review. Web deployed; Android batched. **(45) App-wide native BOLDNESS fix + web header PRO-clip (build 55).** User compared web vs native side-by-side and found native looked far lighter. ROOT CAUSE: `Brand.font` (Theme.swift) did `.custom("Nunito", size:).weight(w)` on a VARIABLE Nunito.ttf whose DEFAULT named instance is **ExtraLight** — and SwiftUI's `.weight()` does NOT reliably drive a variable font's `wght` axis, so most bold/black text rendered near ExtraLight app-wide. Fix: Brand.font now builds a UIFont with the `wght` variation axis set explicitly (`NSCTFontVariationAttribute` [0x77676874: numericWeight]) — regular 400 / semibold 600 / bold 700 / heavy 800 / black 900, cached; falls back to `.system(.rounded)` if Nunito missing. Verified via CoreText that the axis maps correctly (wght 900 → CTFontWeightTrait 0.8). Now the native app matches the web's weight. (Share-card PNGs still use `.custom("Nunito").weight()` directly — same latent bug, deferred; they were acceptable.) Web: the AppHeader was `justify-between` with no min gap, so the "WORDOCIOUS PRO" group met the help (?) button ("PRO clips the button next to it") — added `gap-2` + `shrink-0` on the buttons + `min-w-0 shrink` on the wordmark. iOS BUILD 55 cut (54→55), UPLOAD SUCCEEDED Delivery UUID 451e20f8-168d-426c-a8ba-90e77af9de40; NOT in review. Web header deployed. **Profile/Records expansion — Wave A (Records) DONE on all 3 platforms (build 56).** Plan `ancient-hugging-stearns.md`. New **"You" tab** in Records (third tab): per-mode personal bests (user_stats fastest/fewest/games/W-L), Daily Sweeps totals (dailySweepStats), a "Next Up" milestone card (progress to the next 7/30/100-day streak shield + the closest all-time record you're near beating), and medals + global-records-held — web `app/records/page.tsx` (YourRecordsView), iOS `ProfileTab.swift` (YourRecordsView), Android `RecordsScreen.kt` (YourRecordsTab). Daily tab also gained the **"top X%" percentile** on the rank line + a collapsible **Yesterday's Podium** (top-3 of yesterday via fetchDailyLeaderboard/fetchYesterdayWinners(day:)), AND the Android Daily tab finally shows the **player-count + your-rank row** it was missing (LeaderboardService.playerCount + userRankAndTotal — web/iOS already had it). All reuse existing fetchers, no DB change, all FREE. Web deployed; iOS in BUILD 56 (55→56, usual recipe, UPLOAD SUCCEEDED Delivery UUID c46a5f7f-9337-471d-b72a-c507cdfebc6e, NOT in review — also carries the build-55 boldness fix); Android batched. **NEXT: Wave B = Profile (achievements glow-up, trophy case/full medal gallery, "this week" recap hero card, shareable stats card), web-first.** PRIOR header — Four big pushes since the last header: (1) **Royal palette re-theme** on all 3 platforms — Wordle's green/yellow tiles replaced by brand purple #7C3AED (correct) / amber #F59E0B (present) / slate (absent), with win states, share-card PNGs, and emoji grids (🟪🟧⬛) all moved to the violet family; colors are client-only (no DB storage), colorblind orange/blue untouched, web flows through CSS vars + `.tile-*/.key-*` utilities (`lib/tile-theme.ts` single source). (2) **Six-audit bug sweep** (recording matrix, double/zero-record, date/timezone, colorblind regression, Pro gating, XP/medals) → 3 fix waves: the phantom-win root cause (web restored-daily re-ran the full stats pipeline per session), Android Succession being unwinnable past board 1, iOS calendar-locale seed corruption, midnight day-from-seed recording, a pending-record retry queue, cron idempotency + the applied `medal_type` migration, the Android timer accumulator, and more. Royal re-theme passed the audit with ZERO regressions. (3) **Monetization plumbing**: free unlimited-play closed on web (`UnlimitedGate` on all 9 mode routes), a fail-closed Google Play RTDN webhook (`/api/playstore`) added alongside the Apple one, and AdSense's "low value content" denial remediated with 9 public `/guides` pages (re-review pending). iOS **build 20** (Royal + all audit waves) is the latest on TestFlight; build 10 remains APPROVED at Pending Developer Release (manual) pending the blessed-build swap. (4) **Native parity hardening** (web = source of truth): full profile-dashboard parity on both natives (Pro Stats self-fetch race fixed, Last 7 Days, Insights, section order, account section); a left-edge swipe-back gesture on solo iOS puzzles; Android predictive-back animation flag + Android **hosted share links** (PNG → `share-images/<uid>/<ShareMode>-<date>.png` Supabase bucket + `wordocious.com/s/<key>?…` URL, matching iOS ShareService); and a web-truth audit of the non-profile Android surfaces that fixed two confirmed gaps — Records Daily **Solo|VS toggle** (VS records were unreachable on Android) and the **home Sign Out** button — with Leaderboard, all 9 game modes, and Settings verified already at parity. The deferred Android gap (home Pro "Invite") is now CLOSED — the full outgoing-invite flow (InviteService.createInvite + a Compose InviteSheet matching the web InviteModal: mode picker, Share-link/Username tabs, copy/share, 24h-expiry) is ported and wired to a Pro-only LIVE-banner Invite button; the Android home footer links (About/How-to-Play/Privacy/Terms) are now tappable (route to InfoScreen/Help); and the game boards are vertically centered on iOS (BoardLayout `.center`, matching web `justify-center`; Android already centered). Bundled into iOS build 23. (5) **Gauntlet deep-parity pass** (web = truth): fixed the **final stage-clear gate** (the last StageTransition now waits for a tap instead of auto-advancing, on iOS + Android); discovered & fixed that **Android live Gauntlet never advanced stages** (no NEXT_STAGE dispatch in live play — added GameViewModel.advanceGauntletStage); persist + read **matches.gauntlet_stages** on Android (recordGauntletStages/fetchGauntletStages) enabling **cross-device Gauntlet review reconstruction**; added the Android Gauntlet **"Completed Today"** leaderboard card (3-stat + per-stage inline-expand + score breakdown) matching iOS; and **converged the score breakdown** onto web's Gauntlet completed card so all three platforms match. Also: ProperNoundle/hint **persistence** on native (iOS had none; Android hint-UI flags now persist). Bundled into iOS build 24 + Android versionCode 24. (6) **ProperNoundle + profile polish**: ProperNoundle post-game now shows the FULL un-redacted Wikipedia clue (with the answer) + photo and drops the useless dictionary "no definition" card on all three platforms (WikipediaHint gained a `redact` flag); the profile-page **Account section** (Daily Reminders / Sign Out / Delete Account) was removed everywhere (still in Settings); iOS Home+Profile got extra bottom-scroll clearance (recurring "can't reach the bottom items" report); and the **avatar picker gained a Camera option** (iOS confirmationDialog + UIImagePickerController .camera w/ NSCameraUsageDescription; Android AlertDialog + TakePicture), so "Change Photo" offers Take Photo or Choose from Library. Bundled into iOS build 25. (7) **Leaderboard Play-CTA card**: added the per-mode card (icon + title + "N players today" + orange Play button launching that mode's daily) between the mode grid and Completed Today on both natives (web parity) — iOS LeaderboardTab playCtaCard + fullScreenCover game launch; Android ModeInfoCard reworked with accent bar / ModeGlyph / Play button wired through MainScreen. Bundled into iOS build 26; the Play button was then restyled to the selected mode's accent color (rounded pill + play icon, dropping the off-brand orange) on all three — iOS build 27. (8) **In-game "?" guide sheet**: a help button top-right of every game (mirrors the Home button) opens that mode's strategy guide natively — the /guides content minus "Keep reading" (facts / How it works / How scoring works / Strategy) — closeable via X/backdrop/swipe, and reading it PAUSES the game clock. Content single-sourced via a new /api/guides edge route (natives fetch+cache it through GuideService and render natively; web reads guide-content.ts directly). iOS GuideSheet (.sheet) + Android ModalBottomSheet + web slide-up sheet; timer pause wired on all three (web via a shared setGuidePaused signal in useActivePlayTimer). Bundled into iOS **build 28** (uploaded to TestFlight, Delivery 6f198bba; re-signed applesignin; manual release). Builds 27/24/23 remain prior VALID builds; build 10 still APPROVED at Pending Developer Release pending the blessed-build swap (must be done in the ASC web UI). Build 23/22 remain prior VALID builds; build 10 still APPROVED at Pending Developer Release pending the blessed-build swap (must be done in the ASC web UI). (9) **Aesthetic "gradient titles" pass + build 31 SUBMITTED TO REVIEW (2026-06-16).** Restyled plain-black titles to brand/mode gradients on all 3 platforms: the in-game guide-sheet mode title now renders uppercase in that mode's in-game header gradient (e.g. Seven = lime); the info/help/settings page titles (About, Privacy, Terms, Support, How-to-Play, Game Modes, FAQ, Mode Guides, **Settings** gear, **Help** "?" header) use the brand wordmark gradient (#a78bfa→#ec4899); and the **profile username** (own profile) now uses the same yellow→pink→purple gradient the public profile already used. Also added **case-insensitive username uniqueness** — migration `20260616000001_username_case_insensitive_unique.sql` adds `CREATE UNIQUE INDEX ... ON profiles (lower(username))` so "BMT"/"bmt" can't coexist (clients already surface 23505→"Username already taken"; signup trigger already catches unique_violation); **APPLIED manually via the Supabase SQL editor** (the file is the source-of-truth record only — pushing to main does not run SQL). **App Store re-review:** the 1.0 version sat APPROVED/Pending-Developer-Release on **build 10** for weeks while builds 11–30 only ever hit TestFlight. Discovered the blessed-build-swap blocker: the App Store version build-picker only offered build 10 because builds 11–30 were built with `MARKETING_VERSION=1.0.0` while the version record is **1.0** (the picker filters by short-version match; a stale client list initially masked this). Per the user's call (keep public version "1.0"), set `MARKETING_VERSION` back to **1.0**, cut **build 31** (CURRENT_PROJECT_VERSION 31), re-signed applesignin (the export still strips it), uploaded (Delivery 249603f7) → VALID. In ASC: canceled build 10's release (→ Developer Rejected), removed build 10, attached build 31, verified the reviewer notes (ATT 5.1.2(i) + EULA 3.1.2 guidance + demo account) carried over intact, and submitted build 31 (v1.0) to App Review. **Then cut build 32** with three more fixes and **swapped it in for build 31 (now the build in review)**: (a) the guide example-word fix — the Six/Seven strategy sheets suggested dictionary-rejected openers (AUDITS/PLACED/SPHERES → AROUSE/RELOAD/FRESHLY, all verified in allowed-6/-7.json; rules unchanged, natives read it via /api/guides); (b) iOS invalid-word UX parity — `GameViewModel.rejectGuess` now clears `currentInput` 600ms after the shake to match web's post-shake `setCurrentGuess('')` (Android already did; covers all solo+VS modes); (c) the gradient titles/username work from build 31. Swap flow when a version is "Waiting for Review": click "remove this version from review" (→ Developer Rejected), swap the build, Add for Review, Submit. **Build 32 (v1.0) is the current submission → "Waiting for Review"** (manual release; up to ~48h). NOTE for next time: to swap a build you must remove/cancel the in-flight review submission first, and the candidate build MUST share the version record's short-version string — keep `MARKETING_VERSION` aligned with the ASC version (1.0). The ASC web session expires periodically — driving the swap via Chrome needs the user to re-auth (2FA). ASC build-state polling: `~/.appstoreconnect/asc_builds.rb` (Ruby ES256 JWT, key C8FRS9T697 / issuer 8bdd3f73-0d8b-427d-95c7-8097b77dfb7a). Builds 28/29/30/31 reached VALID but are superseded by 32. (10) **App Review REJECTION (build 32, 2026-06-17) → guest mode on all 3 platforms → build 33 resubmitted.** Reviewer cited **2.3.6** (Age Rating inconsistent — must set Advertising=Yes since the app has ads; FIXED in ASC App Information → Age Ratings, metadata-only) and **5.1.1(v)** (app required registration to access the single-player game; non-account features can't be login-gated). Fix = **guest mode**: a "Play without an account" entry on the sign-in screen lets a signed-out user play the DAILY puzzle of every solo mode; recording is already auth-guarded on all 3 platforms so guests write NOTHING (no stats/leaderboard/XP — stats system untouched); unlimited (Pro-gated), leaderboard, VS, profile, records, and Pro all still require sign-in (show a "Sign in" prompt). Implemented: **web** (auth-context `isGuest`+`enterGuest`, localStorage-persisted; AuthGate `!user && !isGuest`; Landing "Play without an account"), **iOS** (`AuthService.isGuest`; ContentView gate; AuthView button; ProView/VSLobbyView guest sign-in prompts), **Android** (`AuthService._isGuest`+enterGuest/exitGuest; MainActivity gate; AuthScreen button; Profile/VSLobby/Pro guest prompts where "Sign in" calls exitGuest→AuthScreen). All three build/typecheck green; committed+pushed. **Decision (user): guest = DAILY only, zero recording** — unlimited stays a paid/account feature, so guests never get it. iOS **build 33** cut with guest mode (MARKETING_VERSION still 1.0), re-signed applesignin, uploaded, swapped onto the 1.0 version (removed build 32 from review first), resubmitted. NOTE: the local web preview is pinned to the stale ~/Desktop working copy (and that copy's git is corrupted) — couldn't live-verify the web guest flow locally; verify on the Vercel deploy. (11) **Daily Sweep / Flawless Victory celebration + shareable all-dailies card + profile sweep stats (all 3 platforms, 2026-06-17).** When a player finishes all 9 daily puzzles they now get a **one-time-per-day full-screen celebration modal** with DISTINCT effects (NOT the reused per-game victory confetti): **Daily Sweep** = violet/pink sparkle burst; **Flawless Victory** = gold firework burst + foil shimmer — each shows total time + total score + a per-game list + a Share button, keyed on the local day and re-firing on a sweep→flawless upgrade (web localStorage / iOS @AppStorage / Android SettingsPref). The home **Sweep/Flawless banner** gained the summed **total time + total points** in its subtitle, a subtle foil-shimmer sweep, and **tap-to-share** (was: route to /daily). New **all-dailies share card** (`daily-sweep` layout): a 1080×1350 PNG listing all 9 daily modes — each row a mode-accent rounded badge with the mode glyph (C/IV/VIII/S/D/6/7/G/P) + "Ng · M:SS · X pts" + ✓/✗ — under a gold/violet "DAILY SWEEP"/"FLAWLESS VICTORY" header with the summed "X/9 won · time · pts · date"; uploads to the same `share-images` bucket and links `wordocious.com/s/<uid>/DailySweep-<date>?m=DailySweep&sweep=…` (the OG `/s/[...key]` page learned the `DailySweep` mode). New **profile "All"-view stats card** (renders only when no mode is selected): sweep & flawless counts, current sweep streak, avg/best sweep time, best flawless time, and a 30-day **daily-points-over-time** area chart with sweep (pink) / flawless (gold) day markers. Backed by W1 data plumbing: `daily_results.composite_score` now flows through the today's-completions fetch on all 3 (so banner/modal/share/totals always agree via one shared totals helper), plus two new stat fetchers (`fetchDailySweepStats`/`fetchDailyPointsOverTime` web; `MatchStatsService.dailySweepStats`/`dailyPointsOverTime` iOS+Android) querying `daily_bonuses ⨝ daily_results` grouped by day. Android's client-side sweep/flawless **detection already existed** (`MedalService.awardDailyBonusesIfComplete`, wired in GameResultsService) — the recon note claiming it was server-cron-only was stale. Verified: web `tsc` + `next build` green, iOS `xcodebuild` BUILD SUCCEEDED, Android `:app:compileDebugKotlin` SUCCESSFUL. All committed + pushed to main (web/server auto-deploy via Vercel). (12) **5 new Sweep/Flawless achievements — 70 → 75 (all 3 platforms, 2026-06-17).** The achievements grid held exactly 70 on every platform (web `ACHIEVEMENTS`, iOS `AchievementService.all`, Android `AchievementService.all`); 70 in a 3-column grid left one lonely bottom card. Added 5 new ones tied to the daily-sweep stat calcs, taking it to **75** (clean 25 rows). Keys (byte-identical across web/iOS/Android + the `achievements`/`daily_bonuses` data): **`flawless_5`** (High Five — Flawless on 5 different days), **`flawless_25`** (Flawless 25 — 25 Flawless Victories), **`flawless_streak_5`** (Flawless Streak 5 — 5 flawless in a row), **`sweep_streak_60`** (Sweep Streak 60 — 60-day sweep streak), **`flawless_speed`** (Flawless Blitz — a Flawless day with total time < 18 min). Grid badges already render the count **dynamically** (`…/${ACHIEVEMENTS.length}` etc.) so they auto-show /75 with no grid edit. Detection mirrored in each platform's `checkAchievements`: flawless-day count (new), raised the sweep/flawless streak query limits (sweep 30→90, flawless 3→10) + run-length thresholds, and `flawless_speed` rides the existing daily-sweep-speed block (the today's-`daily_results` query is already `completed=true`-filtered, so all-9-present == a Flawless day; Σtime < 1080s). **No DB/schema change** — all 5 read existing `daily_bonuses` + `daily_results`. Web admin backfill route (`/api/admin/backfill-achievements`) extended (flawless count, flawless_streak_5, sweep_streak_60, flawless_speed) so existing qualifying players unlock retroactively on next run. Bumped the four hardcoded "70 achievements" prose strings → "75" (web FAQ, web help-modal, iOS HelpView, Android HelpInfoScreens). Verified: web tsc + next build, iOS `xcodebuild` BUILD SUCCEEDED, Android `:app:compileDebugKotlin` SUCCESSFUL; 5 keys confirmed present on all 3. Committed + pushed. **iOS build 35 cut + swapped into review (2026-06-17):** bumped CURRENT_PROJECT_VERSION 34→35 (MARKETING_VERSION 1.0), archived UNSIGNED (CODE_SIGNING_ALLOWED=NO), exported with the ASC API key (key C8FRS9T697 / issuer 8bdd3f73-0d8b-427d-95c7-8097b77dfb7a), **re-signed the applesignin entitlement** (export strips it — re-signed frameworks + app with Apple Distribution cert E834629E… + /tmp/ent35.plist, signature verified), repackaged the IPA + uploaded via altool (Delivery 36c50e71) → polled VALID. Then drove the ASC swap via Chrome: removed build 34 from review (→ Developer Rejected), attached build 35, Saved, Add for Review → **Submit for Review → "1 Item Submitted"**. Build 35 (v1.0) now covers BOTH the Daily Sweep feature (note 11) AND these 5 achievements; manual release stays on. NOTE: the ASC build-picker had a multi-minute lag — build 35 hit VALID via the API ~immediately but didn't appear in the version's Add-Build picker until a force page-reload a few minutes later (the picker reads page-load-cached build data). (13) **Sweep-stats data backfill (2026-06-17).** The profile DAILY SWEEPS card / points chart / sweep achievements read `daily_bonuses`, whose rows are only written live the moment the 9th daily completes — so full-sweep days completed before that logic shipped had no row and weren't counted. Added two admin routes (Bearer $CRON_SECRET, getAdminSupabase service-role): **`/api/admin/backfill-daily-bonuses`** (reconstructs `daily_bonuses` from solo `daily_results`: all 9 daily modes present in a user-day → sweep_awarded; all 9 won → flawless_awarded; upserts flags only, no XP, idempotent, never downgrades) and **`/api/admin/backfill-sweep-xp`** (grants the missed +200/+400 ONLY for rows with `created_at >= sinceIso` — i.e. rows the flag-backfill just inserted = days provably never paid live; dry-run supported). **Ran the flag backfill (all users): 13 swept days / 6 flawless reconstructed**, then re-ran `/api/admin/backfill-achievements` → 6 inserts incl. the new `flawless_5`. **XP grant intentionally SKIPPED** — dry-run showed the heavy account's ~28 sweep rows are legacy (7-mode-era) and were already paid when earned (granting all-time would double-credit ~7,800 XP); only 1 recent row (600 XP) was a real candidate and the user opted to skip it (forward sweeps pay correctly on their own; no schema/ledger added). To pull prod env for admin curls: `vercel link --cwd apps/web --project spellstrike --yes` then `vercel env pull … --environment=production` (CLI authed as bterchin-1609); delete the pulled env file after. **Reusable pattern for future achievements:** add the def to all 3 catalogs (web `ACHIEVEMENTS`, iOS `AchievementService.all`, Android `AchievementService.all`) + mirror detection in all 3 `checkAchievements`, push, then run `/api/admin/backfill-achievements` to retro-unlock — grids size dynamically so the count badge auto-updates. (14) **Data centralization — single-source duplicated data across all 3 platforms (2026-06-17).** Three kinds of data were re-declared independently on web/iOS/Android (every change meant editing 3–6 files and risked drift). Centralized in three phases, all green on web `tsc`+`next build`, iOS `xcodebuild`, Android `:app:compileDebugKotlin`: **DC1 — static copy** (`/api/content` edge route, source `apps/web/lib/content/static-content.ts`): FAQ/Help/About/Support now fetched + **persistently cached** (UserDefaults / SharedPreferences) by a new `ContentService` on each native (renders from cache offline after first fetch). **Privacy + Terms stay native-hardcoded** (must render pre-sign-in + offline for store compliance). **DC2 — achievement catalog** (`/api/achievements`, source `lib/achievement-service.ts`): the 75-entry display catalog is web-only; iOS `AchievementCatalog` + Android `AchievementCatalog` fetch + cache it (the hardcoded `AchievementService.all` arrays were removed). Detection logic stays per-platform (it's DB queries, not centralizable). **DC3 — mode catalog via build-time codegen** (`packages/core/modes.json` → `scripts/gen-mode-catalog.mjs`, `pnpm gen:modes`): emits `apps/web/lib/modes.generated.ts` + `apps/ios/.../ModeCatalog.generated.swift` + `apps/android/.../ModeCatalog.generated.kt` so mode id/dbKey/title/shortTitle/shareLabel/desc/accentHex/glyph/romanNumeral/order/dailyEligible live in ONE file. Call sites refactored to read the catalog (icons stay native via small per-id chrome maps): web `page.tsx` MODE_CARDS+MODE_ID_TO_DB, `mode-picker` PROFILE_MODES, `invite-modal` MODES, `share-image` accent/glyph, `daily-share` DAILY_SHARE_MODES; iOS `ModeCatalog.homeModes` (init from `ModeGen`), `PostGameViews.ModeStyle` title/accent, `DailySweepShare` (ProperNoundle "Proper" sweep-label override kept — both natives shorten it, web uses full "ProperNoundle"); Android `ui/ModeCatalog` MODE_CARDS+modeTitle, `data/DailySweepShare` MODES. Diffed codegen output vs the prior literals = **zero drift** (titles/descs/accents/glyphs/shareLabels identical). All committed + pushed (web auto-deploys). **NOTE — the live working copy is `~/Developer/WordRush`, NOT `~/Desktop/Projects/WordRush`** (the latter is a stale build-9 checkout the tooling still reports as the project root). **iOS build 36 cut + swapped into review (2026-06-17):** bumped CURRENT_PROJECT_VERSION 35→36 (MARKETING_VERSION 1.0), archived UNSIGNED, exported with the ASC API key, **re-signed applesignin** (export strips it — frameworks GoogleMobileAds/UserMessagingPlatform + app re-signed with Apple Distribution cert E834629E… + `/tmp/ent36.plist` which adds the `com.apple.developer.applesignin` Default array to the existing app-identifier/team/beta-reports/get-task-allow entitlements; signature verified), repackaged + uploaded via altool (Delivery 45aed922) → polled VALID. **NEW — the ASC review swap was done ENTIRELY via the API (`~/.appstoreconnect/asc.rb`), no Chrome/2FA needed:** (1) cancel the in-flight reviewSubmission — `PATCH /v1/reviewSubmissions/{id} {attributes:{canceled:true}}` → CANCELING → version goes DEVELOPER_REJECTED; (2) attach the new build — `PATCH /v1/appStoreVersions/{VERSION_ID}/relationships/build {data:{type:builds,id:<build36>}}` (204) → version PREPARE_FOR_SUBMISSION; (3) `POST /v1/reviewSubmissions` (app rel) → `POST /v1/reviewSubmissionItems` (reviewSubmission + appStoreVersion rels) → `PATCH .../{subId} {attributes:{submitted:true}}`. Build 36 (v1.0) now **WAITING_FOR_REVIEW** (manual release), build 35 pulled. This API swap path is faster/more reliable than the Chrome flow the earlier notes describe — prefer it next time. Build 36 covers the profile sweep-stats card fix (the `.task`-on-empty-`Group` bug — added `else { Color.clear }` so the SweepStatsCard/calendar/heatmap/TopWords `.task` actually fires) + DC1 + DC2 + DC3. (15) **No-build audit pass (2026-06-17).** Three read-only audits (Pro hardening / runtime content / Supabase RLS+indexes) → shipped the web-deployable fixes, queued the rest. **Content (deployed, reaches all 3 platforms via /api):** reconciled three conflicting "What does Pro unlock?" answers in `lib/content/static-content.ts` to one accurate feature list + normalized PRO→Pro; fixed achievement copy in `lib/achievement-service.ts` ("Sequence Ace"→"Succession Ace"/"Win 50 Succession games" to match the UI mode name; "Propernoundle"→"ProperNoundle"). Confirmed the `all_modes` "9 game modes" copy is correct (detection checks exactly those 9 solo modes; VS excluded). **Backend perf (migration authored, NOT applied — `supabase/migrations/20260617000001_perf_indexes_sweep_and_mode.sql`, run manually in SQL editor):** adds `idx_daily_results_user_ptype_day (user_id, play_type, day DESC)` covering BOTH fetchDailySweepStats + fetchDailyPointsOverTime, two optional per-mode match indexes for fetchModeWinStreak, and drops the redundant `idx_user_stats_user_mode` (prefix of the user_mode_type index). **RLS:** clean — no breaches; is_pro/role trigger-protected (once 20260603000004 runs), XP/level/coins not self-settable, per-user tables owner-gated, public-read tables intentional for leaderboards. **Pro hardening (advanced same day):** **Apple webhook now LIVE** — committed the public Apple Root CA G3 to `apps/web/certs/` + added `next.config` `experimental.outputFileTracingIncludes` to force-bundle `./certs/**` into the `/api/appstore/notifications` function (it reads the cert via a runtime path Next can't trace), set Vercel prod env `APPSTORE_BUNDLE_ID`/`APPSTORE_APP_APPLE_ID` (=6775966055), redeployed; a dummy POST now returns `401 verification failed` (past the 503 gate = configured + working). **Play webhook pre-provisioned** — `PLAY_PUBSUB_TOKEN` generated + set in Vercel prod (route stays fail-closed until the SA vars exist); README updated with what's done vs the GCP/Play steps that still need the Google account. **Remaining to ENFORCE (USER-side):** register `https://wordocious.com/api/appstore/notifications` (Prod+Sandbox, V2) in ASC → App Information; sandbox-purchase Pro on TestFlight → confirm is_pro flips server-side; THEN run `20260603000004_lock_pro_columns.sql`. The lock trigger blocks client is_pro writes on BOTH platforms — safe now (no live purchasers either store) but the Play webhook (PLAY_SA_EMAIL/PLAY_SA_PRIVATE_KEY + Pub/Sub) must be wired before real Android purchases once Play billing is set up. (16) **iOS build 36 APPROVED (2026-06-17) → Pending Developer Release.** App Review approved build 36 (v1.0 — the profile sweep-card fix + DC1/DC2/DC3 centralization). Manual release per standing instruction, so it sits at **Pending Developer Release** until the user releases it. **Pro-hardening sandbox verification — attempted, inconclusive, lock DEFERRED:** registered the App Store Server Notification URLs via the ASC API (`PATCH /v1/apps/6775966055` → `subscriptionStatusUrl` + `subscriptionStatusUrlForSandbox` = the webhook, both V2 — no web-UI step needed). User did a sandbox **monthly** Pro purchase on TestFlight; got the Pro badge. Apple's "request a test notification" (App Store Server API, sandbox host `api.storekit-sandbox.itunes.apple.com`, `POST /inApps/v1/notifications/test`) returned `firstSendAttemptResult: SUCCESS` **3×** → proves Apple delivers signed notifications to our webhook and the server verifies + 2xx-acks them. BUT the user's `is_pro`/`pro_expires_at` row (AppDevTester, id 90e35152) never moved across ~40 min of polling — Apple **sandbox did not deliver the subscription/renewal RTDN** (a well-known sandbox quirk, separate from the on-demand test pipeline), and this Vercel plan won't surface runtime function logs to confirm receipt. So the live purchase→is_pro write was never directly observed. Decision (user-agreed): **do NOT run the lock migration yet** — no live purchasers exist, the hole is unchanged-risk, and the first REAL App Store purchase after release will fire a reliable production notification we can confirm. Temporary diagnostic `console.log('[appstore-webhook] …')` left in `apps/web/app/api/appstore/notifications/route.ts` to capture that first real event (remove after confirming, then run `20260603000004_lock_pro_columns.sql`). Everything controllable is verified (delivery, signature verify, appAccountToken wiring in StoreManager.swift:74, product-id match, service-role write path); only the sandbox round-trip observation is blocked by Apple's sandbox. (17) **Build 36 go-live readiness + post-release follow-ups (2026-06-17).** Pre-release verification (all green): the runtime endpoints build 36 newly depends on are healthy — `/api/content` 200 (faq 4 / helpModes 10 / about 6 / support 10), `/api/achievements` 200 (75), `/api/guides` 200; web Terms Stripe-fix live; Apple Paid-Apps agreement + bank (Thread Bank 0888, USD) + W-9 all **Active**; Pro webhook live+registered; ad wiring correct (real App ID/units, UMP, ATT, SKAdNetwork, app-ads.txt 200). **Known/accepted, NOT release blockers:** (a) ads serve *limited* until AdMob app review clears (~1–2 days post-release) — AdMob app shows "Requires review"/"Add store" because the listing isn't public yet + the AdMob **payment profile/account verification is still TODO** (AdMob → Payments: bank+tax, also gates the app review); (b) `is_pro` lock migration still deferred (purchases work; lock after first real-purchase confirm); (c) temp `[appstore-webhook]` console.log left in the web route (remove after that confirm); (d) build 36's BUNDLED native Terms still says "Stripe (web)" — web fixed, native rides the next build; (e) DC1/DC2 screens (Help/FAQ/About/Support/achievements) are network-fetched with persistent cache + NO bundled fallback → first-ever launch OFFLINE shows a brief loading state (chosen tradeoff). **ASC metadata to eyeball before Release (first public 1.0):** screenshots/What's-New, Age Rating reflects Advertising=Yes (the 2.3.6 fix), Marketing URL = wordocious.com (feeds the AdMob app-ads.txt crawl), Privacy Policy URL set, App Privacy "tracking" declared, export-compliance answered. **Immediately AFTER manual release:** (1) AdMob → finish payment profile + "Add store" → link the now-live App Store listing → app-ads.txt verifies + review clears; (2) on the FIRST real Pro purchase, confirm the webhook wrote is_pro (Vercel logs + DB), remove the temp logging, then run `20260603000004_lock_pro_columns.sql` to close the self-grant hole. (18) **support@wordocious.com email forwarding set up (2026-06-17).** Pre-launch check found wordocious.com had **NO MX records** → the Support-screen address (`static-content.ts:150`, served via /api/content to all 3 platforms) was a dead/bouncing address. Fixed with **ImprovMX free forwarding**: created an ImprovMX account, added wordocious.com, aliases `support@` + catch-all `*@` → **bterchin@gmail.com**; added the two MX records in GoDaddy DNS (`mx1.improvmx.com` pri 10, `mx2.improvmx.com` pri 20, host `@`). Verified propagated from Google (8.8.8.8), Cloudflare (1.1.1.1), AND the authoritative GoDaddy NS (ns72.domaincontrol.com) — all return mx1/mx2. So inbound mail to support@ now lands in the user's Gmail. (Free tier = receive/forward only; SPF TXT `v=spf1 include:spf.improvmx.com ~all` optional, only needed to *send* as support@ — would require ImprovMX Premium $9/mo + Gmail "send as".) DNS registrar = GoDaddy; nameservers ns71/72.domaincontrol.com. This was the last real pre-launch gap on the support side. (19) **VS multiplayer fix push (2026-06-17) — live-test findings across web + native + server.** Cross-tested wordocious.com VS (mobile Safari) and surfaced multiple bugs; fixed via direct edits + two parity-audit workflows. **WEB (deployed):** (a) VS board cut off behind the keyboard — all 7 per-mode VS components rooted on `h-full` while nested in vs-game's `flex-1 min-h-0` slot (didn't resolve on mobile Safari) → changed each root to `flex-1 min-h-0 flex flex-col`. (b) Opponent booted home on the other player's win — the socket connect `useEffect` listed `profile` (volatile; auth refreshes it on a win's XP write) so its cleanup `disconnect()`-ed mid-match → server emitted `opponent_left` → opponent navigated home AND the winner's stats never recorded (match_ended never delivered). Fixed: connect ONCE per mount, read profile/seed via refs, deps `[matchService, presenceId]`; also guarded `onOpponentLeft` with a `screenRef`. (c) Rushed intro — countdown was set concurrently with the intro splash; now starts from MatchIntro's `onDone`. (d) Leaderboard "Completed Today" showed Classic's board for every mode — `serverSession` (cross-device reconstruction) wasn't cleared on mode change → `setServerSession(null)` at the top of the effect. (e) WS1 cross-platform daily-VS linking: matchmaking seed was keyed on LOCAL date so different-timezone players never paired → added `getTodayUTC()` and use it for the daily-VS seed (solo dailies stay local). (f) WS2 Pro VS-card: dropped the Pro bypass so Pro plays the SAME shared daily VS as everyone (dailyVsActive no longer excludes Pro), card shows "✓/Played today", and the already-played screen offers a "Play Unlimited VS" CTA for Pro. **SERVER (Railway, all platforms):** mid-match disconnect now = forfeit — `endMatch(matchId, forfeitBy)` credits the remaining player a recorded WIN + result screen instead of bare `opponent_left` (was: booted home, no stats). **NATIVE — staged uncommitted for build 37 (both compile green):** iOS VS board-fit (`.frame(maxHeight:.infinity)` on the match VStack+GeometryReader) + `matchCompletionHandled` re-entry guard; iOS+Android UTC daily-VS seed (`todayUTC()`/`todayUTCDate()`); iOS InviteSheet rebuilt to match the web invite modal exactly (gradient "Invite a friend" title + X close, borderless modal, "Share link"/"Username" tabs, accent-colored picker, "Link expires in 24 hours", removed reset button, "USERNAME" label + "e.g. wordmaster", success copy). **Verified no native change needed:** completed-daily mode-leak — iOS already resets on `.task(id:mode)`, Android uses `remember(modeId)`. **DEFERRED (user todo):** Android VS board-fit needs an emulator check (the audit's `imePadding` suggestion is wrong — custom Compose keyboard, not system IME). Method note: the parity-audit workflows nailed the WEB diagnoses (verified, shipped) but several NATIVE suggestions were stale/wrong (Android invite already matched; Android imePadding wrong) — always compile/verify agent-proposed native edits, don't trust wholesale. (20) **VS forfeit recording + "FORFEIT" tag + iOS build 37 (2026-06-18).** Two linked VS fixes on top of push 19's server forfeit-credit: (a) **forfeited matches now populate Recent Matches** — the disconnect handler's `endMatch(matchId, forfeitBy)` credited the win but the single-writer `recordMatch` flag still pointed at the (gone) disconnector, so no history row was written; fixed so the REMAINING player is the designated writer (`remainingIsP1 = forfeitBy == null || forfeitBy !== player1.id`; `p1Records`/`p2Records` gate the two `match_ended` emits). (b) **visual "FORFEIT" cue** — `match_ended` now carries `forfeit: forfeitBy != null` (server `types.ts` + both emits); a new `matches.forfeit boolean NOT NULL DEFAULT false` column (`supabase/migrations/20260617000002_matches_forfeit.sql`, **applied manually in Supabase**) stores it; the writing client persists it (web `recordMatch` includes it ONLY when true so pre-migration inserts were unaffected — web select/chip deployed only AFTER the column existed; iOS/Android always send it post-migration); and Recent Matches renders an amber chip (bg #FEF3C7 / fg #B45309) beside the VS badge on forfeit-win rows on all 3 platforms (web `profile/page.tsx`, iOS `RecentMatchRow` in `PublicProfileView.swift`, Android `RecentMatchRow` in `ProfileScreen.kt`). Server deployed first (flag harmless without the column), then the migration, then web. **iOS build 37 cut (2026-06-18):** bumped CURRENT_PROJECT_VERSION 36→37, batches ALL of push 19's staged native VS fixes (board-fit + `matchCompletionHandled` guard, UTC daily-VS seed, rebuilt InviteSheet) PLUS this forfeit tag; archived/exported/uploaded via the ASC API key → TestFlight. **NOT swapped into review** — per the user, build 36 stays the approved/Pending-Developer-Release submission while they TestFlight-test build 37 on-device first; swap later via `asc.rb` if it passes. Android forfeit edits are compile-green (`:app:compileDebugKotlin`) and ride the eventual Play build. Still deferred: the Android VS board-fit emulator check. (21) **VS lobby UX overhaul — daily VS finished-screen + private-match code + Pro daily VS (iOS + Android, 2026-06-18, build 38).** TestFlight build-37 testing surfaced that tapping the home **VS Battle** card (daily) dropped Pro users into the mode-picker **lobby** instead of the daily VS queue/finished screen. Root cause: native routed the VS card to `VSLobbyView` for everyone, AND `dailyVsActive` was still gated `!isPro` (the web WS2 fix that dropped that guard never reached native), so Pro never saw the already-played screen. Audit-then-match against the web (the web `/practice/vs` is just Classic DUEL — daily or random; there is NO web mode-picker lobby; the native lobby is a native-only construct kept for Unlimited). Fixes on BOTH platforms (compile-green iOS `xcodebuild` + Android `:app:compileDebugKotlin`): (a) **daily VS card → the shared daily Classic VS directly** (queue, or the already-played finished screen), **unlimited VS card → the lobby** (iOS HomeView `cardLink`; Android MainScreen `onSelectMode` + HomeScreen unlimited flag for the no-engineMode VS card). (b) **`dailyVsActive = isDaily && mode == DUEL`** (dropped `!isPro`; Pro now plays the same once-a-day shared VS); already-played detection now also checks the server (`hasPlayedDailyVS`/`hasPlayedDailyVsToday`) so it's correct cross-device. (c) **`DailyVsAlreadyPlayed` gains `isPro` + `won`** — a **YOU WON/YOU LOST** pill + (Pro) a **"Play Unlimited VS"** button → the lobby (free still sees Upgrade to Pro). (d) **home VS card shows the daily-VS W/L badge + accent tint** (greyed when locked for freemium) via a new `DailyResultsService.dailyVSResult()`/`dailyVsResult()` (true=won/false=lost/nil) — VS has no solo daily_results row so it's special-cased. (e) **private match now surfaces the shareable code** — the queue screen renders the invite code + a Share button (`wordocious.com/vs/join/<code>`) whenever `inviteCode` is set (previously "Create a private match" jumped to a code-less waiting screen, so the host could never send it). (f) **iOS lobby scroll cutoff** — bumped the ScrollView bottom inset to clear the tab bar (the last create-match row was hidden; the Android lobby is fullscreen so no cutoff). **Recording confirmed (user Q):** per-mode VS W/L records to `user_stats` (play_type='vs') unconditionally for whatever mode the match is — so every VS mode populates its own Profile VS-toggle row; this is orthogonal to the routing changes and was made reliable by build 37's connect-once + forfeit-credit fixes (signed-in only; guests record nothing). **iOS build 38 cut (2026-06-18):** CURRENT_PROJECT_VERSION 37→38, archived unsigned → exported with the ASC API key → re-signed applesignin → uploaded via altool → TestFlight. **NOT swapped into review** (build 36 stays the approved/pending-release submission; build 38 is for on-device testing). Android edits ride the eventual Play build. (22) **VS native pairing FIXED — join_queue was dropped pre-connect (iOS + Android, 2026-06-18, build 39).** Two iOS build-38 users both tapping VS Battle sat in the waiting screen forever and never matched. Diagnosed with a temporary `/debug/queue` server endpoint (dumps connected sockets + queue buckets; added in index.ts/matchmaking.ts `snapshot()`): both users were CONNECTED (distinct user ids, 2 sockets each) but the server queue was EMPTY — neither client ever emitted `join_queue`. **Root cause:** the native VMs called `service.connect()` then **synchronously** `service.joinQueue()`; the Socket.IO **Swift and Java** clients (unlike the JS client, which buffers) DROP emits sent before the connection is established → the join event was lost every time. This is exactly why web↔web paired fine but native never did. **Fix:** emit `join_queue` from the socket's `onConnect` callback instead of immediately after `connect()` — iOS `VSMatchViewModel.connectAndQueue` sets `service.onConnect = { joinQueue(...) }` before connecting; Android same (`service.onConnect = { if (screen==QUEUE) joinQueue(...) }`). Guarded to the queue screen so a mid-match reconnect doesn't re-queue; the server already dedupes by player id (`addToQueue`). Covers ALL native VS entry (daily, random/unlimited, per-mode, private invite). Both compile green (iOS `xcodebuild`, Android `:app:compileDebugKotlin`). **Also in this build:** the guest **Sign Out** gate — the home-footer Sign Out only renders with a real session (a guest has nothing to sign out of; the header shows "Sign In"), fixed on iOS (`HomeView`, `if auth.isAuthenticated`), Android (`HomeScreen`, `isAuthenticated` collect), and web (`page.tsx`, `{user && …}`, deployed). **iOS build 39 cut (2026-06-18):** CURRENT_PROJECT_VERSION 38→39, archived unsigned → exported with the ASC API key → re-signed applesignin → uploaded via altool → TestFlight. NOT swapped into review (build 36 stays approved/pending-release). **The temp `/debug/queue` endpoint is still live** for verifying pairing on build 39 — REMOVE it once confirmed. (23) **VS match-screen UX: forfeit = recorded loss + confirm dialog, keyboard-fit (iOS + Android, 2026-06-18, build 40).** First successful native↔native pairing (build 39) surfaced three match-screen issues. (a) **Forfeit now counts as a loss + consumes the daily** — leaving an in-progress match only disconnected (server credited the opponent + wrote the shared match row, but the forfeiter's own `user_stats` VS loss and `daily_results` loss were never written, and the daily wasn't marked played → replayable). `VSMatchViewModel.forfeit()` now, when `screen == match|waiting` and `!resultRecorded`, records a self-loss via `GameResultsService.record(playType:"vs", won:false, …)` + (daily) `VSPlayLimit.markPlayedToday()` + `DailyResultsService.recordVs/recordDailyVsResult(won:false)`; bailing from the queue (no match yet) still records nothing. (b) **Forfeit confirmation dialog** — the in-match Home button now prompts ("Forfeit match? …counts as a loss" + daily note) before leaving, instead of silently forfeiting (iOS `.confirmationDialog`, Android `AlertDialog`; other Home buttons — result/queue/opponent-left — leave directly). (c) **Keyboard fit** — the VS board's greedy area was clipping the keyboard's bottom row; gave `KeyboardView` `.layoutPriority(1)` (and the board `.layoutPriority(0)`, dropped the redundant `.frame(maxHeight:.infinity)`) so the VStack reserves the keyboard's full height first and the board yields. **Both compile green.** NOTE: the keyboard-fit is a layout fix made without a simulator (the VS match screen needs two live players to reach) — pending on-device confirmation on build 40. **DEFERRED:** the match **intro** still reads as clunky/unfinished per the user — needs a closer look (what specifically; likely the splash/countdown timing or layout). **iOS build 40 cut (2026-06-18):** CURRENT_PROJECT_VERSION 39→40, archived unsigned → exported (ASC API key) → re-signed applesignin → uploaded → TestFlight; NOT swapped into review. (24) **VS match-screen polish + ProperNoundle fixes (iOS, 2026-06-20, build 41).** **VS:** (a) the bottom **tab bar showed inside the VS match** (game pushed in the Home tab's nav stack), overlapping + clipping the keyboard's bottom row → `.toolbar(.hidden, for: .tabBar)` on VSGameView makes it fullscreen like solo (THIS was the real keyboard-cutoff cause, not the earlier layoutPriority tweak; Android VS already fullscreen via MainScreen's early return); (b) **avatar borders sat proud of the image** → `Circle().stroke` → `Circle().strokeBorder` on intro cards + in-match header avatars; (c) the **countdown popped in color after the dark intro** (it ticked behind the 2.5s splash) → gated `countdown != nil && !showIntro`. **ProperNoundle:** (d) victory screen showed **"No definition available"** — PN's `VictoryOverlay` passed the proper-noun name as `solution` so DefinitionCard rendered → added `showDefinition` (default true), PN passes false; (e) **expanding a completed PN daily card crashed** — the generic board reconstruction can't rebuild PN's multi-word proper-noun board → skip the grid for PN in CompletedDailyCard (answer + stats + score still show); (f) **finished daily needed a tab round-trip to populate** → `reloadDaily()` on Home `.onAppear` + game `fullScreenCover` `onDismiss`. iOS compile green; **Android parity for (d)/(e)/(f) TODO. iOS build 41 cut** (40→41, same unsigned-archive → API-key-export → re-sign applesignin → altool flow; NOT swapped into review). **Separately (no build): VS recording silently failing since build 37** — the native `matches` insert always sends the `forfeit` column (added June 17); if PostgREST's schema cache never picked it up, every VS insert is rejected (errors swallowed by `try?`) → only a pre-June-17 row exists, H2H shows stale "BMT leads 1-0". Fix = `NOTIFY pgrst, 'reload schema'` (pending user confirm); will also harden the insert to send `forfeit` only when true (web already does). (26) **Celebration-modal icons + sweep-card centering + Android parity (2026-06-20, iOS build 43).** The Daily Sweep / Flawless celebration modal now shows real game icons (ModeIconView / ModeGlyph, mapped by dbKey) instead of letter glyphs on both platforms; the profile DAILY SWEEPS card lays out as equal centered thirds (iOS .frame(maxWidth:.infinity), Android weighted Box) instead of edge-pinned. **Android parity pass:** celebration icons + sweep centering + VS countdown gated to !showIntro (the avatar-border, home-refresh, PN-definition, and PN-completed-crash iOS fixes were already N/A on Android — Compose Modifier.border insets, Home re-composes on game return, PN already excluded from DefinitionCard, and the completed card is keyed by mode + reducer-validated). iOS build 43 cut (42→43). (27) **AdSense "low value content" remediation — web content push (2026-06-26).** AdSense re-denied wordocious.com for thin/low-value content. Root issue was NOT missing infra (SSR content pages, sitemap, robots, crawler-facing landing all existed) but: (a) the sitemap listed 10 login-gated GAME routes (/six, /quordle, /daily, …) that serve the SAME Landing to logged-out crawlers = ~10 duplicate pages; (b) limited net unique editorial pages. Fixes deployed: dropped the duplicate game routes from app/sitemap.ts (now only crawlable unique pages); made /pro public (auth-gate PUBLIC_PATHS) so its feature copy is indexable. Then a content push: **Word of the Day archive** (lib/word-of-day.ts shared word-for-date + dictionary lookup, same algo as the home card; /word/[date] server pages with original per-word letter analysis so it's not just a scraped definition, future dates 404; /words index; sitemap += last 60 days) and a **/strategy section** (lib/strategy-content.ts — 3 original long-form articles: best-starting-words, solve-faster, modes-explained; /strategy + /strategy/[slug] SSR). Home footer now links Guides/Strategy/Words/FAQ for internal crawl paths; the WOTD card links to the archive. The 9 mode guides were already substantial (real scoring + 5 strategy tips each) so NOT re-deepened. web build green (66 pages). Pending: user requests an AdSense re-review. (28) **ProperNoundle "Completed Today" card now shows the real board on all 3 platforms (2026-06-26).** Web: the PN completed card was localStorage-only (no cross-device fallback like other modes) → vanished when PN was played on native/another browser. Added matches-row reconstruction (player1_guesses + today's answer via evaluateGuess) so it renders everywhere; local save still preferred for hint detail. Native: iOS previously SKIPPED the PN board (crash-dodge), Android rendered a wrong generic 5-letter reconstruction — both now build a dedicated PN mini-board from the recorded guesses, tiles re-derived against today's answer via the PN engine (evaluate/wordGroups/dailyPuzzle), laid out in the answer's multi-word groups, matching the web. iOS CompletedProperNoundleMiniBoard + render branch; Android ProperNoundleCompletedDailyCard + ProperNoundleMiniBoard fed by fetchRecordedDailyMatch. Web deployed; native committed + compiles, BATCHED for the next iOS/Android build (not yet cut). (29) **Sign-out data-leak fixed on all 3 platforms (2026-06-26).** Signing out then bypassing sign-in (guest) showed the previous account's completed dailies. Root cause: per-account local state survived sign-out — the daily-completions cache (web sessionStorage `wordocious-daily-completions` / Android `wordocious_prefs:daily-completions-cache`) and the local daily game saves (web `wordocious-session-*-daily` + `wordocious-propernoundle-daily`; iOS GamePersistence files + `pn-save-*`; Android `wordocious_games` prefs), plus the web completions provider deliberately KEPT its in-memory map on null-user (anti-flicker). Fix: web signOut purges the cache + local daily saves, and the provider now clears its map + cache once auth has RESOLVED to no-user (guarded by !loading). Native signOut now calls GamePersistence clear-all + (Android) DailyCompletionsService.clearCache(). Web deployed; native committed + both compile, BATCHED for the next build. (30) **iOS BUILD 44 cut + uploaded to App Store Connect (2026-06-26).** CURRENT_PROJECT_VERSION 43→44, xcodegen regenerate, unsigned archive (CODE_SIGNING_ALLOWED=NO) → exportArchive via ASC API key (-allowProvisioningUpdates, app-store-connect, auto signing; no Wordocious profile is installed — only ShowLoud's — so the API key fetches one on the fly) → export stripped applesignin (as always) → re-signed Payload/Wordocious.app with Apple Distribution cert E834629E… + ent.plist (application-identifier Q32F6GRDYG.com.wordocious.app + applesignin Default + beta-reports-active + get-task-allow false) → repackaged IPA → altool validate (VERIFY SUCCEEDED) → altool --upload-app (UPLOAD SUCCEEDED, Delivery UUID d19afea5-391f-4e6f-938f-5d903eb12f83). Build 44 contents (new since build 43): ProperNoundle 'Completed Today' card now renders the REAL board (reconstructed from recorded guesses, tiles re-derived, multi-word layout) [push 27]; sign-out now purges per-account local game state (GamePersistence.clearAllSaves incl. pn-save-*) so a guest never inherits the prior user's dailies [push 29]. NOT swapped into App Review (user tests on TestFlight first; manual release always). Build 36 remains the approved/pending-release one; user swaps via asc.rb when ready. (31) **Native site-nav: header "?" dropdown + full home footer (web parity) — iOS + Android (2026-06-26).** User wanted the new web footer menu mirrored natively AND the dated single-"?"→Help replaced by a dropdown. Decisions: BUILD native Strategy + Words screens (fed from the web like guides) and show BOTH a footer + the dropdown. Web: added /api/strategy + /api/words; moved wordPlayAnalysis() into lib/word-of-day.ts (shared by the /word/[date] page + the API so page/API/native render identical prose). iOS (InfoMenu.swift): InfoMenuDestination enum (8 items: How to Play/Guides/Strategy/Words/About/FAQ/Privacy/Terms), header "?"→Menu, StrategyService+StrategyView, WordsService+WordsView (Word-of-the-Day archive), GuidesIndexView (lists 9 guides→GuideSheet), HelpView gains initialTab; home footerLinks now full 8 via InfoFooterLinks. Android (InfoNavScreens.kt): StrategyService/WordsService (HttpURLConnection, memory cache), StrategyScreen/WordsScreen/GuidesIndexScreen, AppHeader "?"→DropdownMenu(onNav), MainScreen infoRoute += faq/guides/strategy/words, HelpScreen initialTab, HomeScreen FooterLinks→InfoFooter (8 items). Web deployed; both native targets compile, BATCHED for the next build. (32) **iOS BUILD 45 cut + uploaded (2026-06-26).** CURRENT_PROJECT_VERSION 44→45; same recipe (unsigned archive → exportArchive via ASC API key → re-sign applesignin with cert E834629E… + ent.plist → repackage → altool upload). UPLOAD SUCCEEDED, Delivery UUID eeb6d574-b69d-4208-9dd0-6852060c2f3d. NEW since build 44: the native site-nav — header "?" is now a dropdown of all 8 web-footer items + the home footer lists the same 8 (was 4), with new native Strategy + Words (Word-of-the-Day archive) + Guides-index screens fed from /api/strategy + /api/words [push 31]. NOT swapped into App Review (user TestFlight-tests first; manual release always). Build 36 still the approved/pending-release one. (33) **Menu aesthetic overhaul + Word-of-the-Day tappable, all platforms (2026-06-26).** iOS: header "?"→styled MenuSheet (accent bar + colour-accented icon tiles + title/subtitle) replacing system dropdown; Strategy/Words/Guides use shared MenuScaffold; word detail gradient hero + icon-headed accent cards; WordOfTheDayView tappable→WordsView ('Past words ›'). Android: "?"→InfoMenuSheet (ModalBottomSheet icon-tile rows); word detail gradient hero + accent cards; Strategy lightbulb tile; WordOfTheDayCard tappable→Words. Web /word/[date]: gradient hero + BookOpen/Lightbulb accent cards. Web built green; native compiles, BATCHED. Preview tool hits the stale Desktop checkout not ~/Developer — verified via build+tsc. (34) **iOS BUILD 46 cut + uploaded (2026-06-26).** CURRENT_PROJECT_VERSION 45→46; same recipe (unsigned archive → exportArchive via ASC API key → re-sign applesignin cert E834629E… + ent.plist → repackage → altool). UPLOAD SUCCEEDED, Delivery UUID 1fb444f3-b82e-48d1-8062-2905ec8667a6. NEW since build 45: the menu aesthetic overhaul (styled MenuSheet replacing the system dropdown; MenuScaffold chrome on Strategy/Words/Guides; gradient-hero word detail) + Word of the Day card now tappable→Words archive [push 33]. NOT swapped into App Review (TestFlight first; manual release). Build 36 still the approved/pending-release one. (35) **iOS BUILD 47 + native How to Play + instant completed-state refresh (2026-06-26).** Web: gradient titles on /strategy + /words (were plain black); single-sourced the How to Play doc into lib/how-to-play-content.ts + /api/howtoplay so native matches. iOS: new HowToPlayView (renders the web /how-to-play doc — basics+tile colours, 10-mode guide, scoring, XP, streaks, tips — from /api/howtoplay) wired to the menu 'How to Play'; and completed-state surfaces now refresh the instant a daily is recorded via a shared .onDailyCompletion modifier (listens DailyCompletionsStore.completionPosted) + reloadToken on CompletedDailyCard/LeaderboardTab/ProfileTab/DailyRecordsView, plus HomeView refresh — no more tab round-trip to see a finished puzzle. Build 47: bump 46→47, recipe as usual, UPLOAD SUCCEEDED Delivery UUID 000acfce-34af-4490-9d31-ec289e0aa94c, NOT in review. PENDING Android parity: How to Play screen + the reactive completed-state reload. (36) **Android parity: instant completed-state refresh + native How to Play (2026-06-26).** Reactive reload: added DailyCompletionsService.completionTick (MutableStateFlow bumped in noteCompletion — the Android analogue of iOS completionPosted); HomeScreen/LeaderboardScreen/ProfileScreen/RecordsScreen/CompletedDailyBoard now key their produceState/LaunchedEffect (and CompletedDailyBoard's local-save read) on the tick, so a finished daily shows immediately with no tab round-trip. How to Play: new HowToPlayService + HowToPlayScreen (renders the web /how-to-play doc from /api/howtoplay, OverlayScaffold chrome) wired to the menu 'help' route (was HelpScreen). Compiles; batched for the next Android build. iOS equivalents shipped in build 47. (37) **iOS BUILD 48 — About/Privacy/Terms restyled to match How to Play + 'Past words' (2026-06-26).** iOS: InfoPage now renders in MenuScaffold chrome (accent bar + wordmark-gradient title + Close), system nav bar hidden (.toolbar(.hidden)) so it matches whether presented from the '?' menu OR pushed from Settings; dropped the InfoSheet NavigationStack/Done wrapper. WordsView gained a navTitle param — the Word-of-the-Day card opens it as 'Past words' (was 'Words'). Android parity (batched): InfoScreen gained the 6pt accent bar; WordsScreen navTitle param + a 'pastwords' route the WOTD card uses. Build 48: bump 47→48, recipe as usual, UPLOAD SUCCEEDED Delivery UUID a28d3150-391b-4b46-8186-d1a15a22b00c, NOT in review.*

A complete walkthrough of what Wordocious is, how it's built, where it stands, and what's pending — written for someone joining on the business side.

---

## 1. The product in one sentence

Wordocious is a daily word-puzzle game (Wordle's lineage, expanded) with ~9 distinct modes — solo dailies, multi-board variants, a 5-stage Gauntlet, and real-time head-to-head VS matches — live on the web, **iOS build 34 (v1.0) submitted to App Review** (manual release; build 32 was REJECTED on 2.3.6 + 5.1.1(v) — see §7 — fixed via guest mode; build 34 adds the guest "Sign In" discoverability polish), and feature-complete as a native Android app awaiting Play Store setup (target API 35; guest mode added; signed AAB + Play developer account pending).

## 2. Why it exists

Wordle proved a single daily 5-letter puzzle could become a habit. Wordocious takes that loop and multiplies it: multiple board counts (1/2/4/8), letter lengths (5/6/7), a roguelike Gauntlet that chains modes into one run, and synchronous multiplayer where two players race the same seed. Daily seeds mean everyone plays the same puzzle each day — the shared-water-cooler effect that made Wordle viral — while the mode variety and VS ladder give committed players somewhere to go after the daily.

Monetization is two-pronged: a **Pro** subscription (unlimited replays, streak shields, **ad-free**, etc.) and **ads** (AdMob on iOS and Android / AdSense on web) shown to free users. Neither gates the core daily — it stays free to protect the habit loop. See §5.

---

## 3. Technical stack

| Layer | Stack |
|---|---|
| Web app | Next.js 13 (App Router), React 18, TypeScript, Tailwind + shadcn/ui. Full SSR (API routes + middleware) — **not** a static export. |
| Native iOS | 100% native SwiftUI (no Capacitor/WebView). Game engine is a Swift port of `packages/core`; data via `supabase-swift`; realtime VS live on `socket.io-client-swift`; IAP live on StoreKit 2. |
| Native Android | 100% native Kotlin + Jetpack Compose (`apps/android`, `:app` + `:core` Gradle modules; minSdk 26 / target 34). Game engine is a Kotlin port of `packages/core` in `:core`; data via supabase-kt; realtime VS on `io.socket:socket.io-client`; IAP via Google Play Billing (billing-ktx 7.1.1); ads via AdMob + UMP; daily reminder notifications via WorkManager (18:00 local). |
| Realtime server | Standalone `socket.io` server (`apps/server`) on **Railway** behind `server.wordocious.com` (deploys from `main`) — matchmaking (bucketed by mode + dailySeed\|random) + live VS match state. |
| Shared engine | `packages/core` — pure TypeScript game logic (evaluator, reducer, seed, scoring, dictionary, prefill). Source of truth for game rules across web + the Swift (iOS) and Kotlin (Android) ports. |
| Hosting | Vercel (web, wordocious.com — the live Vercel project is `spellstrike`) + Railway (socket server). DNS at GoDaddy. Both deploy from `main`. |
| Auth / DB / Storage | Supabase (Postgres + Auth + Storage). Project `eniiqqsxpmuyrspvepiw`. RLS enabled + correct on all 13 public tables. Email + Google + Apple sign-in all functional on iOS (confirmed on device). Android Google sign-in is built (Credential Manager) but blocked on creating an **Android OAuth client** in Google Cloud (package `com.wordocious.app` + SHA-1 `7A:2C:E8:AC:82:B7:F7:F6:10:64:E1:9D:CC:88:23:0A:8E:63:79:B0`) before tokens vend. |
| Payments (web) | `lib/payment/` abstraction (PaymentProvider interface + factory). Currently `DemoProvider` — **no real charges collected on web yet.** Stripe is the planned web provider. |
| Payments (iOS) | **Live: StoreKit 2 direct** (`com.wordocious.app.pro_{monthly,yearly,day}`); purchases tag `appAccountToken` = the Supabase user id. An App Store Server Notifications V2 webhook (`apps/web/app/api/appstore/notifications/route.ts`) is built fail-closed but **not yet enforced** (see §11). |
| Payments (Android) | Google Play Billing (billing-ktx 7.1.1): `pro_monthly`/`pro_yearly` subs (+4 shields) + stacking `pro_day` consumable, `obfuscatedAccountId` = user id, restore supported. The 3 products still need to be **created in Play Console**. A Play RTDN (Real-Time Developer Notifications) webhook (`apps/web/app/api/playstore/notifications/route.ts`) is built fail-closed (mirrors the Apple one) but **not yet configured** (see §11). |
| Cron | Vercel cron: `/api/cron/daily-medals` (12:05 UTC — moved from 05:05 so US-evening local-day rows are finalized before podium grading), `/api/cron/daily-reminder` (14:00 UTC). Auth via `CRON_SECRET`. |
| iOS project gen | XcodeGen (`project.yml` → `Wordocious.xcodeproj`, gitignored & regenerated). |
| Android build | Gradle: `./gradlew bundleRelease` produces a **signed release AAB (~18.5MB)** via the gitignored `apps/android/key.properties` → upload keystore at `~/.android-keys/wordocious-upload.jks` (**must be backed up**). |

---

## 4. Architecture: three clients, one backend

The web app and the native iOS and Android apps are **three separate clients that share one backend** (Supabase + the socket server). Neither native app is a WebView shell — each renders fully native UI and runs its own port of the game engine. Nothing about adding a native client changes the web app.

| Concern | Web | iOS (native) | Android (native) |
|---|---|---|---|
| UI | React / Tailwind | SwiftUI | Jetpack Compose |
| Game logic | `packages/core` (TS) | Swift port of `packages/core` (parity-tested) | Kotlin port of `packages/core` in `:core` (parity-tested) |
| Auth | `supabase-js` + RLS | `supabase-swift` + RLS (same `auth.uid()`) | supabase-kt + RLS (same `auth.uid()`) |
| Data | Direct Supabase reads + Next API routes | Direct Supabase reads | Direct Supabase reads |
| Realtime VS | `socket.io-client` | `socket.io-client-swift` (live) | `io.socket:socket.io-client` (live) |
| Pro entitlement | `is_pro` + `pro_expires_at`, read via `isProActive()` | same columns, `isProActive()` ported 1:1 | same columns, ported 1:1 |
| Ads | AdSense | AdMob (real IDs, approved) | AdMob (Google **test IDs** until an Android AdMob app exists) |
| IAP | `DemoProvider` (Stripe planned) | StoreKit 2 (live) | Google Play Billing (built; products pending in Play Console) |

**Engine parity is the load-bearing constraint.** Because daily seeds and VS matches must resolve to the *same words* on every client, the Swift and Kotlin engines are 1:1 ports validated against JSON fixtures generated from the live TS engine (see §8). Any two players on the same daily seed — web, iOS, or Android — get the same puzzle, byte-for-byte.

**Why fully native (not a Capacitor shell):** the sibling app ShowLoud was repeatedly rejected by Apple under Guideline 4.2 ("minimum functionality") while it was a WebView wrapper; rejections stopped once it went fully native. Wordocious — an even more web-shaped SSR app — would hit the same wall, so it's native from day one. (Validated: build 10 passed App Review.)

### Game modes

| Mode | Boards | Guesses | Notes |
|---|---|---|---|
| DUEL (Classic) | 1 | 6 | 5-letter daily |
| DUEL_6 / DUEL_7 (Six / Seven) | 1 | 7 / 8 | 6- and 7-letter, separate dictionaries |
| MULTI_DUEL | 2 | 6 | |
| QUORDLE (QuadWord) | 4 | 9 | |
| OCTORDLE (OctoWord) | 8 | 13 | |
| SEQUENCE | 4 | 10 | boards solved in order |
| RESCUE (Deliverance) | 4 | 6 | starts with 3 prefilled guesses |
| TOURNAMENT | 5 | 6 | |
| PROPERNOUNDLE | 1 | — | proper-noun puzzles, separate data |
| GAUNTLET | 1→4→4→4→8 | per-stage | 5-stage run chaining Duel→Quordle→Sequence→Rescue→Octordle |

---

## 5. Monetization

Two channels: **(A) Pro subscription** (removes ads + unlocks extras) and **(B) ads** shown to free users. The core daily stays free either way.

### A. Pro subscription

**Three plans** (defined in `lib/payment/types.ts` `PRO_PLANS`):
- `pro_day` — $1.00 / 24 hours (day pass; stacks on existing expiry, no streak-shield grant)
- `pro_monthly` — $6.99 / month
- `pro_yearly` — $59.99 / year (~$4.99/mo)

**Unified entitlement.** All Pro state lives in two `profiles` columns: `is_pro` (write-side marker) and `pro_expires_at`. Every Pro gate reads through `isProActive(profile)` — never the raw boolean — because there's no expiry sweep cron yet, so the boolean alone would keep expired users Pro. Monthly/yearly grant 4 streak shields; the day pass intentionally does not.

**The swap point.** `lib/payment/` is a clean abstraction: a `PaymentProvider` interface, a factory in `index.ts` ("the single swap point for monetization"), and fulfillment functions (`fulfillSubscription` / `cancelSubscription`) that write the entitlement. Today the factory returns `DemoProvider` — instant fulfillment, **no real money collected on web yet** (Stripe not wired).

**Billing status by platform:**
- **iOS — LIVE: StoreKit 2 direct.** Product IDs `com.wordocious.app.pro_{monthly,yearly,day}` (day pass = consumable that stacks 24h on the existing expiry). A verified transaction writes the same `profiles` entitlement columns the web uses; purchases tag `.appAccountToken(user id)`. **Hardening built but not enforced:** an App Store Server Notifications V2 webhook (`apps/web/app/api/appstore/notifications/route.ts`, verifies Apple's signed payload, maps `appAccountToken`→user, writes via service-role) ships fail-closed (503) until configured; the deferred migration that blocks client-side `is_pro` writes (`20260603000004_lock_pro_columns.sql`) runs only after a sandbox test. Until then fulfillment is client-side (RLS own-row writes, same trust model as the web). A Restore Purchases button ships (Apple-mandatory). Apple takes 15–30% of iOS subscription revenue.
- **Android — built, pending store setup: Google Play Billing** (billing-ktx 7.1.1). `StoreManager` mirrors iOS: `pro_monthly`/`pro_yearly` subs (+4 shields), stacking `pro_day` consumable, `obfuscatedAccountId` = user id, restore. Pro-page CTAs show real prices with fallbacks. **User TODO: create the 3 products in Play Console** + an internal-testing track + license testers.
- **Web — Stripe (planned;** the `DemoProvider` placeholder is in the same factory).

### B. Ads (free users only)

Free users see ads; **Pro removes them entirely** (every ad surface is gated on `isProActive`). The serving format on native is a **game-start rewarded interstitial** (banner units exist on iOS but are unused — interstitial-only is full parity across native); the web has an interstitial-on-game-start + bottom banner.

- **iOS — AdMob** (`GoogleMobileAds` SDK, `AdsManager`/`AdBannerView`). **Account + app approved by Google.** Real units under publisher **`pub-3015627373086578`**: App ID `…~8393761846`, banner `…/4287985559`, rewarded-interstitial `…/6909445311`. Launch flow on first play: **Google UMP consent** (GDPR, EEA/UK) → **ATT** prompt (hardened `requestATTOnce`, see §11) → SDK init → ads serve (gated on `canRequestAds`). The **GDPR (UMP) consent message is PUBLISHED in the AdMob console** (2026-06-10; privacy policy URL `https://wordocious.com/privacy` set). `app-ads.txt` (`google.com, pub-3015627373086578, DIRECT, f08c47fec0942fa0`) is live at wordocious.com. **Remaining:** payment profile (banking), then link the App Store listing + verify app-ads.txt once the app is publicly released.
- **Android — AdMob wired but on Google TEST IDs.** UMP consent → MobileAds init → game-start rewarded interstitial for free users (ad time excluded from the game timer). **User TODO: create a Wordocious ANDROID app in the AdMob console and swap the real IDs** into `AndroidManifest` (APPLICATION_ID) + `AdsManager.kt`.
- **Web — AdSense** (`pub-3015627373086578`, same publisher). Interstitial-on-game-start (`AdGate`) + bottom `AdBanner`; `ads.txt` present. Activation is config (`NEXT_PUBLIC_ADS_ENABLED` + slot IDs) + site approval. **DENIED 2026-06-13 for "Low value content"** — cause is structural (gameplay is login-gated, so the crawler saw only thin public pages). Remediated same day: 9 substantive public mode guides at `/guides/*` (rules, real scoring formulas, strategy) + landing/sitemap wiring. **User TODO: request AdSense re-review** after the ~48h crawl window. (AdMob app ads are a separate program and unaffected.)
- AdMob takes a revenue share (publisher keeps ~68% on content ads).

---

## 6. Core user flows

### 6a. Play the daily (solo)
1. Open the app → pick a mode from the home grid.
2. Today's puzzle loads from the **daily seed** (`daily-YYYY-MM-DD-<MODE>`, keyed to the device-local date) — identical on web, iOS, and Android.
3. Type guesses on the on-screen keyboard; tiles reveal the **Royal palette** — purple (correct) / amber (present) / slate (absent); the keyboard keys color to the best-known state per letter. (Colorblind mode swaps to orange/blue; the palette is purely client-side, no server/DB color storage.)
4. Win/loss is computed client-side by the engine; result persists locally (resume mid-game after backgrounding) and to Supabase for stats/leaderboards.

### 6b. Gauntlet
A single run through 5 escalating stages (Duel → Quordle → Sequence → Rescue → Octordle). Any board bust ends the run; clearing a stage advances to the next. Results screen shows a per-stage breakdown with a "review this stage" snapshot.

### 6c. VS (head-to-head)
Two players are matched via the socket server — **matchmaking is bucketed by (mode, dailySeed|random)** so clients only pair on the same puzzle — share a seed, and race. Live on all three clients. The 2026-06-10 overhaul added: a match-intro splash with all-time head-to-head record, a persistent tug-of-war progress header, moment callouts + typing indicator, a spectator waiting screen for the finished player, and a side-by-side final-board reveal (letters only after match end). Scoring = win bonus + guess diff + time diff + DNF penalty via `calculateScore`; rematch flow included. Daily-VS uses a shared daily seed; private matches use an invite code. **ALL completed VS matches now count on the daily VS Records leaderboard**, and the personal profile has a Solo/VS toggle. *(Pending: a live cross-platform pairing test of the bucketing fix.)*

### 6d. Go Pro
On `/pro` (web) the plans + copy are shown; the Subscribe button is what branches by platform. Web currently runs the demo provider (no charge); **iOS runs live StoreKit 2**; **Android runs Google Play Billing** (live once the products exist in Play Console). Entitlement lights up everywhere via `isProActive()`.

---

## 7. Where things stand (June 13, 2026)

**iOS — approved, awaiting manual release.** Version 1.0 **build 10 is APPROVED by App Review** and sits at **Pending Developer Release** (manual release, per standing instruction). **Build 22 is the latest on TestFlight** — it carries the full Royal palette re-theme plus all three audit-fix waves (see below). Build lineage since approval: 11 = VS experience overhaul + Play Again + Unlimited-PN fix; 12 = freemium VS-scoring floor removed + opponent names; 13 = instant home-card completion; 14 = guess-distribution mode-max buckets + victory-card wrap; 15 = completed-daily review fixes (engine replay, cross-device daily replay); 16 = multi-board guess count = MAX across boards; 17 = tab-bar flash + backspace icon; 18 = Royal palette re-theme; 19 = Succession guess-count truncation + Deliverance keyboard prefill + Gauntlet run-cumulative scoring; 20 = audit waves 1–3 (PN daily-seed leak, POSIX date formatters, sequence guess-list shape, day-from-seed recording, PN clue consumes a row + can lose, Six/Seven zero-candidate hint counting). The user is TestFlight-testing build 20; the blessed build gets swapped onto the approved version before release.

**Android — feature-complete, zero known gaps vs web/iOS.** Full inventory: all 9 game modes + VS multiplayer (socket.io), Supabase auth (email + native Google sign-in via Credential Manager — needs the Android OAuth client created before tokens vend), Google Play Billing, AdMob (UMP consent + game-start rewarded interstitial — currently Google test IDs), synthesized sound engine, daily reminder notifications (WorkManager 18:00, DST-proof self-rescheduling chain), account deletion, public profiles, share-image pipeline (FileProvider PNG), achievements unlock detection, medals + sweep/flawless XP, deep links (`https://wordocious.com`, assetlinks.json published), real adaptive launcher icon, release signing, signed release AAB builds (~18.5MB), Royal palette, and the audit-wave fixes (Succession unwinnable-past-board-1 fixed, active-play timer accumulator, day-from-seed, VS match rows carry solutions + guesses). **Remaining before Play submission (all user-side):** Play Console app + products + internal track + license testers, an Android AdMob app + real ad IDs, the Android OAuth client, and the store listing/privacy form.

**Web + server — stable, deploying from `main`.** Web on Vercel (project `spellstrike`, wordocious.com); the socket.io matchmaking server on Railway behind `server.wordocious.com`. Royal palette live. Recent web work: free unlimited-play hole closed (all 9 mode routes wrapped in `UnlimitedGate` — daily + Pro pass through, free non-daily URLs get a Go Pro upsell); pending-record retry queue (results lost to tab-close re-sent on next visit with a server dedupe check); midnight day-from-seed recording; the AdSense remediation guides at `/guides`; a fail-closed Play RTDN webhook at `/api/playstore`.

**Six-audit bug sweep (2026-06-12, all confirmed findings fixed across 3 waves).** Six parallel read-only audits (recording matrix, double/zero-record, date/timezone, colorblind/dark regression, Pro/freemium gating, XP/medals/achievements) verified findings in both codebases. Headline fixes: the **phantom-win root cause** (web restored-daily effect re-ran the full stats pipeline per browser session) is fixed; the medals unique-key migration `20260612000001` (adds `medal_type`) was **applied by the user**; the daily-medal cron is now idempotent. The Royal re-theme passed with **zero regressions** and XP/streak/scoring formulas were certified identical across platforms.

**AdMob (iOS):** account + app approved; GDPR (UMP) consent message published (2026-06-10). Remaining: payment profile, then link the App Store listing + verify app-ads.txt after release. **AdSense (web):** DENIED 2026-06-13 (low value content) → guides remediation shipped → re-review pending (see §5.B).

### Build & change log (newest first)

*2026-06-15 part 2: iOS build 22 VALID on TestFlight (Delivery f68a16bd). Android Daily Reminders toggle added to the profile Account section — the notification BACKEND already existed (NotificationService.kt WorkManager chain + a working Settings toggle); the profile-audit agent had wrongly reported "no backend" and omitted it. Now wired into the profile via the existing service (web/iOS parity).*

*2026-06-15 (FULL PROFILE PARITY AUDIT + swipe-back → iOS build 22; web deployed earlier): user reported build 21 STILL showed no Pro Stats + missing stats vs web, and wanted a full profile parity pass (web = truth) on both natives. Two parallel audit agents (iOS, Android) reconciled every section/stat/chart/menu against apps/web/app/profile + components/profile. **REAL Pro Stats root cause (the bug my null-decode fix didn't catch):** iOS ProStatsCard self-fetched via .task(id: auth.isProActive) — a Bool id — so when auth.profile hydrated a frame late the guard early-returned and the task NEVER re-fired → bars empty forever → card permanently hidden for a Pro user. Fixed by fetching statRows once in ProfileTab (.task id = profile.id) and passing down; bars is now a synchronous computed property. **iOS profile gaps fixed:** reordered All view to web order (Activity → Last 7 Days → Guess Dist → Solve Time → Top Words → Insights → Daily Medals → Pro Stats; medals were rendering at the very top); ADDED the missing Last 7 Days chart + Insights card; ADDED an Account section (Daily Reminders toggle, Sign Out, Delete Account) with bottom padding so Sign Out clears the BottomNav/safe area (the unreachable-sign-out bug). **Android profile gaps fixed:** Delete Account added to the profile (was Settings-only); Sign Out restyled + navigationBarsPadding so it clears the gesture nav; Last 7 Days now All-view-only + global; When You Play now per-mode-only; activity() counts player1 OR player2, gapless (web parity). Pro Stats solo-filter parity retained on both. **Swipe-back-to-home:** new iOS SwipeToGoBack.swift modifier — left-edge DragGesture via simultaneousGesture (won't block keyboard/tile taps) on GameScreen (all modes + post-game), ProperNoundleView, SolvedPuzzleView; dismiss() covers both fullScreenCover + pushed presentations. Live VS excluded (its exit forfeits). Android already had full BackHandler coverage = system edge-swipe back works everywhere. Build 22 carries all of it + everything from build 21; Android changes committed (AAB on request). Note: web Account section has a NotificationToggle Android still lacks a backend for — omitted, noted for later. The build-swap onto the approved 1.0 version still must be done in the ASC web UI (Pending-Developer-Release API limit, §9/§12) — build 22 is now the swap candidate once blessed.*

*2026-06-13 part 3 (post-build-20 polish wave → iOS build 21; web deployed): a run of user-reported fixes across all 3 platforms. **AdSense guides were invisible to crawlers** — AuthGate's PUBLIC_PATHS omitted /guides, so signed-out visitors + Google's JS-rendering crawler got the login Landing at every /guides URL (the guide content only existed in pre-hydration HTML). Added /guides to PUBLIC_PATHS — the remediation guides now actually render. Also gave the landing + guide pages real game icons (were letter/roman placeholders). **Solved-board ✓ badge clipped** on right-column boards (offset overhang hit the ScrollView clip bound) → anchored flush, kept vertical float. **Completed-daily card cross-device**: web (non-gauntlet) + Android only read the local session, so a daily beaten on another device showed nothing; added the server replay fallback (replayRecordedGuesses) on both — iOS already had it. Native summary wording corrected to web parity ("4/6 · 13s" single, "solved/total · Ng · 13s" multi) — the leaderboard ROW stays "4 Guesses · 13s". **Web profile**: Pro Stats showed raw DUEL_6/DUEL_7 (MODE_LABELS missing → now Six/Seven); sign-out cleared the footer (pb-20→pb-32). **iOS Pro Stats showed nothing even on the All view (real bug)**: UserStatRow decoded best_score/average_time/fastest_time as non-optional Int, but those are NULL for some rows (post-reconcile / no recorded time); Swift's strict JSONDecoder threw and the `try?` collapsed the whole fetch to [] → empty bars. Fixed with a null-tolerant custom init (decodeIfPresent ?? 0). Android was immune (Supabase serializer coerceInputValues=true). Both natives also now solo-filter Pro Stats to match web. Web fixes deployed; iOS fixes ride in **build 21** (also carries everything from build 20). Android changes committed (next AAB on request). Earlier this session: build-20 ASC swap is BLOCKED by the Pending-Developer-Release API limitation — must be done in the ASC web UI (see §9/§12); build 21 will be the one to swap once blessed.*

*2026-06-13 part 2 (build-20 swap ATTEMPTED, blocked by ASC API — NOT released): user asked to swap build 20 onto the approved 1.0 version (manual release). Build 20 is VALID on TestFlight. The swap could NOT be completed via the ASC API: version 1.0 is in PENDING_DEVELOPER_RELEASE (build 10 approved), and the API refuses to change the build relationship (INVALID_STATE) or add the version to a new reviewSubmission (STATE_NOT_SUITABLE_TO_SUBMIT: "Pending Developer Release") — there is no API endpoint to developer-reject an approved version back to editable. This is a NEW finding: the cancel→patch-build→resubmit flow only works from IN_REVIEW/WAITING, not from an approved-pending-release version. NOTHING BROKEN: build 10 still approved + releasable, release still MANUAL. The swap must be done in the ASC web UI (replace the build on the version, which performs the implicit un-approval) — steps in §12. One inert empty draft reviewSubmission (a176542b, 0 items, unsubmitted) remains as a harmless artifact (API allows neither DELETE nor cancel on an unsubmitted empty one). Durable ASC helper added at ~/.appstoreconnect/asc.rb (the /tmp scripts had been cleaned out, same as the export plist).*

*2026-06-13 (iOS build 20 — audit waves 1-3 + Royal palette; Delivery 41a28877): cut build 20 carrying everything since build 19 (PN daily-seed leak fix, POSIX date formatters, sequence guess-list shape + reconstruction, day-from-seed recording, gauntlet guess-list parity, UTC activity-calendar bucketing, PN Wikipedia clue consumes a board row + can lose, PN vowel/consonant loss check + reducer fix, Six/Seven zero-candidate hint counting). BUILD-PIPELINE NOTE: /tmp/wr_export.plist had been cleaned out of /tmp; recreating it by hand surfaced two gotchas now documented — the export plist MUST use signingStyle=automatic (with -allowProvisioningUpdates + the API key, so xcodebuild fetches the App Store provisioning profile; manual+no-profile produces an IPA with no embedded.mobileprovision → 409), and the applesignin re-sign MUST dump the COMPLETE exported entitlements first (codesign -d --entitlements - --xml, which includes application-identifier + team-identifier) then add applesignin — a partial/stale ent.plist drops application-identifier → 409. Working plist backed up to ~/.appstoreconnect/wr_export.plist so /tmp cleanup can't recur. Awaiting VALID on TestFlight.*

*2026-06-12 part 2 (AdSense denial remediation; commit 68adc41, live + verified): wordocious.com was denied by Google AdSense for "Low value content" — root cause: gameplay is login-gated so the crawler only saw ~6 thin public pages. Shipped: /guides index + 9 statically-generated per-mode strategy guides (original content — exact scoring formulas, hint economics, mode strategy; unique metadata each), landing mode-cards now link to their guides + Mode Guides footer link, sitemap +10 URLs. /ads.txt confirmed serving (pub-3015627373086578). Verified live. USER: request re-review in AdSense (Sites → wordocious.com) after ~48h crawl window; escalation if denied again = anonymous daily play (product decision). NOTE: AdMob (app ads) is unaffected — approval separate and intact.*

*2026-06-12 (AUDIT WAVE 3 — every remaining sweep item closed; commit ef2a3be; medals-key migration APPLIED by user): **Free unlimited-play hole closed (user decision):** all 9 web mode routes wrapped in UnlimitedGate — daily + Pro pass through, free non-daily URLs get a Go Pro upsell (matches natives; share-page Play links now hit the gate). **Lost-result hardening (web):** pending-record localStorage queue written before the network record, per-part cleared on success, drained on next signed-in home visit with a matches-row dedupe check; supabase errors now throw inside record paths (failure toast was unreachable). **Midnight rollover (all 3):** daily_results.day derives from the SEED's date, not the finish-time clock; iOS home-grid optimistic completion guarded against marking the wrong day. **Parity:** PN Wikipedia clue consumes a board row + can lose (web shape), PN vowel/consonant loss check (incl. a latent Kotlin reducer fix — PROPERNOUNDLE missing from SubmitHint status propagation), Six/Seven zero-candidate hints count toward the 150-pt penalty (both natives). **Android:** active-play timer accumulator (ProcessLifecycleOwner ON_STOP pause + persisted elapsed — backgrounded hours no longer count); DST-proof self-rescheduling reminder chain + timezone-change re-anchor; VS matches rows record solutions + both guess lists; cross-device Gauntlet replay advances stages. **Google Play RTDN webhook scaffolded** at apps/web/app/api/playstore (fail-closed 503; service-account JWT verification, idempotent is_pro/pro_expires_at re-sync, day-pass stacking; setup in its README) — the lock_pro_columns prerequisite both stores now have webhooks for. **DB:** medals unique key now includes medal_type (migration 20260612000001 — user ran it, success). FALSE POSITIVES KILLED: "Android missing sweep/medals/achievement detection" — both exist and are wired (MedalService.kt + 694-line AchievementService.kt called from record()). All six audits' confirmed findings are now fixed across waves 1-3; remaining user-side items unchanged (AdMob Android app/IDs + banner decision, Play Console setup, RTDN/Apple webhook env setup before lock_pro_columns). iOS fixes since build 19 await the next build cut (build 20 on request).*

*2026-06-11 part 2 (SIX-AUDIT BUG SWEEP → fix waves 1+2; commits 27c2301 + 2db6b13): ran six parallel read-only audits (recording matrix, double/zero-record, date/timezone, colorblind/dark regression, Pro/freemium gating, XP/medals/achievements), each verifying findings in both codebases. RESULTS: Royal re-theme = ZERO regressions (colorblind coverage actually expanded); XP/level/streak formulas certified identical on all 3. CONFIRMED + FIXED (wave 1): web restored-daily "re-record" effect ran the FULL stats pipeline per browser session — ROOT CAUSE of the phantom-wins the SQL reconcile cleaned up (now an idempotent daily_results upsert); Android Succession was UNWINNABLE past board 1 (submits targeted never-advancing currentBoardIndex → now applyToAll like web/iOS, active board = first PLAYING); iOS ProperNoundle UNLIMITED games recorded under the DAILY seed (could improve the daily leaderboard with random puzzles); iOS day-formatters lacked en_US_POSIX (Buddhist/Japanese-calendar devices minted year-2569 seeds); Android VS wrote a phantom solo matches row per match; Android PostGameScreen second daily writer (stage-local gauntlet values could beat the correct row); SEQUENCE recorded-guess shape unified to web's flat per-board concatenation + first-PLAYING-board replay on both natives. (Wave 2): daily-medal cron made idempotent (INSERT, skip XP on conflict) and moved 05:05→12:05 UTC (was excluding US evening players; its claimed client fallback was dead code); DEFERRED migration 20260612000001 adds medal_type to the medals unique key (USER: run in SQL Editor — old key let podium medals collide with perfect medals); web achievements' 4 day-scoped checks UTC→local; web VS queue re-derives the daily seed at join (stale-tab pairing); Android daily-seed latched per launch (midnight recomposition swapped live boards); Android recordDailyVsResult guarded + freemium VS lobby now server-checked (iOS parity); Android timestamptz parse via OffsetDateTime (API 26-29 silent streak resets); iOS gauntlet guess-list + activity-calendar parity (UTC buckets, player2 counted). STILL OPEN from audits (wave 3 backlog): Android has NO sweep/flawless bonuses, streak/perfect medals, or achievement unlock detection (feature port or move server-side); systemic lost-result risk (terminal state persists locally before the fire-and-forget network record on ALL platforms — needs a persisted pending-record retry queue); Android game timer counts backgrounded time (needs the web/iOS accumulator); daily day-from-seed at midnight rollover (all 3 record onto the finish-time date); PN Wikipedia-clue consumes a board row on web but not natives; Six/Seven zero-candidate hint counted on web only; Android reminder drifts after DST; Android test AdMob IDs + no banner (known user-side AdMob setup); WEB DECISION NEEDED: non-daily game routes (/quordle etc., linked from share pages) have no Pro gate — free users get unlimited play via URL; no Google Play RTDN webhook (blocks lock_pro_columns migration without breaking Android purchases). iOS builds 18 (Royal) + 19 (Royal + guess-count/keyboard/gauntlet fixes) VALID on TestFlight; wave 1+2 iOS fixes await the next build cut.*

*2026-06-11 (ROYAL PALETTE RE-THEME, all 3 platforms — builds: web deployed, Android compile-green, iOS build 18): user-driven rebrand of the tile colors away from Wordle's green/yellow trade dress onto the app's own palette ("B — Royal", chosen from rendered options): correct = brand purple #7C3AED (keyboard #6D28D9), present = amber #F59E0B (keyboard #D97706), absent = slate #64748B (keys #94A3B8). Scope was "everything game-colored" per user: board + keyboard tiles (incl. quadrant keyboards), VS opponent boards/result reveals/tug-of-war accents/confetti, solved-board borders+tints (violet-400/violet-50), completed-daily cards, gauntlet steppers/stage-complete overlays/review tints, win pills + W/L text (violet; light #F5F3FF/#7C3AED, dark #2E1065/#A78BFA), guess-distribution bars, activity-calendar heatmap (violet ramp #DDD6FE→#A78BFA→#7C3AED), share-card PNGs on all 3 generators (identical ports), emoji share grids 🟩🟨→🟪🟧 (web share-utils.ts + Android ShareHelper.kt; iOS shares image-only), and Help/FAQ/landing/how-to-play/support copy ("purple"/"amber"). DELIBERATELY KEPT: loss/error reds, medal golds, live-presence green dots, difficulty badges, trend arrows, invite/copied success greens, Simulate Pro toggle, RankDelta arrows, Seven's lime mode accent, and the colorblind orange/blue palette (unchanged on all platforms). Solo chips on profile lists moved green→blue (#2563EB) matching web; ProperNoundle consonant-hint button green→teal (stays distinct from purple Clue / blue Vowel). ARCHITECTURE: web colors now centralized — globals.css :root --tile-*/--key-* vars + .tile-*/.key-* utility classes (colorblind = var overrides under [data-colorblind], replacing the old !important .bg-green-500 hack; dark theme gets violet/amber border variants), lib/tile-theme.ts rewritten as the single hex/class source (share-image.ts imports it); iOS Theme.swift + ThemeManager.swift and Android Theme.kt carry the token flips, with ~45 hardcoded leak sites routed/flipped across ShareCardView/ShareImage, VS views, BoardView/MiniBoardView, GameScreen, GauntletViews/GauntletCompletedView, PostGame, Profile/PublicProfile, Home, Help/Info. Colors are never stored server-side (TileState enums only) — zero data migration, fully reversible. Verified: next build, xcodebuild simulator, compileDebugKotlin all green; straggler greps clean. NOTE: the 4 background re-theme agents froze when the user hit a usage limit mid-run; web profile/share surfaces + Theme.kt came from agents, everything else was completed directly afterward. Commits 96000de (web) / e2f0de0 (iOS) / ee0a7c4 (Android).*

*2026-06-10 update part 6 (TestFlight-pass bug wave — builds 14/15/16 + stats reconcile + co-tester setup): user's build-13/14/15 testing surfaced and we fixed, each respun into a fresh build the same hour: **(build 14, Delivery 9cf643a8)** guess-distribution buckets had been CLAMPED AT 6 on all three platforms (a 7/13 OctoWord win piled into "6") → buckets now run 1..modeMax (6/7/8/9/10/13) with GAUNTLET + the All view clamping into a labeled "N+"; victory-card multi-board solutions no longer wrap mid-word ("WORL D") — smaller font above 4 solutions + lineLimit + minScaleFactor. **(SQL reconcile, user-run in Supabase SQL Editor)** stats card said 46 wins while the chart said 39 — user_stats/profiles are lifetime counters that predate per-game match-history recording; a preview-then-apply SQL recomputed user_stats (wins/losses/games/best/fastest/avg) AND profiles totals from the matches table for all accounts, permanently dropping the phantom counter-only wins (user-approved; XP/streaks/medals/achievements untouched). **johnnyauer granted is_admin** (Simulate Pro button) for two-Pro VS co-testing; revoke before public launch. **(build 15, Delivery b6d5eb64)** four user reports: Gauntlet guess-distribution hidden everywhere (50-guess runs make a histogram meaningless); Succession "Couldn't load" — SolvedPuzzleView required the fire-and-forget matches row even when the LOCAL finished game existed → local-first rendering + one 1.5s retry for the insert race; Deliverance review showed no prefills + "won in 1 guess" — flat reconstruction replaced by a TRUE ENGINE REPLAY (createInitialState from the daily seed regenerates prefills, recorded guesses replayed through the reducer); cross-device completed dailies (played native → web showed a FRESH board) — web gained useServerDailyReplay in use-game-snapshot (RESTORE_STATE via reducer replay incl. Six/Seven hint rows, double-record guarded by the existing restored ref) and Android gained the same fallback (installReplayedState, never re-records, no VictoryOverlay). **(build 16, Delivery 5fb75791)** multi-board guess count: natives counted boards[0].guesses — which STOPS accumulating once that board solves — so a 12-guess OctoWord reported "9"; web always used max(b.guesses.length); natives now match at the single source (rowsUsed), fixing in-game header, post-game line, victory overlay, share text, recorded player1_score, daily composite, and VS player_completed at once. Web recording verified correct for every mode (max for parallel + sequence, gauntlet's own accumulator, VS counts server-side). All builds: archive → export → applesignin re-sign → altool → VALID.*

*2026-06-10 update part 5 (build 10 APPROVED → TestFlight builds 11/12/13 + AdMob GDPR published + freemium VS-scoring fix + instant completion updates + bible reformat): **Build 10 approved by App Review** (Pending Developer Release, manual). **AdMob GDPR (UMP) consent message published** via browser automation in the user's AdMob console — required setting the app's privacy policy URL (https://wordocious.com/privacy) in the message's app grid and choosing "Do not consent: Off"; message "Wordocious GDPR consent" live, targeted GDPR countries; remaining AdMob items: payment profile + post-release app-ads.txt verification. **Build 11** cut + uploaded (unsigned archive → export → applesignin re-sign → altool; Delivery 2d3e7f35) with the VS overhaul + Play Again + Unlimited-PN fix. User testing surfaced two fixes: (1) **freemium VS-scoring flaw** — the daily VS leaderboard's 3-game qualification floor meant 1-game/day free players scored a permanent 0; floor removed on ALL platforms (web lib/daily-service.ts calculateVsCompositeScore, Android DailyResultsService.vsCompositeScore, iOS DailyResultsService) — volume terms (wins·100 + games·5) still rank grinders higher; (2) **"· vs <username>" opponent names** on profile Recent Matches VS rows on iOS + Android (batched profiles lookup; web already had it). Both shipped as **build 12** (Delivery afd0e52e). Also clarified to the user: the morning iOS↔web match predates the records change and will never backfill onto the daily VS board (lives in Recent Matches / VS Record / head-to-head); anon-key DB probes are RLS-blind. Then (3) **instant home-card completion updates**: native home grids only refreshed completions on tab switch — the daily-record path now optimistically updates the local store FIRST (iOS NotificationCenter 'wordocious.daily-completion' consumed by DailyCompletionsStore; Android day-keyed cache write re-seeded on recomposition; best-result semantics — wins never downgraded) = **build 13** (Delivery 165a7b25, VALID on TestFlight). One Android Int/Double compile slip was pushed and fixed within a minute. Same day: WORDOCIOUS_BIBLE.md restructured to the SHOWLOUD_BIBLE format (sections first, this change log inside §7) with a full audit. PENDING: user's build-13 TestFlight pass (incl. live iOS↔web VS pairing with johnnyauer), then swap build 13 onto the approved 1.0 version and manual release.*

*2026-06-10 update part 4 (VS overhaul — pairing fix + records visibility + full experience upgrade, ALL platforms; server+web+iOS+Android, 4 commits): **PAIRING BUG (user report: iOS↔web never matched):** verified handshake compatible (same server/events/mode strings); root design flaw = server queues keyed by mode ONLY (first-two spliced regardless of seed; stale ghosts could absorb a side). FIX: matchmaking bucketed by (mode, dailySeed|random) + 10-min stale pruning + [queue] join/paired logs; queue_status now carries queueSize+dailySeed. Railway auto-deploys from main. NEEDS LIVE iOS↔web pairing test (task #75/#80 still open). **RECORDS (user report: no VS games visible):** writes were fine — surfacing wasn't: personal profile merged solo+vs with NO toggle (public had one) and Records only counts daily rows. Per user decision ALL VS now counts: every completed VS match records the daily_results play_type='vs' row (web stats-service + iOS GameResultsService + Android VSMatchViewModel; play-limit still daily-gated); personal profile gains Solo/VS toggle + VS RECORD card on all 3. **VS EXPERIENCE (all 3 platforms, web source of truth):** match_found now carries opponentUserId, match_ended carries opponentGuessLog+solutions (letters only after match end), new typing/opponent_typing relay. Features: 2.5s match-intro splash (avatar slam-in, Lv chips, ALL-TIME head-to-head per user decision — new head-to-head service on each platform, lines "You lead A–B"/"First meeting!"), persistent tug-of-war header (progress = 0.7·boards + 0.3·bestRowGreens, boundary clamp [0.1,0.9], purple you / pink them, crown #f59e0b, 500ms), moment callouts ("N greens! 😱"/"solved board N!"/"last guess!", 2.5s dedupe) + opponent-guess thunk (220Hz) + haptic, typing indicator (1.5s throttle, 2s decay), spectator waiting screen (2x colors-only live board, live clock, 5 stakes-copy variants incl. "must solve in N or fewer"), result rewrite (WINNER/DRAW/DEFEAT, win-only confetti, comparison bars, side-by-side FINAL BOARDS with letters, h2h refetch @1.2s, prominent Rematch keeping Pro gates), VS stinger (392+523Hz). Android also fixed opponent strip ABSENT color + pop-in. ProperNoundle gotcha honored (no local solution until end → 0 greens, reconstruct at end). Verified: server tsc, web tsc, xcodebuild, compileDebugKotlin all green. iOS changes in build-11 queue.*

*2026-06-10 update part 3 (Pro daily-vs-unlimited tweak, all 3 platforms): user spec — Pro daily tab tapping a COMPLETED puzzle must open the completed/solved screen (never a new puzzle; new puzzles only from the Unlimited tab), and Unlimited post-games need a "Play Again" next to Home/Share. AUDIT RESULT: **web already compliant** (snapshot persistence resumes finished dailies; Play Again exists on all non-daily Pro game components, gauntlet prop-gated) — no web changes. **iOS already routed Pro completed-daily taps to SolvedPuzzleView**; added Play Again to FinishedStatsHeader + GauntletResultsView (gated !isDaily && Pro; HomeView mints a fresh unlimited seed + updates unlimited-current-MODE), AND fixed a real bug: Unlimited ProperNoundle fell through to the daily NavigationLink and replayed the DAILY puzzle — PN now takes seed+onPlayAgain via a fullScreenCover item with a fresh seed per tap. **Android** daily-completed taps already resume into PostGameScreen via persistence; added Play Again ("Try Again" on loss, amber, web Classic convention) threaded MainScreen→GameScreen→PostGameScreen, gated on seed unlimited- prefix + Pro; Play Again swaps activeSeed (new VM). xcodebuild + compileDebugKotlin green; iOS in build-11 queue.*

*2026-06-10 update part 2 (ANDROID "all of it" — every remaining gap built; 8 commits, signed release AAB green): **Wave A** — account deletion in Settings (POST wordocious.com/api/account/delete w/ Bearer, web/iOS copy; Play compliance), VS daily leaderboard recording (recordDailyVsResult port, called on daily VS match end), live presence count (LIVE banner polls {server}/presence per 10s), daily reminder notifications (WorkManager 18:00-local + POST_NOTIFICATIONS runtime perm; Settings toggle wired), https://wordocious.com deep links. **A3** — FULL native achievement unlock detection: all ~70 web checkAchievements checks ported 1:1 (agent-built, compile-green first try), wired into GameResultsService.record (solo + VS); local-day deviation per app standard. **B1** — native Google sign-in: Credential Manager + googleid → Supabase signInWithIdToken (web client audience 193086095286-2h2…; Skip-nonce already ON); REQUIRES creating an ANDROID OAuth client (package com.wordocious.app + SHA-1 7A:2C:E8:AC:82:B7:F7:F6:10:64:E1:9D:CC:88:23:0A:8E:63:79:B0) in the Google Cloud project. **C wave** — gauntlet stage-transition interstitial + stage list w/ tap-to-review modal (boardsSnapshot renderer), pending VS invites banner (InviteService + Play→join-by-invite-code route), streak-shield modal (ShieldService: >20h + day-rolled at-risk check, useShield/decline; checked once per session in MainScreen). **D1** — Google Play Billing (billing-ktx 7.1.1): StoreManager mirrors iOS (pro_monthly/pro_yearly subs +4 shields, stacking pro_day consumable, obfuscatedAccountId=uid, restore); ProScreen CTAs wired w/ real prices + fallbacks. USER TODO: create the 3 products in Play Console + internal-testing track + license testers. **E1** — AdMob: UMP consent → MobileAds init → game-start rewarded interstitial for free users (ad time excluded from game timer via addPausedTime); GOOGLE TEST IDs until a Wordocious ANDROID AdMob app is created (swap manifest APPLICATION_ID + AdsManager unit). iOS parity note: banner ads unused on iOS too — interstitial-only is full parity. VS interstitial deferred (matchmaking starts in VM init). **F1** — real adaptive launcher icon built from AppIcon-1024 (gradient bg #7D3AEC→#EB489A + extracted white-W tile foreground, all densities + legacy); release signing via gitignored key.properties → keystore ~/.android-keys/wordocious-upload.jks (BACK IT UP; storepass inside apps/android/key.properties); SIGNED bundleRelease AAB builds (18.5MB); web /.well-known/assetlinks.json published (upload-cert SHA-256 57:2C:24:66…) so deep links auto-verify. **Remaining USER setup before Play submission:** Play Console app + products + track, Android AdMob app + real ad IDs, Android OAuth client for Google sign-in, Play listing/privacy form. Android now has ZERO known feature gaps vs web/iOS.*

*2026-06-10 update (ANDROID feature-gap closure — the 5 previously-deferred items are now BUILT; 3 commits 7477b6d/455fa61/2a0062a, assembleDebug green): audited web (3 agents: sounds / medals+pro-prompt / public-profile+share-image) then built: **(1) SoundManager.kt** — exact web lib/sounds.ts synthesized tones (keyTap 800Hz·40ms·0.06, invalid 200+150Hz, success C5-E5-G5, gameOver G4-E4-C4) via AudioTrack PCM with the iOS (1-t)^1.5 envelope, gated on pref-sound (existing Settings switch); call sites = every keyboard key, rejection, VictoryOverlay mount (win/loss jingle; gauntlet losses silent = web); new SoundToggleButton top-right of the game header. **(2) MedalService.kt** — streak_7/30/100 + perfect medals (exact web criteria) + awardDailyBonusesIfComplete → (sweep 200, flawless 400), idempotent daily_bonuses upsert; GameResultsService now records the daily_results row FIRST (idempotent; PostGameScreen's call now a no-op) then awards — web stats-service ordering; XpResult gains sweepBonus/flawlessBonus; XpToast pink/yellow chips + 5s dwell. **(3) Pro-prompt banner** on Home (streak≥7, not Pro, not dismissed; Crown/gold/Go Pro gradient; persists local + profiles.pro_prompt_shown via new AuthService.markProPromptShown; Profile model + select* gains pro_prompt_shown). **(4) PublicProfileScreen.kt** — web /profile/[id] parity (avatar/gradient name/level badge/XP bar/social-link pills from profiles.social_links, 4 overall cards w/ 1-decimal win rate, solo-vs toggle + mode chips + per-mode Wins/Losses/Best/Fastest, Top Words, 10 recent matches); state-overlay route in MainScreen; leaderboard usernames + all-time record holders now tap through; ProfileService.UserStat gains play_type. **(5) ShareImage.kt** — full Canvas port of web share-image.ts (1080×1080/1350, gradient wordmark, meta formats, W/L + PN category pills, two-pass wordGroups gaps, win/loss card tints, gauntlet stage chips, footer) shared as PNG via new FileProvider + EXTRA_TEXT, text-only fallback; PostGameScreen Share sends card+text. NOT ported (deliberate): iOS's Supabase share-images bucket upload + /s/ URL. Android now has NO known deferred web-parity features. User tests via emulator only (no Android device).*

*2026-06-09 update part 6 (ANDROID port of the full parity slate + main game-board parity — 6 commits, compile + assembleDebug green): Ported the entire iOS fix wave to Android (web stays source of truth; 3-agent triage first, every finding verified in both codebases). **Batch commits aa599a6→509bc59:** (1) dark-theme W/L+gold tokens (7 new Palette tokens + dark overrides), HINT_USED ghost tile, colorblind quadrant cells, web-param confetti (50pc/8 colors/2–4s/720°) + XP-toast fade-in-up; (2) Skeleton.kt + RankDelta.kt (new), leaderboard/records skeletons + empty states, playerCount (postgrest count, no solo filter), day-keyed daily-completions cache, WotD skeleton; (3) gauntlet win-only overlay + full-run share text, multi-board "N/M solved" label, ProperNoundle daily #N + post-game Wikipedia image/answer reveal (WikipediaHint.fetchImageUrl, ProperNoundle.dailyPuzzleNumber/puzzleFor); (4) VS match header live stats, rematch upsell modal (non-Pro Rematch was a SILENT no-op), already-played countdown + Go Pro CTA; (5) profile chart empty states + Recent Matches loading/empty, auth validation + tappable legal links, edit-profile error surfacing + length-only username validation, welcome copy, privacy AdMob sections, settings copy. **Then a dedicated main-game-board audit (user request) → 91c8b6f:** rejection toast ("Not enough letters"/"Not in word list"/"Already guessed", top-90px dark pill) + input cleared at 600ms + keys gated while shaking (web handleKey timing); tile flip now a REAL rotateX 0→90→0 at web timing (0.5s full/0.3s mini; was a 160ms scaleY grow from invisible); single-board tiles sharp-cornered, mini 4dp, ABSENT keeps gray-300 border; colorblind MINI palette = cyan-600/orange-500 (web multi-board differs from single-board override — audit caught it); keyboard unstated-key borders, font-black, BACK=backspace icon, ENTER text, light/medium haptics, quadrant letter text-shadow; Sequence locked boards gray-50 + Lock icon + gray masked tiles; octo zoom min(0.9w,384dp)×min(0.95h,w·2.2) + fade-in-scale; combined keyboard states skip non-playing boards; header 30sp title + Trophy/Clock icon spans (no "·"). **Deliberately deferred on Android (feature-missing, not parity bugs):** key-tap/game sounds + SoundToggle (no audio engine yet), share-image pipeline, sweep/flawless XP chips (GameResultsService comment marks it deferred), public profiles, pro-prompt modal. No Android device — user tests via emulator/sim only.*

*2026-06-09 update part 5 (BUILD 10 SUBMITTED — replaced build 9 in review, per user decision): bumped CURRENT_PROJECT_VERSION 9→10 (c2fbd10) and cut build 10 with the full 12-commit parity+aesthetic queue (Simulate Pro fix, Unlimited cards, parity batches 1-6, aesthetic batches 1-2, rank-delta badge). Same flow: unsigned archive → exportArchive (ASC key) → re-sign with applesignin (still stripped by export — verified 0 pre-resign, OK after) → altool upload SUCCEEDED (Delivery UUID 7bab6115; CFBundleVersion verified 10 in the IPA pre-upload). Processed to VALID within minutes. **Chrome extension was down, so the ASC swap was done ENTIRELY via the App Store Connect API** (ES256 JWT helper at /tmp/asc_api.rb pattern): (1) PATCH reviewSubmissions/8aa9eedd… canceled=true (the build-9 submission → CANCELING), (2) PATCH appStoreVersions/5ddc9045…/relationships/build → build 7bab6115 (verified attached build = 10), (3) POST reviewSubmissions (new id 71560670-bf19-4078-a2a6-500d48281b8f) + POST reviewSubmissionItems (the 1.0 version) + PATCH submitted=true → **WAITING_FOR_REVIEW** (submitted 2026-06-09T22:14Z). Verified: version 1.0 state WAITING_FOR_REVIEW, releaseType=MANUAL, attached build 10 VALID. The ATT/EULA Review Notes + Description EULA block carry over (version-level metadata). Build 10 also auto-distributes to TestFlight internal testers. NEXT: user TestFlight-tests build 10 (key visual checks: dark theme pills, gauntlet-win celebration, NO confetti on losses) while Apple reviews; on approval, manual release.*

*2026-06-09 update part 4 (AESTHETIC parity sweep — themes/motion/sound/loading audited + fixed; the final parity dimension): Three agents diffed theme palette tokens, animation timings/curves, sound events, and loading/empty states value-for-value. **Verified at parity:** all 4 theme palettes (light/dark/ocean/forest surface+text tokens match globals.css hex-for-hex), tile flips (0.5s/150ms + 0.3s/80ms mini), shake, gauntlet glow period, XP-toast dwell, the entire synthesized sound palette + toggle gating, board/keyboard colors, single-board colorblind values. **Fixed (batch 1, e75761b):** (a) iOS CELEBRATED LOSSES — VictoryOverlay fired confetti + a success haptic unconditionally; now win-only (web losses get a quiet fade-in card). (b) Gauntlet WINS got no celebration (explicit !isGauntlet gate) — now overlay+confetti on a won run; gauntlet losses also skip the game-over sound (web parity). (c) Win/loss jingle now fires when the overlay mounts (after the final flip), matching web timing. (d) DARK THEME: 7 new themed tokens (win/loss-bg/text, highlight-gold, gold-border/-light) with the globals.css:41-48 dark overrides — applied ONLY where web consumes the CSS vars (W/L pills ×3, rank banner, isMe gold rows, RecordStatCell, all-time gold card, RankDelta); spots web hardcodes (streak pill, completed-daily card, DailyRankBadge, share image) stay hardcoded = parity. (e) Theme.reduceMotion unified = in-app toggle OR OS setting, gating everything (tile flips/glow previously ignored the in-app pref; confetti/toasts ignored the OS setting). (f) Colorblind palette now reaches the quadrant keyboard mini-cells. (g) Confetti matches web (50pc 12×12 rounded, 2–4s linear, 720°, 8-color palette); XP toast = fade-in-up 8px/300ms; victory card = scale 0.8→1.0 ease-out; HINT_USED tile = faint gray-100/200/300 ghost. **Fixed (batch 2, ca6fc87):** new Skeleton.swift (animate-pulse analogue) replaces spinners on daily leaderboard, Records daily+all-time, Word of the Day (structural), profile Recent Matches (previously NO loading state); profile charts match web empty behavior (calendar/heatmap/TopWords/ProInsights/ProStats HIDE when empty; GuessDistribution/SolveTime get web's specific empty copy); Records daily empty gains the trophy, per-mode all-time shows trophy+"No records yet" instead of a dash grid, Recent Matches empty copy added; DailyCompletionsStore seeds from a day-keyed UserDefaults cache so cold launches don't flash unbadged cards (web sessionStorage parity); public-profile loading = pulsing "Loading...". **Deliberately platform-idiom (left):** VS invalid-guess sound (iOS exceeds web). PARITY — functional, copy, AND aesthetic — is now fully swept. Build-10 queue = 12 iOS commits.*

*2026-06-09 update part 3 (parity COMPLETE — rank-delta badge + the two web cleanups): **(iOS, batch 7)** Ported web ui/rank-delta.tsx → new `RankDelta.swift`: in-memory session store (analogue of sessionStorage) remembers your last-seen rank per (pageKey, mode, playType); on change, a green/red "+N/−N" trend pill (arrow.up/down.right, win/loss palette) shows for 5s then fades. Wired into the Leaderboard rank banner (pageKey "daily") and the Records daily rank line (pageKey "records-daily") — same two spots as web. NOTE: new file → ran `xcodegen generate` before compiling. **(web, live via Vercel)** (1) Pending-invite banner now maps `game_mode` through PROFILE_MODES to the display title ("Classic", not raw "DUEL"). (2) Renamed the misleading `getSecondsUntilMidnightUTC` → `getSecondsUntilMidnightLocal` across all 7 files + dropped the daily-service alias — it ALWAYS computed local midnight, so the "UTC vs local countdown" audit finding was a naming bug, not a behavior bug; `tsc --noEmit` clean (ran `pnpm install --ignore-scripts` in the ~/Developer clone — 3.6s, fully cache-reused). **WEB↔iOS PARITY SWEEP IS NOW COMPLETE — zero known parity items remain.** Build-10 queue = 9 iOS commits: 8449f0a, 5c0fa5a, 0131dfd, 29eca65, e0b5427, 6c235c8, 705fee3, 1efac00, +rank-delta.*

*2026-06-09 update part 2 (iOS parity sweep WAVE 2 — modals/share/EditProfile/achievements audited; 3 more fix batches, all compile-checked; build-10 queue now 8 commits): Second audit wave covered the never-audited surfaces: modal family (streak-shield/pro-prompt/mode-limit/XP toast/invite banner), share output, AppHeader, Word of the Day, Edit Profile, achievements catalog. **At full parity already:** streak-shield modal, AppHeader, Word of the Day (solutions.json byte-identical), achievements (web 70 = iOS 70, all keys/names/descriptions identical — the "72" in old notes was stale). **Fixed (batch 4, 6c235c8 — the deferred LOWs):** non-Pro Rematch now opens the VSLimitModal upsell (de-privatized for reuse); VS match header shows live N/M guesses + elapsed clock; ProperNoundle header shows daily "#N" (dailyPuzzleNumber = daysSinceEpoch+1) and result copy matches web (green name + "Solved in N guesses · time" / red "The answer was: X"); Auth client-side validation (password ≥6, signup username 3-20 required, random-username fallback removed). **Fixed (batch 5, 705fee3):** HIGH — ModeLimitModal's showViewSolved was dead code, so the locked VS card's "View Solved Puzzle" dead-ended into an EMPTY fullScreenCover → now shows web's "Come back tomorrow" dismiss; HIGH — live Gauntlet share only mapped attempted stages + last-stage guess count (stage-2 loss shared "1/2 stages · 3 guesses" vs web "1/5 · 9") → now maps all stage configs + run-total guesses, matching web and the (already-correct) iOS revisit path. MED — XpResult splits sweepBonus/flawlessBonus → distinct "+200 sweep"(pink)/"+400 flawless"(yellow) toast chips + 5s dwell (MedalService.awardDailyBonusesIfComplete now returns the split); ProperNoundle finally shows the post-game XP toast (was the only mode without it); pro-prompt dismissal persists to profiles.pro_prompt_shown (cross-device; added to Profile.selectColumns); EditProfile drops the iOS-only username charset regex (web/DB validate length-only — the regex locked out users whose web-set name has a space/hyphen), surfaces avatar-upload failures + real save errors; avatar fallback = 2-char initials. **Fixed (batch 6, 1efac00):** ProperNoundle share image gains the category pill + full tile-width gaps between first/last-name groups (two-pass tile sizing ported from share-image.ts) + PROPERNOUNDLE label casing; ShareService/ShareCardView gain optional category/wordGroups. **Deliberately NOT matched (web-side fix candidates):** invite-banner copy (web prints raw "duel"; iOS's "Duel" is better), VS countdown UTC-vs-local (web internally inconsistent; iOS self-consistent local). **Only remaining parity item anywhere: rank-delta "+N/−N" badge** (net-new transient web feature; build on request). **Build-10 queue = 8449f0a, 5c0fa5a, 0131dfd, 29eca65, e0b5427, 6c235c8, 705fee3, 1efac00.***

*2026-06-09 update (iOS parity sweep — 8-screen web↔iOS audit + fixes, all compile-checked; queued for build 10): While build 9 sits in App Review, ran an adversarial multi-agent parity audit (Leaderboard, Records, Pro, Post-game, VS, Auth, Onboarding, Help/Info, Public profiles) — each agent returned file:line evidence from BOTH codebases; verified every finding before coding (the two leaderboard agents even contradicted each other on the Win/Loss pill — resolved by reading the actual rows). **Pro page + Help = already at full parity.** Fixed & committed (all `xcodebuild` green): **(Home, 5c0fa5a)** Unlimited cards now show static descriptions (no daily W/L badge/tint) — was the user-reported gap, mirrors Android #91. **(Simulate Pro, 8449f0a)** dev toggle now also sets pro_expires_at +1yr so it activates despite a lapsed expiry. **(batch1 0131dfd)** daily-row time format M:SS→web "Ns/Nm Ns"; Records dailyRow gained Win/Loss pill + multiboard fraction; playerCount counts all play_types (web getDailyPlayerCount has no solo filter); multi-board in-game header shows guesses count; Settings "Daily Reminders"/"Sign Out" copy. **(batch2 29eca65)** onboarding pillar copy + "Let's Play!" CTA + username helper; Auth Privacy/Terms now tappable NavigationLinks (App Review needs working legal links); **Privacy page adds AdMob/Advertising + ATT disclosure + Data Security/Your Rights/Children's Privacy/Changes/Contact sections** (material for an ad-serving build); Records record-holder + daily-row usernames now link to public profiles (were dead text); public-profile win-rate one-decimal. **(batch3 e0b5427)** VS already-played screen gains gold "Upgrade to Pro" (NavigationLink→ProView) + live "Next daily VS in HH:MM:SS" countdown; public-profile social-links row (ProfileExtras.socialLinks); ProperNoundle result Wikipedia answer photo (new WikipediaHint.fetchImageURL from the same REST summary, green/red border). **(web 9e29f02, live via Vercel)** About "midnight UTC"→local; Support scoring example fixed (guess bonus is hint-modes only, no Classic guess/completion bonus). **STILL DEFERRED (all LOW):** VS rematch upsell-modal (vs inline msg), VS in-match elapsed timer/guesses counter, ProperNoundle puzzle-#N + finished-line copy, auth password min-length, rank-delta badge. **Build-10 queue = 8449f0a, 5c0fa5a, 0131dfd, 29eca65, e0b5427** (all dev/UI, none affect build 9 in review). User testing build 9 on device; will cut build 10 when they finish the testing pass.*

*2026-06-09 update (iOS App Store REJECTION fix → build 9 uploaded; macOS iCloud build-hang root-caused & fixed): **App Review rejected build 8 (1.0)** on two guidelines (reviewed on iPad Air 5): **(3.1.2(c) Subscriptions/EULA)** the App Store Description lacked a functional Terms of Use (EULA) link. FIX (metadata, no build needed — done in ASC): appended a WORDOCIOUS PRO section to the Description with subscription names/lengths/prices (Monthly $6.99, Yearly $59.99), standard auto-renewal language, a link to the standard Apple EULA (https://www.apple.com/legal/internet-services/itunes/dev/stdeula/), and Privacy Policy (https://wordocious.com/privacy); Saved. **(5.1.2(i) Privacy/ATT)** privacy labels declare tracking but the reviewer never saw the ATT prompt. ROOT CAUSE: ATT was requested at the tail of a fragile chain (cold-launch `.task` → Google UMP network round-trip → consent form → ATT) AND while the app could still be `.inactive` (iOS silently returns `.denied` WITHOUT showing the prompt). FIX in `AdsManager.swift` (committed 9ebbbca): new `requestATTOnce()` that presents the prompt exactly once but only once `UIApplication.applicationState == .active` (retries every 0.4s until active), plus a 3s watchdog so ATT fires even if the UMP callback never returns (offline during review); `afterConsent` routes through it before GMA init. Personalized ads retained (user chose keep-ads over non-tracking). Bumped CURRENT_PROJECT_VERSION 8→9. Added ATT-location + EULA Review Notes in ASC. **Build 9 cut WITHOUT the cloud re-sign quirk:** archived UNSIGNED (`CODE_SIGNING_ALLOWED=NO`) → `xcodebuild -exportArchive` (app-store-connect, automatic signing, `-allowProvisioningUpdates` + ASC API key C8FRS9T697 / issuer 8bdd3f73-0d8b-427d-95c7-8097b77dfb7a — App Store distribution profiles need NO registered devices, unlike the dev profile that blocked a direct signed archive). Export STILL stripped `com.apple.developer.applesignin` (known quirk) → re-signed the .app with full entitlements incl applesignin (embedded `iOS Team Store Provisioning Profile` permits it) → repackaged → `altool --upload-app` SUCCEEDED (Delivery UUID 384f3c0b). Build 9 processing in ASC. **NEXT:** once processed, attach build 9 to the 1.0 version (replace build 8), confirm Manual release, Resubmit to App Review. **macOS BUILD-HANG ROOT CAUSE (also hits ShowLoud):** repos lived under `~/Desktop` which is iCloud-synced ("Desktop & Documents" + "Optimize Mac Storage" ON); at 91% disk, iCloud evicted project file CONTENTS (names stay, so `ls`/`find` look fine but reads hang forever), and `fileproviderd` pegged 130%+ CPU. Fixed: cleared 13GB (DerivedData 5.4G + old Archives 7.7G), killed wedged git/xcodebuild procs, and **cloned a fresh fully-local copy to `~/Developer/WordRush`** (reads went minutes→0.009s) — all iOS build work now runs there. RECOMMEND to user: turn OFF iCloud "Optimize Mac Storage" (do NOT toggle "Desktop & Documents" off — current macOS removes local files), and keep code under `~/Developer` (non-synced). Stale committed `apps/ios/build/` dir flagged for cleanup. Active git repo is now BOTH the old ~/Desktop copy and ~/Developer clone (same origin); committing from ~/Developer going forward.***

*2026-06-08 update (Android part 9 — #91 Profile parity + Unlimited W/L fix, verified live): **(Home Unlimited)** In Unlimited mode the mode cards no longer show the daily W/L badge or "{guesses}·{time}" (those are daily-only) — they show the mode description and every tap starts a fresh puzzle (pass `shownCompletion=null` when unlimited). VERIFIED: Classic shows "1 word, 6 tries" in Unlimited (was "L · 6 guesses·57s"). **(Profile buttons)** Matched web: "Edit profile" is now a rounded-full pill (surface-hover bg + pencil icon + lowercase label, web EditProfileButton), and Go Pro + Simulate/Disable Pro sit SIDE-BY-SIDE in a gap-2 Row (were stacked). **(Today's Dailies clickable)** Each daily badge is now tappable (web `<Link href="/{mode}?daily=true">`) → opens that mode's daily game via new ProfileScreen `onPlayDaily` → MainScreen (activeGame=modeCardFor(mode), daily seed). VERIFIED LIVE: tapping the played Classic badge opened the completed-puzzle screen (board + Top 50% rank badge + score breakdown + definition); an unplayed badge starts the fresh daily. **Remaining Android: #80 VS 2-player test, #75 full mode playthrough, Google Play Billing; open Qs: harden Simulate-Pro to clear pro_expires_at (web+Android), revert AppDevTester Pro/admin.***

*2026-06-08 update (Android part 8 — Simulate Pro toggle + #90 Daily/Unlimited no-shift + Sweep/Flawless hero, verified live): **(Simulate Pro)** is_admin-gated "Simulate Pro / Disable Pro" button in the Profile header (1:1 with web profile/page.tsx) — flips is_pro via AuthService.setProDev + reload. Added is_admin to Profile. Verified live: flips AppDevTester to PRO (header/pill badges), enabling the Pro-only Home UI. **(#90 no-shift)** All home heroes now share a fixed `HERO_HEIGHT` (78dp, centered) so toggling Daily<->Unlimited no longer shifts the game-cards grid — VERIFIED LIVE: GAME MODES y=887 / Classic y=1103 identical in BOTH states (was a ~10px jump). **(#90 Sweep/Flawless)** DailyHero now swaps to **Daily Sweep!** (purple/AutoAwesome, "All 9 dailies completed · +200 XP earned") once all 9 dailies are done, and **Flawless Victory!** (gold/EmojiEvents, "+600 XP earned") when all 9 are won — matching web page.tsx; the Profile Today's-Dailies card already had these states. **(Pro Unlimited UI — verified live via Simulate Pro)** Daily/Unlimited PlayModeToggle, UnlimitedHero, and the teal VS-swords overlay on non-VS cards all render correctly for Pro. **WEB BUG FOUND (diagnosed via the user's Chrome + Supabase):** the web's Simulate Pro button only flips is_pro and never touches pro_expires_at, so once an account's pro_expires_at lapses, toggling is_pro can't re-activate Pro (isProActive = is_pro && (expires_at null || > now)). BMT had pro_expires_at=2026-06-06 (expired) → not Pro despite is_pro=true; fixed by setting pro_expires_at=null. RECOMMEND patching web Simulate Pro (+ Android setProDev) to also clear/extend pro_expires_at. **Remaining Android: #80 VS 2-player test, #75 full mode playthrough, Google Play Billing; optional: harden Simulate-Pro to clear pro_expires_at.***

*2026-06-08 update (Android part 7 — web-faithful mode icons everywhere + Pro Unlimited dimension): **(Icons)** Added one shared `ModeGlyph(card/mode, tint, glyphSize, iconSize)` in ModeCatalog (+ public `modeIconRes`/`modeCardFor`) that renders a mode EXACTLY as the web/home card does — roman/number glyph (IV/VIII/6/7) or the lucide drawable (WordleGrid/Swords/TrendingUp/Shield/Skull/Crown). Wired into Home cards, the **Leaderboard 5-over-4 grid** (was C/S/D/G/P letters), and **Profile** dailies + mode-picker (was MODE_GLYPH letters). VERIFIED LIVE: leaderboard grid shows WordleGrid/TrendingUp/Shield/Skull/Crown icons (not letters). **(Pro Unlimited dimension — the deferred #85 remainder)** New `PlayModeToggle` (Daily/Unlimited pill, Pro-only, Star/AllInclusive icons) + `UnlimitedHero`; HomeScreen shows the toggle for Pro and swaps DailyHero↔UnlimitedHero. **Unlimited routing:** Pro+Unlimited card taps pass `unlimited=true` → MainScreen builds a fresh non-daily seed `unlimited-<mode>-<nanoTime>`; `PostGameScreen` now records daily_results ONLY for `seed.startsWith("daily-")` so Unlimited games never clobber the daily leaderboard (XP/matches/user_stats still record — "all stats count"). **VS-swords overlay:** non-VS cards in Pro+Unlimited show a teal swords button (bottom-end) → quick-match that mode (onVs → vsActive). HomeScreen `onSelectMode` signature is now `(ModeCard, unlimited:Boolean)`; added `onVs`. **NOT live-verified:** the Pro-only toggle/hero/swords (test account is free; no Simulate-Pro toggle on Android — needs is_pro flip to see). **Still deferred:** cross-device completed-board reconstruction from `matches` (device-local session only), Gauntlet/ProperNoundle completed-board detail. **Remaining Android: #80 VS 2-player test + deferred VS pieces, #75 full mode playthrough, Google Play Billing, + live-verify Pro Unlimited once a Pro account is available.***

*2026-06-08 update (Android part 6 — leaderboard completed-board dropdown + 5-over-4 mode grid + #85 Home Pro/Unlimited lock+modal, all verified live): **(Leaderboard CompletedDailyBoard)** Ported the web CollapsibleCompletedCard — a "Completed/Attempted Today" dropdown above the leaderboard for the selected mode (green✓/gray✗ accent bar + badge, summary "{g}/{max} · {time}[· hints]", ChevronDown). Reads the finished daily from `GamePersistence.load(todayLocalSeed(mode), mode)`; expands to the board(s) + solution + DefinitionService block + stats + reused (now `internal`) ScoreBreakdownCard. Single + multi board supported; renders nothing if today's daily isn't finished for that mode. **Board-collapse fix:** MiniBoardView uses weight(1f) rows so it needs a height — wrapped each board Box in `.aspectRatio(cols/rows)` (width 180dp single / 64–96dp multi). **(5-over-4 mode grid)** Replaced the leaderboard's horizontal-scroll ModePickerRow with the web `<ModePicker grid>` layout — top row 5 modes, bottom row 4 centered (Spacer 0.5 each side), accent icon-box cells (IV/VIII/6/7 glyphs + short titles) — so all 9 games are visible without scrolling, matching the Profile dailies. **(#85 Home Pro/Unlimited)** Free users get one daily play per mode; once played the card LOCKS (`!isPro && completion!=null` → dimmed alpha 0.6 + gray #D1D5DB border) and tapping opens the new `ModeLimitModal` (lock glyph, "{mode} — Played Today", upsell, live "play again tomorrow" countdown, gold Upgrade to Pro→Pro page, "View Solved Puzzle"→opens the finished daily). Pro users replay unlimited (no lock). Wired via HomeScreen `onGoPro` → MainScreen `infoRoute="pro"`. **VERIFIED LIVE:** leaderboard shows the 9-mode grid + "ATTEMPTED TODAY · 6/6 · 57s" card expanding to the full CRANE/MOIST/… board + LAMIA + noun definition + "0 pts / Did not finish" breakdown; Home Classic/Six cards dimmed for the free account, tap → ModeLimitModal with 10:46 countdown. **DEFERRED:** Pro PlayModeToggle (Daily/Unlimited hero) + Unlimited fresh-seed routing + VS-swords-on-cards overlay (the rest of the Pro Unlimited dimension); cross-device completed-board reconstruction from `matches` (currently device-local session only); Gauntlet/ProperNoundle completed-board detail. **Build note:** the macOS stale-incremental-cache cascade keeps recurring after `find '* 2.*' -delete`; `rm -rf app/build/kotlin` + rebuild reliably clears the spurious "unresolved reference" flood. **Remaining Android: #80 VS 2-player test + deferred VS pieces, #85 Pro Unlimited toggle/overlay, #75 full mode playthrough, Google Play Billing.***

*2026-06-08 update (Android part 5 — #80 VS multiplayer board-mode flow + #72 aesthetic audit, lobby/queue verified live): **(#80 VS)** Added `io.socket:socket.io-client:2.1.0`; built the full VS stack porting iOS 1:1 — `VSConfig`, `VSProtocol` (all wire events/payloads), `VSMatchService` (socket wrapper: handshake `auth.presenceId`, all emits, JSON→kotlinx decode marshalled to main thread), `PresenceService`, `VSPlayLimit` (SharedPreferences daily gate), `recordVsMatch` (single shared matches row), additive **versus hooks in GameViewModel** (onGuessCommitted/onBoardSolved/onCompleted relay, ephemeral state, stopTimer — solo path untouched), `VSMatchViewModel` (queue→countdown→match→waiting→result→rematch state machine), and Compose `VSLobbyScreen` + `VSGameScreen` + `VSOpponentStrip` (opponent mini-board colors-only preview, cycling status), wired to the Home VS card via MainScreen. **VERIFIED LIVE on emulator:** lobby renders (iOS parity — swords/Play Daily VS/Pro upsell); tapping Play Daily VS **connected to server.wordocious.com, handshook, emitted join_queue, and received queue_status → "Position in queue: 1"** (full socket layer proven end-to-end); Cancel returns home. **Scoped to board modes** (DUEL/DUEL_6/7/QUORDLE/OCTORDLE/SEQUENCE/RESCUE). **DEFERRED (need new infra):** Gauntlet-VS + ProperNoundle-VS (no ProperNoundleVM), private invite codes (no InviteService), AchievementService.checkAchievements writes, ads-on-VS-start; and the actual MATCH→RESULT flow needs a 2-player live test (can't verify on one emulator). **(#72 aesthetic audit)** Ran 5 parallel Explore agents (web source = truth vs Android), verified each finding in BOTH codebases, applied 8: victory-title gradient 2→3 color (gold), onboarding pillar copy ×3 ("9 unique game modes"/"Climb the Leaderboards"/"Earn medals, build streaks…"), WotD part-of-speech → plain italic #7C3AED (was a pill) + definition #4B5563 bold 11sp, Records subtitle "The best of the best across Wordocious", daily countdown → Clock icon (dropped "Resets"), Profile summary cards 12dp/1.5dp/14dp, chart cards 14→16dp, leaderboard rows 14/10→16/12. SKIPPED (spec-gospel, not bugs): keyboard 7/5dp spacing. DEFERRED: Records tab-bar restyle, mode-picker grid, FAQ Pro wording, XP-toast sweep/flawless chips (Android doesn't compute those bonuses). **Build note:** `Icons.Filled.Schedule` is an extension property needing an explicit import even when fully-qualified; a stale Kotlin incremental cache produced a spurious cascade of "unresolved" errors → `rm -rf app/build/kotlin` + rebuild cleared it. **Remaining Android: #80 VS 2-player test + deferred sub-pieces, #85 Home Pro/Unlimited, #75 full mode playthrough, Google Play Billing.***

*2026-06-08 update (Android parity sweep, part 4 — #88 XP pipeline + toast + onboarding, XP path verified live): Android previously NEVER recorded games (the Profile charts were fed only by web/iOS plays of the same account) — closed that gap. New **GameResultsService.kt** ports iOS GameResultsService 1:1: on a LIVE finish it writes a `matches` history row (powers the charts), upserts the `user_stats` aggregate (wins/losses/games/best/avg/fastest), and updates `profiles` progression — XP (+100 win / +25 loss, +50 win-streak>1, +50 daily), level (xp/1000+1), win-streak, daily-login-streak (+7-day shield grant) — returning an XpResult. Gated in GameScreen via `rememberSaveable recorded` + `LaunchedEffect(isFinished)` so it fires exactly once and skips resumed games (deltas aren't idempotent). daily_results stays in DailyResultsService (PostGameScreen, idempotent best-score) — no double-write. Daily Sweep/Flawless bonus deferred (Android has no MedalService; server-cron + iOS only). New **XpToast.kt** ports web xp-toast.tsx/iOS XpToastView: purple-gradient pill, gold star, "+N XP", streak/daily chips, level-up line, slides in from top, 3s auto-dismiss; rendered over the victory/post-game overlays. New **WelcomeScreen.kt** ports iOS WelcomeView/web WelcomeModal (gradient accent bar, WORDOCIOUS wordmark, 3 pillars Daily Puzzles/Compete Head-to-Head/Leaderboards & Medals, username picker 3-20 validation, Get Started/Skip both set has_onboarded=true); added `has_onboarded` (default true) to Profile + gated as a cover in MainActivity when `hasOnboarded == false`. **Verified live on emulator:** played Classic daily to a loss → profile XP-to-next 375→300 (=+75: 25 base + 50 daily), win-rate 67%→50% (total_losses 1→2), streak stayed 0, daily-login recomputed to 1 (last play >1d ago) — confirms the full progression write; then played Six daily → caught the **XP toast** ("+75 XP · +50 daily") over GAME OVER. Onboarding cover is built/wired/compiles but NOT force-shown live (would require flipping the real account's has_onboarded — left untouched). **Remaining Android: #80 VS multiplayer, #85 Home Pro/Unlimited, + Google Play Billing for the Pro CTAs.***

*2026-06-08 update (Android parity sweep, part 3 — #89 full profile dashboard, verified live): completed the per-mode profile dashboard 1:1 with iOS ProfileCharts/MatchStatsService. **MatchStatsService** extended (ports stats-service.ts helpers): `solveTimes`, `timeOfDay` (LOCAL hour via Instant→ZoneId.systemDefault), `topWords`, `proInsights` (+ sub-fetchers `wordInsights` nemesis/lucky/avgGuesses/firstTryRate, `modeWinStreak`, `peakHour`, `headToHead`); all existing fetchers gained an optional `mode` filter. **ProfileScreen** now has a horizontal **per-mode picker** (All + 9 mode chips with game counts, iOS ProfileModePicker) that re-scopes every chart reactively, plus new cards: **Solve Time** (Canvas line+area+dashed-avg, Fastest/Average/Slowest), **When You Play** (24h opacity heatmap + 12a/6a/12p/6p/12a axis + peak label), **Top Words** (rank/letter-tiles/bar/count/win%), and a Pro-gated card that swaps **Pro Insights** (per-mode: stat-cell grid + nemesis(ic_skull)/VS/trend rows) for the **All** view's **Pro Stats** (Win-Rate-by-Mode + Avg-Solve-Time-by-Mode bars from user_stats; added `average_time` to ProfileService.UserStat) — both show a frosted 🔒 'Upgrade to Pro' teaser for free users. postgrest-kt 3.0.2 DSL note: IS-NOT-NULL uses `filterNot(col, FilterOperator.IS, null)` (no `negate` arg on `exact`). Verified live on emulator: All-view shows GHOST 3x/66%, peak 4pm, Fastest 2:05/Avg 4:21/Slowest 6:38, Pro Stats locked; tapping Classic re-scopes all charts (guess dist → single '5' bucket, '1 game'). **Remaining Android: #80 VS multiplayer, #85 Home Pro/Unlimited, #88 onboarding + XP pipeline, + Google Play Billing.***

*2026-06-05 update (Android parity sweep, part 2 — menus + profile dashboard, all verified live): **(#86) New screens** — HelpScreen (3 tabs How-to-Play/Game-Modes/FAQ, purple→pink→amber accent bar, WEARY/PILLS/VAGUE colored examples, 10-mode list, 9 verbatim FAQ entries), InfoScreen (About/Privacy/Terms/Support verbatim; '10 modes' About vs '9' Help kept; Terms billing adds 'or Google Play (Android)'), ProScreen (gold crown, 8 verbatim benefit cards, Monthly $6.99/Yearly $59.99 BEST VALUE/$1 Day Pass, Google Play disclosure — billing CTAs display-only until Google Play Billing lands). Wired: header '?'→Help, Settings About/Help/Privacy/Terms LinkRows (were dead)→info pages, Profile 'Go Pro'→Pro, all via an `infoRoute` overlay in MainScreen. **(#87) Profile dashboard** — 70-item Achievements catalog (AchievementService 1:1 from web/iOS, reads unlocked keys from `achievements` table, collapsible '{n}/70' 3-col grid: ✓ purple unlocked / ? gray locked); Guess Distribution + Last-7-Days charts (new MatchStatsService — VERIFIED vs web that solo games write GUESS COUNT into matches.player1_score, so min(score,6) bucketing is correct); avatar IMAGE via Coil (io.coil-kt:coil-compose:2.7.0, falls back to initial); Edit Profile (username 3-20 validation + 6 social links + avatar upload to public `avatars` bucket <uid>/avatar.jpg resized 256² JPEG upsert → profiles.avatar_url, like web/iOS). **Home**: Word of the Day now shows the definition (DefinitionService) + part-of-speech pill; daily countdown switched to LOCAL midnight (the /daily countdown stays UTC to match web). **Remaining Android: #80 VS multiplayer (dedicated pass), #85 Home Pro/Unlimited (needs Pro free-limit/VS/invite infra), #88 onboarding + XP toast + XP pipeline, #89 extra profile charts (SolveTime/WhenYouPlay/TopWords/Pro Insights), + Google Play Billing for the Pro CTAs.***

*2026-06-05 update (Android parity sweep — audit-then-build per surface, all verified live on Pixel 7): **(#81) In-game feedback** — invalid guess (non-word/already-guessed) turns the current row red (#FEF2F2/#F87171/#EF4444) + ±4px sin shake 0.4s; empty tiles now WHITE fill (were transparent); colorblind swap (correct→orange/present→blue) on tiles+keyboard; Reduced-Motion gates flip + shake. **(#82) Leaderboard+Records correctness** — CRITICAL: daily now keys off device-LOCAL date (was UTC, diverged from web/iOS); removed `completed=true` filter that HID all losers; added created_at ASC tiebreak. Leaderboard rebuilt to web /daily (Crown/Medal vector icons, "You're ranked #N of M", "{n} players today", row detail "{g} Guesses · m s · bs/tb · hints" + Win/Loss pill, "(you)" gold suffix, Yesterday's Winners). Records All-Time rebuilt: Hall of Fame + By Game Mode (per-type icons + formatted values "N golds"/"Level N"/"N dailies"/"Ns"/"N guesses"/"N games" + me-highlight). **(#83) Post-game rebuild + CRITICAL composite-score fix** — new shared `DailyScoring` (1:1 web MODE_SCORE_CONFIG/iOS DailyScoring); the old Android formula was WRONG (timeBonus ÷5 instead of full seconds; flat guessWeight 50 not per-mode 100/90/80; single-board completion bonus omitted; wrong timeCaps/maxGuesses) → every Android daily score mis-ranked vs web/iOS. Post-game UI now ports iOS SolvedPuzzleView: gradient mode-title FinishedStatsHeader (stat row + green/red summary + Home/Share links, NO colored banner), DailyRankBadge "Top N% · #r of t", board reveal, fixed ScoreBreakdown (Win/Guess/Time/Completion bonus + Hint penalty, correct labels/gating), DefinitionCard "No definition available" fallback. Verified: Classic post-game 1200 pts (1000+0+200 completion). **(#84) Profile rebuild** — 96dp gradient avatar, level-tier pill (Diamond/Platinum/Gold/Silver/Bronze), amber XP bar ((xp%1000)/10% + "N XP to next"), Member-since, Go Pro, Today's Dailies 5+4 badge grid (W/L + Sweep/Flawless), Global Summary 4 icon cards (Wins/Win Rate/Streak/Daily), Daily Medals (gold/silver/bronze). Added created_at + medal cols to Profile. Deferred to #87: dashboard charts, 72-item Achievements, Edit Profile, avatar image (Coil). **Remaining Android: #85 Home Pro/Unlimited + modals, #86 new screens (Help/Info/Pro/Onboarding/XP toast), #80 VS multiplayer, #87 profile dashboard.***

*2026-06-05 update (latest+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++): **Native Android planning + exhaustive parity audit + late-session gauntlet/PN/leaderboard fixes.** (1) **Android decided: native Kotlin + Jetpack Compose**, engine **hand-ported to Kotlin** (NOT KMP — leaned KMP, then the ShowLoud-Claude critique surfaced the decisive point: parity comes from the shared JSON fixtures, not code-sharing; KMP's complexity tax isn't worth it for a solo learner on a stable engine; KMP consolidation is an optional future refactor). Solo build (assistant drives engine-port/Gradle, owner drives Compose — Compose ≈ SwiftUI). Plan in `WORDOCIOUS_ANDROID_BUILD_PLAN.md`; **complete build-to spec in `WORDOCIOUS_ANDROID_PARITY_SPEC.md`** (6-agent parallel audit of the iOS app → every mode/screen/animation/completed-state/socket-event/Supabase-column, with a master "rework hot-spots" checklist so Android skips the web→iOS rework). Decisions: RevenueCat lean for billing, Apple-login-on-Android for account continuity (later phase), all 3 clients share Supabase + socket + seed/scoring math. (1b) **Android engine port COMPLETE (Phase 1 done):** pure-Kotlin/JVM `core` module (`apps/android/`, package `com.wordocious.core`, Gradle wrapper pinned to 8.8, Kotlin 2.0.20 + kotlinx-serialization) — ported Seed (JS-i32 `simpleHash`, daily/match seeds, per-length solution generation), Dictionary (+ ordered allowed-word pool for prefill), Evaluator (two-pass), Scoring (`calculateScore`), Prefill, the full Types state model (GameMode/BoardState/GauntletProgress/GameState/GameAction sealed interface/gauntletStages), and the **Reducer state machine** (`createInitialState` + `gameReducer` for all 9 modes incl. gauntlet stage progression/blackout/steal — immutable data classes + `.copy()`, `System.currentTimeMillis()` for wall-clock). **Parity locked by the SAME 12 JSON fixtures as Swift** — 10 tests GREEN (Seed×3, Evaluator, Scoring, Prefill, Reducer×4 behavior). (1c) **Android Phase 2 STARTED — Compose app module compiles:** installed the **headless Android SDK** myself via cmdline-tools (`~/Library/Android/sdk`, platform-tools + android-34 + build-tools 34.0.0, ~3-4GB, no emulator, no Studio/sudo — owner installs Android Studio later for live UI work). Scaffolded the `:app` Gradle module (AGP 8.5.2 + Compose BOM 2024.09.00 + Kotlin compose-plugin, minSdk 26/target 34, `android.useAndroidX=true`), `AndroidManifest` + adaptive launcher icon (anydpi-v26 placeholder) + Material no-action-bar theme, **`WTheme` Compose tokens ported 1:1 from globals.css :root + iOS Theme.swift** (bg/surface/text/border, tile green-500/yellow-500, key 600-weights, purple→pink wordmark gradient — dark/ocean/forest deferred to the settings pass), and **`GameViewModel`** (StateFlow<GameState> over the pure `:core` reducer — Kotlin analogue of iOS GameViewModel: type/delete/submit/dispatch). `MainActivity` renders a **SMOKE-TEST Classic board** (engine→ViewModel→Compose, real keyboard, win/loss status) — deliberately NOT final UI; the web-faithful screens come in the audit-then-match pass. **`./gradlew build` GREEN → real 16MB debug APK produced.** Nothing about web/iOS touched; `:core` stays pure JVM (Android props only affect `:app`). (1d) **Compose Home screen built (audit-then-match):** read the web `app/page.tsx` (source of truth) + cross-checked iOS HomeView, then ported the Home layout 1:1 — gradient WORDOCIOUS header, **Daily Challenge hero** (violet→indigo gradient, Star icons, "9 puzzles · Leaderboards & medals", live "Resets in HH:MM:SS" midnight-UTC countdown), **Word of the Day** card (computes today's word from the solutions list; definition deferred to the networking layer), **GAME MODES** 2-col grid of all 10 cards (`ModeCatalog.kt` ported from web `MODE_CARDS` — title/desc/accent/engineMode, top accent bar, accent@8% icon box, Roman numerals IV/VIII + 6/7 glyphs), LIVE banner + footer links. Tapping a card opens the engine-backed `GameSmokeScreen` for that mode (single-board renders fully; multi-board shows the active board + a notice — full multi-board UI pending). Minimal Home↔Game nav in MainActivity (4-tab shell + real nav graph come later). Data-driven bits (daily completions/locks, live count, Pro Daily/Unlimited toggle, invites, exact lucide/brand-SVG icons) render faithful shells, wired when the Android Supabase/socket layer lands. `:app:assembleDebug` GREEN. (1e) **Multi-board Compose game screens (audit-then-match from web multi-board.tsx + board.tsx):** `TileView` (orthographic scaleY flip, 150ms stagger per tile, invalid-word red state), `KeyboardView` (QWERTY layout matching web keyboard.tsx, letter-state coloring: keyCorrect green-600/keyPresent yellow-600/keyAbsent gray-400/keyDefault #e8e5f0), `MiniBoardView` (gray card → green-400/green-50 won, red-400/red-50 lost; green ✓ badge; prefill rows @ 75% opacity; tile flip on last submitted row at 80ms stagger), `MultiBoardLayout` (2×2 for QuadWord, 4×2 for OctoWord with tap-to-zoom overlay; Succession sequential locking — boards after currentBoardIndex are hidden), `computeCombinedLetterStates` (merged across all boards, CORRECT>PRESENT>ABSENT priority). `GameScreen` dispatches applyToAll for parallel modes, sequential guess for Succession. `MainActivity` passes the real daily UTC seed (same `generateDailySeed(date, modeName)` as the engine). `clickableNoRipple` extracted to shared `Util.kt`. Smoke-test screen removed — all modes now play on real game screens. APK green at 16MB. Web/iOS untouched. Android Studio is FREE (no license). (1f) **Android Phase 2 layout + nav + post-game fixes (verified live on emulator):** (a) Keyboard: replaced fixed widths with `RowScope.weight()` — keys fill screen width exactly like web keyboard.tsx. (b) Board: `BoxWithConstraints` computes largest board fitting both width AND height (web `max-w-[400px] max-h-full aspect-ratio`), tile font scales with tile size. (c) **4-tab bottom nav** (`MainScreen.kt`, `Scaffold` + `NavigationBar` — Home/Leaderboard/Profile/Records with purple accent, tab bar hidden during game, matching web). (d) **Post-game screen** (`PostGameScreen.kt` from web post-game-summary.tsx + score-breakdown.tsx): green/red gradient header, solution card (UPPERCASE), score breakdown (win 1000 + guess bonus), stats chips, Home + Share buttons. Verified live: CRANE + MINUS → "🎉 Solved! in 2 guesses · 1200 pts". (1g) **Android Supabase data layer + full tab screens + timer/persistence (parity pass):** `SupabaseConfig` (same project/anon key as iOS — eniiqqsxpmuyrspvepiw, ktor-android engine), `AuthService` (StateFlow session/profile/isLoading, email sign-in/up, `isProActive`, email-confirmation-aware sign-up message), `DailyResultsService` (`recordDailyResult` with the web composite-score formula — base 1000 + time bonus floor(under/5) + completion + guess bonus − hint penalty; upserts only on improved score), `LeaderboardService`/`ProfileService`/`DailyCompletionsService`. UI: **AuthScreen** (login gate — wordmark, email+password, sign in/up toggle), **LeaderboardScreen** (mode-picker, countdown, user-rank card, top-50 with 🥇🥈🥉/W-L), **ProfileScreen** (avatar, level/XP bar, stats-by-mode, recent matches, sign-out), **RecordsScreen** (Daily + All-Time tabs, Hall-of-Fame 2×2). MainActivity routes Loading→Auth→MainScreen. **Game timer** (`GameViewModel.elapsed` StateFlow ticks 1/s; header shows M:SS; freezes at finish), **local persistence** (`GamePersistence` SharedPreferences saves/restores full GameState JSON per seed+mode — resume mid-game + completed-daily re-entry shows post-game), post-game score now uses real elapsed time + records to Supabase, Home mode cards show **W/L pills + accent tint + guesses·time** when today's daily is done (`DailyCompletionsService`). **VERIFIED LIVE on Pixel 7 emulator:** auth screen renders; Supabase round-trips (returned real server email-validation error → integration confirmed working); sign-up hits backend (project requires email confirmation — surfaced as "check your email" message, correct web parity). Signed-in screens (leaderboard/profile/home-badges) are code-complete; live-data demo needs a confirmed login (email-confirmation is a backend setting, can't complete on emulator). (1h) **Android signed-in flow VERIFIED LIVE (logged in as AppDevTester, drove Supabase dashboard to confirm schema) — fixed 3 real schema bugs:** (a) `profiles` data class queried `email`/`display_name`/`streak` which DON'T exist — real columns are `current_streak` + `total_wins`/`total_losses` and there's no email/display_name on profiles; the explicit column select hit non-existent columns → query errored → null profile → "Player"/Level1/0XP defaults. Fixed: select `*` (like web), corrected field names, added `ignoreUnknownKeys`/`coerceInputValues` to the Supabase Json serializer. (b) Leaderboard `profiles(username)` join returns a NESTED object — flat `username` field never populated → fixed with `ProfileRef` nested data class + computed `username` getter. (c) `all_time_records` has `holder_id` not `holder_username` → fixed with `profiles!inner(username)` embed (matches web daily-service.ts). **Verified end-to-end on emulator:** Profile shows AppDevTester / 525 XP / 2W-1L / per-mode stats (Classic 1W Best:5, Succession 1W Best:4, Gauntlet 0W-1L Best:12) / recent matches; Leaderboard shows BMT 1481 + (after playing) AppDevTester #2 w/ gold user-rank card; Records Daily + All-Time Hall of Fame (Longest Streak 38, Most Gold 317, Highest Level 111, Most Dailies 266). **Full pipeline verified:** played Classic → live header timer ticked (0:21→1:00) → post-game showed time-based score (1048 = win 1000 + time bonus 48 [240s under 300 cap ÷5]) → recorded to daily_results → Home Classic card flipped to W badge + accent tint + "1 guess · 1m 0s" → appeared on leaderboard #2. Local persistence + 4-tab nav all confirmed working. (1i) **Android post-game polish (all verified live as AppDevTester):** **word definition** (`DefinitionService` fetches dictionaryapi.dev like the web `useWordDefinition` — verified MINUS→"The minus sign (-)." noun, HUDDLE→"A dense and disorderly crowd." noun; hidden for ProperNoundle); **share sheet** (`ShareHelper` builds 🟩🟨⬛ emoji grid + header + ⏱time + wordocious.com, launches Android ACTION_SEND chooser — verified "Wordocious Classic 1/6" grid renders in the system share sheet); **frozen-time fix** (re-opening a FINISHED persisted game was recomputing elapsed from startTime → inflated time/score; now `GamePersistence.saveElapsed/loadElapsed` freezes elapsed-at-finish — verified Six re-entry shows 17s not 7m); **hint-mode guess bonus verified** (Six = +300 guess bonus [6 unused×50], Classic = none — matches the web rule); grammar "1 guess" singular. `all_time_records` holder name now via `profiles!inner(username)` embed (web-matching). (1j) **AESTHETIC PARITY PASS (user: "it still doesn't look like Wordocious" — aesthetic+icons must match identically, now a tracked workstream):** (a) **Nunito brand font** bundled (variable TTF, same as iOS) + applied app-wide via Typography + `LocalTextStyle` override so EVERY Text() uses it — the single biggest fix, app now reads as Wordocious (rounded heavy face) not system default. (b) **Exact icons** — created vector drawables from real lucide paths (Swords/Skull/Crown/Shield/TrendingUp/Star/BookOpen) + custom WordleGrid (5×6 dot grid); replaced all material stand-ins on Home (Classic=WordleGrid, VS=Swords, Gauntlet=Skull, ProperNoundle=Crown, etc.) — match web MODE_CARDS identically. (c) **btn-3d** reusable `Button3D` (solid-offset dark shadow + press-down, matching web `boxShadow:0 4px 0` + translateY) on the Auth Sign In button; **card drop-shadows** (`cardShadow` modifier) on Home mode cards. Compared live against wordocious.com (resized to phone width) for reference. Verified on emulator. NOTE: macOS keeps creating " 2.dex"/" 2.xml" duplicate artifacts in build/ → clean with `find build -name '* 2.*' -delete` if dex/resource-merge fails. (1k) **Per-screen aesthetic audit RE-ANCHORED to `WORDOCIOUS_ANDROID_PARITY_SPEC.md`** (user asked if I'm still using the big parity-audit file — I'd drifted to comparing live-web/web-source directly; now reading the spec's exact values per screen). **Game screen → spec Part 2 (Headers/Nav):** replaced the plain "‹ Classic" with the spec header — **Corner Home button** (44dp circle, surface fill, 2dp accent stroke, house icon, shadow, top-left), **centered gradient mode title** (CLASSIC/QUADWORD/etc, Black 28, exact `ModeStyle.gradient` stops — DUEL #A78BFA→#EC4899, DUEL_6 cyan, DUEL_7 lime, others accent→accent65%), **progress label + live M:SS clock** under title, **vertical gradient bg** #F8F7FF→#F3F0FF. Added modeTitle/modeAccent/modeTitleGradient helpers. Live-verified QuadWord 2×2 multi-board renders correctly. **Remaining aesthetic (per spec):** keyboard key height 52/Nunito-Bold-18/action-keys-54×52, post-game gradient-title + DailyRankBadge treatment, leaderboard/profile/records deep value-match, exact bottom-nav lucide icons, dark/ocean/forest themes. (1l) **Functionality pivot — spec as gospel, combined functional+aesthetic pass per surface (user: fix aesthetic while testing each page for efficiency):** **Hot-spot #12 quadrant keyboard** built (Quad/Octo/Deliverance: per-letter sub-cell grid colored per-board, 2-col ≤4 boards / 4-col octo; `computePerBoardLetterStates`; verified live — E key yellow quadrant, C green quadrant on QuadWord). **Hot-spot #8 Sequence masking** (locked future boards dimmed 0.6 + committed rows masked `•`, active board `#FACC15` yellow border, keyboard from active board only — verified live). **Multi-board tile-overlap bug fixed** (tiles forced aspectRatio square inside fixed-height rows → overlap; now `TileView square:Boolean`, multi-board uses non-square fill per spec). **8/9 modes LIVE-VERIFIED end-to-end on emulator:** Classic/Six/Seven (gradient title, corner-home, timer, definition, share), QuadWord (2×2 + quadrant kbd), Succession (active highlight + dim/mask), Deliverance (3 prefill rows RUCHE/PATEL/MADAM + quadrant + any-loss), OctoWord (4×2 + tap-to-zoom dim overlay), Gauntlet (stage 0 renders, amber title). Game-screen header redone to spec (corner Home 44dp circle + centered gradient mode title Black 28 + progress/clock + gradient bg); keyboard to spec (52dp keys, Bold 18, 7/5 spacing). **KNOWN REMAINING:** ProperNoundle = SEPARATE engine NOT ported (currently picks a 5-letter word — wrong; needs variable-length answers, category round-robin epoch-2024 selection, Wikipedia hints — hot-spot #19); Gauntlet 5-node stepper header (have "Stage 1/5" text); victory overlay+confetti; settings screen; dark/ocean/forest themes; Google OAuth; VS multiplayer (socket.io). Core daily/leaderboard/profile/records loop + 8 modes fully working + verified live. (1m) **"All of it" pass — Gauntlet stepper + victory overlay + shared header + Settings + themes (spec+iOS+web as gospel):** **Gauntlet 5-node stepper** (done=green✓/active=purple-glow/future=number + connectors). **Victory overlay + confetti** (two-phase finish hot-spot #4: live win → dim+confetti card "VICTORY!" gradient/solution/stats/tap-to-continue → stats; resumed skips; Gauntlet excluded; verified ELEMENT win). **PROCESS CORRECTION (user flagged):** I wrongly put the theme picker on Profile — must check wordocious.com + native iOS + spec for placement, never decide solo. Re-read iOS `AppHeaderView`+`SettingsView`: built **shared `AppHeader`** (wordmark+PRO+Help+Settings-gear+streak-pill+shield-pill) rendered by MainScreen on EVERY tab (removed per-screen wordmarks; Leaderboard→"DAILY CHALLENGE", Records→"RECORDS" titles); **`SettingsScreen` 1:1 iOS** (THEME radio cards/Sound/Notifications/Accessibility/About/Sign-out/v1.0.0) opened from the gear; **WTheme palette-driven** (mutableStateOf, 4 globals.css palettes) — Dark/Ocean/Forest live-recolor verified (whole app→dark). Profile +daily_login_streak/streak_shields. (1n) **HINTS + ProperNoundle DONE (user flagged missing hints + Wikipedia clue — fixed via audit-first):** lesson learned — read the FULL iOS implementation (engine+view+helper) BEFORE building, not after, so gaps don't surface live. **Hints** now on all 3 hint modes: GameViewModel.revealVowel/revealConsonant (reveal random un-guessed vowel/consonant as a HINT_USED board row, '—' if exhausted, 1:1 iOS); Six/Seven = 2 accent pills (💡 Vowel/Consonant → "Vowel: X"), ProperNoundle = 3 capsule pills (Clue #9333EA / Vowel #2563EB eye / Consonant #16A34A #, exact iOS colors). **ProperNoundle engine** (`core/ProperNoundle.kt`, separate from reducer): loads 670-entry puzzles JSON, normalize/evaluate/wordGroups, daily category round-robin (alpha-sorted, epoch 2024-01-01 UTC), FNV-1a 64-bit VS seeds; reducer uses the real variable-length answer + skips dict validation for names; 5 tests green. **Wikipedia Clue** (`WikipediaHint.kt`, 1:1 port): REST summary fetch + abbreviation-dot protection + first-2-sentences + name/word redaction (______); rendered italic-centered in header. **VERIFIED LIVE:** today's PN = Solar System; Clue fetched the redacted summary ("The ______ is the gravitationally bound ______ of the Sun..."); 11-tile board plays. **ONE LARGE ITEM REMAINS: (#80) VS multiplayer** (socket.io client + full Part-6 real-time protocol: presence u:userId, matchmaking, seed, invites, in-match relay, match_ended player1-writer, rematch Pro, daily limit) — the biggest single feature, deserves a focused dedicated pass. Plus smaller: colorblind/reduced-motion behavior wiring, Help/About info pages, onboarding, Pro/billing, ads. (2) **Late-session iOS fixes (ride next build):** leaderboard gauntlet Completed-Today card was missing the ScoreBreakdown (added); gauntlet re-entry showed a generic flat board grid when stage data absent → added `GauntletReconstruct` (deterministic replay of recorded guesses through the reducer) + gated the generic grid off gauntlet everywhere; added the corner Home button to the gauntlet results + ProperNoundle completed screens (web parity — web renders GameHomeButton on both); extracted a shared animated `GauntletResultsView` so the **in-game gauntlet finish (win OR loss) shows the proper animated results screen** instead of GameScreen's generic finished layout. All build-green, uncommitted-to-build-8-successor.*

*2026-06-04 update (latest++++++++++++++++++++++++++++++++++++++++++++++++++++++++++): **BUILD 8 cut, uploaded & being swapped into App Review (replaces build 7).** After live sim co-testing, cut build 8 (CURRENT_PROJECT_VERSION 8) batching everything since build 7: keyboard layout (Delete left/Enter right), live invalid-word red row + shake + "Already guessed", tile-flip reveal + winning-word-flips-before-results + victory/confetti-over-dimmed-board, full-screen game presentation (no tab bar on game/victory screens), Gauntlet active-stage glow, Recent-Matches aesthetic, completed-daily card mode-leak fix, AND the **multi-board redesign** (below). Same signing flow as 6/7: unsigned archive (`CODE_SIGNING_ALLOWED=NO`) → cloud `-exportArchive` (app-store) → **re-sign with applesignin** (cloud export strips it; `codesign -f -s "Apple Distribution: …(Q32F6GRDYG)" --entitlements <full incl. com.apple.developer.applesignin> --preserve-metadata=identifier,flags,runtime`) → repackage IPA → altool upload (Delivery UUID 07c953a1). **MULTI-BOARD REDESIGN (this session, QuadWord/OctoWord/Succession/Deliverance + Gauntlet stages; single-board untouched):** (a) **fill the screen** — web uses `1fr` rows so tiles stretch to non-square cells filling full width+height; native now matches (`TileView` optional `height`, `BoardView` fill mode w/ `tileHeight`+2px gaps, `BoardLayout` fills width AND height, 8px between boards) — fixed the big margins (esp. OctoWord, which is height-driven). (b) **per-board gray card frame** (`#E5E7EB`/white; green won/red lost) — was missing. (c) **OctoWord tap-to-zoom** — tap a board → uniform scale-from-1× morph anchored to that board's exact slot up to a centered, still-playable view + dim backdrop; tap to minimize back into the slot. matchedGeometryEffect was tried first but flew in from the top (reads source frame wrong across `.overlay`); replaced with deterministic scale+offset from the analytic slot rect (like web `getSourceTransform`). Non-uniform scale "squeezed" → switched to uniform; "started tiny" → render the enlarged board with the SAME tile layout as the mini and scale from 1× so it starts/ends at the board's real size. (d) **flip = `scaleEffect(y: cos)` squash** (= orthographic rotateX, cheaper than rotation3D). NOTE: user finds the multi-board flip **jittery on the SIMULATOR** (many tiles at once); single-board is smooth. Tried whole-row-as-one-flip (user disliked losing the stagger) → reverted to per-tile stagger. Strong hypothesis: simulator software-renderer artifact — **verify on device via build 8** before degrading the animation. **NEXT:** build 8 finishes ASC processing → swap into the review submission (remove build 7), submit, **manual release**. User TestFlight-tests build 8 on device — judge the multi-board flip smoothness for real + confetti.*

*2026-06-03 update (latest+++++++++++++++++++++++++++++++++++++++++++++++++++++++++): **Reveal/victory/nav animation polish — live sim co-testing session (all native, UNCOMMITTED, rides build 8; web edits pending push).** Iterated on game "feel" with the user watching the simulator. (1) **Tile flip reveal** — first tried a 0→180° face-swap (looked wrong / "down-to-up"); user noted web Classic doesn't flip on normal guesses but **Succession does it correctly**. Audited web: the flip is `rotateX 0°→90°→0°` (flip to edge-on and back, color applied throughout — NOT a face swap) via the `tile-flip`/`tile-flip-mini` keyframes (0.5s/150ms full, 0.3s/80ms mini). The web was INCONSISTENT — `board.tsx` (Classic/Six/Seven) flipped only the **winning** row (`isCorrect===true`) and `multi-board.tsx` (Quad/Octo/Rescue/Deliverance) only when the board **isWon**, while Succession/Gauntlet/ProperNoundle flip **every** guess. **WEB FIX (deploys on push):** relaxed `board.tsx` → `animate = isLastRow && !!evaluation` and `multi-board.tsx` `lastSubmittedRow` → every submitted row, so all modes flip every guess. **NATIVE:** added `FlipRevealTile` + `TileFlip` (orthographic squash, `perspective:0` — the 3D perspective was the "flicker"; `rotation3DEffect` axis-X, progress→0/90/0 triangle), gated to the freshly-committed last row (BoardView `seenGuessCount`, suppresses resume re-flips), mini vs full timing by `vm.isMultiBoard`. (2) **Winning word now animates before results** — `vm.isFinished` used to instantly swap the in-play board → finished/stats layout, killing the final flip. Added `revealComplete` gate: hold the in-play board until the flip plays (`revealDuration` = dur + (wordLen-1)*stagger), THEN show the victory card. (3) **Smoother victory + confetti** — victory card + confetti now spring in OVER the dimmed in-play board (`.scale(0.92)+.opacity`, spring response 0.5); the heavier finished/stats `ScrollView` is built only on "tap to continue" (`onDismiss` sets `revealComplete`), so it no longer competes with the 40-piece confetti for frames (was the choppiness cause). Confetti `Canvas`/`TimelineView` rewrite deferred — user to judge tomorrow. (4) **Bottom nav off all game screens** — the tab bar was bleeding onto the board/victory screen because games were PUSHED inside the Home tab (nav mounted behind). Root cause of the regression was the fragile `ChromeVisibility` enter/exit COUNTER drifting on unpaired appear/disappear; rewrote it to a per-screen **UUID Set** (isolated, can't be un-hidden by a stray exit), AND — the real fix — routed both GameScreen entry points (daily + unlimited) through `.fullScreenCover(item:$pendingGame)` so games present full-screen OVER the tab bar like the web's game route (added `if vm.isFinished { revealComplete=true }` onAppear guard for resumed-finished games). (5) **Gauntlet active-stage glow** — added `StageGlow` pulsing purple halo (#A855F7, shadow 3px↔7px+12px, 2.5s ease-in-out loop) on the active stage node, matching web `gauntlet-glow`; cleared=green✓, upcoming=flat grey. All reduce-motion-aware. **Cross-device mid-game persistence: TABLED** (user asked, then deferred — would need a `daily_progress` Supabase table + replay-reconstruction per mode; free in $, moderate effort + per-mode correctness risk; not standard, Wordle is per-device too). Reviewer demo account confirmed in ASC: `bterchin+appdevtester@gmail.com`. **NEXT AM:** user judges confetti smoothness (maybe Canvas rewrite); then commit native + the 2 web flip-gate edits together; build 8 batches all of the above + the prior build-8 queue (keyboard/invalid-row/shake/dup/Recent-Matches).*

*2026-06-03 update (latest++++++++++++++++++++++++++++++++++++++++++++++++++++++++): **Cross-game in-play UX parity audit + fixes (post build-7 testing).** User compared native Seven vs web and flagged: Enter key on the wrong side, no live red on invalid words, and questioned cross-version progress persistence. Ran a full web↔native in-game UX audit (2 agents, all modes). Fixed (shared KeyboardView/BoardView/GameViewModel → applies to ALL board modes): (1) **keyboard layout** — native had ENTER-left/Delete-right; swapped to web's Delete-left/ENTER-right. (2) **live invalid row** — full-length word not in dictionary OR already guessed now turns the row red BEFORE Enter (TileView.isInvalid, red-400/50/500), matching web isInvalidWord. (3) **shake** on rejected guess (ShakeEffect ±4px ~0.4s, web animate-shake). (4) **"Already guessed"** now blocks duplicates (was "Not in word list"). The toast was already at parity (white on #1a1a2e, top 90px). **PERSISTENCE CLARIFICATION:** in-progress daily time/guesses are stored PER-DEVICE on both platforms (web localStorage `wordocious-session-{mode}-daily`, native GamePersistence `wordocious-{mode}-{seed}.json` + elapsed in UserDefaults) — they do NOT sync native↔web (same as web↔web across browsers); only the COMPLETED daily result syncs via `daily_results`. Working as designed, not a bug. **KNOWN REMAINING GAP (not yet done):** web flips each committed tile on reveal (rotateX, 0.5s, 150ms stagger; mini 0.3s/80ms) — native reveals instantly. Deferred (riskier SwiftUI animation; want on-device visual verification before shipping). All fixes build green; ride the next native build (8).*

*2026-06-03 update (latest+++++++++++++++++++++++++++++++++++++++++++++++++++++++): **BUILD 7 cut + submitting for review; all 3 native logins confirmed.** TestFlight build 6 on a real device confirmed **Apple sign-in now works** (the applesignin re-sign fix) — so Google + Apple + email are all functional natively. Then a final parity polish: the native **Recent Matches** row was rebuilt to match wordocious.com exactly — the mode's own icon in its accent-tinted box (`ModeIconView` via `homeModes` lookup by dbKey) instead of a generic win/loss check; proper-case titles (Classic/Six/OctoWord/…); a colored **Solo/VS pill**; an **"N guesses · time"** subtitle (added `player1_score`/`player2_score` to the native RecentMatch model + query); and **"Win/Loss" + "MMM d · h:mm a"** on the right. Shared row → updates the own-profile tab AND public profiles. Cut **build 7** (CURRENT_PROJECT_VERSION 7): unsigned archive → cloud-sign export → **re-sign with applesignin entitlement** (the now-standard flow — `CODE_SIGNING_ALLOWED=NO` archives drop capability entitlements) → uploaded via altool (UUID 173adb3b). **DONE — build 7 SUBMITTED to App Review:** removed build 4 from review, deleted it from the 1.0 version, attached build 7, confirmed "Manually release this version", added to a review submission and Submitted → version now **"Waiting for Review"** with build 7 (manual release). Build 4 is off the submission. Reminder: native auth needs Supabase Google "Skip nonce checks" ON (GoogleSignIn 7.x); `ResilientAuthStorage` for the sim keychain; ITSAppUsesNonExemptEncryption=false skips the export-compliance prompt.*

*2026-06-03 update (latest++++++++++++++++++++++++++++++++++++++++++++++++++++++): **BUILD 6 — all 3 native logins working; fixed Sign in with Apple entitlement.** TestFlight testing of build 5 on a real device: **Google ✓** (after enabling Supabase Google provider's **"Skip nonce checks"** — GoogleSignIn 7.x issues a nonce we can't echo; see [[reference_native_google_signin]]), email ✓, but **Apple failed with `ASAuthorizationError 1000`**. Root cause: archiving with `CODE_SIGNING_ALLOWED=NO` strips the `.entitlements`, and the cloud-sign export can't add an entitlement absent from the archive — so `com.apple.developer.applesignin` was missing from the signed binary on builds 2–5 (Apple never actually worked). **Build 6 fix:** unsigned archive → cloud-sign export → **re-sign the .app with the full entitlements (incl. applesignin) pulled from the embedded App Store profile**, using the local Apple Distribution cert (`E834629E…`, matches the profile). Verified applesignin embedded + signature valid; uploaded via altool (UUID 672c191e). Also bumped CURRENT_PROJECT_VERSION 6 and (earlier) made Info.plist pull `$(CURRENT_PROJECT_VERSION)`/`$(MARKETING_VERSION)` + declared `ITSAppUsesNonExemptEncryption=false`. **BUILD-PROCESS GOTCHA (durable):** the `CODE_SIGNING_ALLOWED=NO` archive → cloud-export flow drops capability entitlements (applesignin). Either re-sign post-export (as done) or install the Wordocious App Store profile + manual distribution signing in the archive. **NEXT:** user TestFlight-tests build 6 — confirm **Apple** now works (+ re-confirm Google/email + the VS/Recent-Matches/completed-card screens); then swap build 6 into App Review (build 4 still sits there). Build 5 also remains on TF (Google/email good, Apple broken).*

*2026-06-03 update (latest+++++++++++++++++++++++++++++++++++++++++++++++++++++): **BUILD 5 uploaded to App Store Connect** (CURRENT_PROJECT_VERSION 5, 1.0.0; archived → cloud-signed → altool, Delivery UUID ee76d143 → processing → TestFlight Internal Testers). Bundles everything since build 4: (a) **VS parity** — fixed rematch handshake (Accept re-emits `offer_rematch`; server has no `accept_rematch`), daily-VS leaderboard write (`DailyResultsService.recordVs`), post-match XP toast, **opponent live tile mini-board** (ports opponent-mini-board.tsx), **VS → Recent Matches** (client-writes the matches row; server `match_ended` now sends `opponentId`+`recordMatch`) incl. a Recent Matches section on the native OWN profile (shared `RecentMatchRow`), and the **VS home-card greys out** after the daily VS (was never locked). (b) **Completed-daily card** (web leaderboard) now reads recorded `daily_results` values so time/guesses/score match the leaderboard row, fixed-px boards (crisp/uniform on Octo), checkmark clipping. (c) **server.wordocious.com** realtime URL (Railway custom domain) in native VSConfig + Vercel. (d) **NATIVE GOOGLE SIGN-IN REWRITTEN**: was Supabase web OAuth (ASWebAuthenticationSession + PKCE) which returned an empty auth code on iOS; now uses the **GoogleSignIn SDK → `signInWithIdToken`** (same as Apple, matches ShowLoud). Needs: a Google Cloud **iOS** OAuth client for `com.wordocious.app` (created — `193086095286-4ftdf92g8cnsur8kd7odqq0jueqq5oog`), web client as `serverClientID` (`…lris79d9`), reversed-client-ID URL scheme in Info.plist, and the iOS client added to the **Supabase Google provider's Client IDs** list (done). OAuth consent screen renamed Spellstrike→Wordocious. (e) **Resilient auth storage** (`ResilientAuthStorage`): Keychain first (device, secure) → UserDefaults fallback when Keychain is entitlement-less (unsigned simulator), fixing the sim "keychain error". **SIM CAVEAT:** email + Apple now work on the simulator, but **Google can't fully complete on the unsigned sim** — GoogleSignIn's OWN internal Keychain save throws there; it works on signed builds (device/TestFlight), so Google is verified via build 5 on a real device. Fixed a latent build-number bug: GENERATE_INFOPLIST_FILE=NO + xcodegen hardcoding CFBundleVersion="1" → Info.plist now uses `$(CURRENT_PROJECT_VERSION)`/`$(MARKETING_VERSION)`. **NEXT:** user TestFlight-tests build 5 on device (all 3 logins end-to-end + the VS/card/Recent-Matches screens); after it's good, decide whether to swap build 5 into App Review (build 4 still sits there).*

*2026-06-03 update (latest++++++++++++++++++++++++++++++++++++++++++++++++++++): **VS mode parity audit + fixes; VS now in Recent Matches.** Ran an end-to-end web↔native↔server VS audit (3 parallel mapping agents + manual verification). The socket protocol/state-machine/opponent-HUD/scoring/invites/9-modes all matched; found + fixed THREE native-only gaps: (1) **rematch was broken** — server pairs on two `offer_rematch` (no `accept_rematch` handler), but native's Accept emitted `accept_rematch` → silently dropped; Accept now re-emits `offer_rematch` (web parity); removed dead acceptRematch/VSEvent.acceptRematch. (2) **daily VS leaderboard** — native skipped `daily_results` for play_type='vs'; added `DailyResultsService.recordVs` (+vsCompositeScore, 1:1 with web recordDailyVsResult) wired into `record()`. (3) **post-match XP toast** — native discarded the XpResult; now captures + shows XpToastView on the VS result screen. THEN added **VS → Recent Matches** on BOTH platforms (was missing everywhere; `recordMatch` had no callers): client-writes design (no service-role key on the public realtime server). Server's `match_ended` now carries `opponentId` (derived from the opponent socket's presenceId `u:<id>`) + `recordMatch` (true only for player1 = single designated writer → one shared row, RLS-safe). Web vs-game + native VSMatchViewModel insert the `matches` row (player2_id set, player2_guesses null — counts only relayed). Server `tsc` also fixed (dangling QueueEntry import; runtime was fine via tsx). All build/typecheck green. NOTE: these ride **build 5** (native, not yet cut) + the server/web redeploy from this push; build 4 (in review) predates them. THEN closed the loop: added a **Recent Matches** section to the native OWN profile tab (ProfileTab) — web's own profile has it; native previously only had it on the public-profile view. Extracted a shared `RecentMatchRow` so both render identically (up to 5 rows, solo + VS, VS labelled "VS Match"). Verified VS stat writes are fully consistent web↔native — `user_stats(vs)` (identical best/avg/fastest, both store SECONDS), profiles XP/streak/level/7-day-shield (identical), `daily_results(vs)` composite, and the `matches` row. The earlier "web ms vs native seconds" worry was a MISREAD: web's recordGameResult param is `timeMs` and divides by 1000 internally, so both persist seconds — NO fix needed (dividing again would've been a 1000× bug).*

*2026-06-03 update (latest++++++++++++++++++++++++++++++++++++++++++++++++++): **Realtime server is LIVE again on a durable custom domain.** The socket.io matchmaking/presence server (`apps/server`) had been down (Railway trial expired; Render was never actually deployed), which broke VS + the LIVE-players presence count on both web and native. Redeployed `apps/server` on **Railway** (project glorious-creation/production, service WordRush, GitHub `BTBTBTBTBTBTBT/WordRush@main`, build `pnpm install`, start `cd apps/server && npx tsx src/index.ts`, target port 8080; auto-deploys on push to main). Verified `/presence` returns `{"online":N}`. To never rewire clients on a future redeploy, added a stable **custom domain `server.wordocious.com`**: a Railway custom domain (CNAME `server` → `0s8i2qg6.up.railway.app` + TXT `_railway-verify.server` on the **GoDaddy** zone for wordocious.com — DNS is GoDaddy, NOT Vercel; apex A `216.198.79.1` points at Vercel). Both records verified live on the authoritative NS. Native `VSConfig.serverURL` updated `wordocious-server.onrender.com` → **`https://server.wordocious.com`** (committed; rides the next native build). TODO once Railway finishes issuing the Let's Encrypt cert for the custom domain: set Vercel `NEXT_PUBLIC_SERVER_URL=https://server.wordocious.com` + redeploy web. Cost: Railway ~$5 Hobby usage-based (more cost-effective than Render $7 Starter for this RAM-driven, CPU-light socket server). NOTE: match state is in-memory/single-instance — horizontal scaling later needs a socket.io Redis adapter.*

*2026-06-03 update (latest+++++++++++++++++++++++++++++++++++++++++++++++): **Post-submit hardening (Tier C) + Monetization §5 now documents ads.** While build 4 is in review: (1) **§5 Monetization rewritten** to cover BOTH channels — the Pro subscription AND ads (AdMob iOS / AdSense web, `pub-3015627373086578`, UMP/ATT flow, app-ads.txt, payment-profile TODO); it was subscriptions-only before. (2) **#4 — server-side Pro entitlement (fail-closed, NOT yet enforced):** closes the `is_pro` client-self-grant hole. New web webhook `app/api/appstore/notifications/route.ts` verifies Apple **App Store Server Notifications V2** via `@apple/app-store-server-library@3.1.0` (added with `pnpm add` — repo is a **pnpm** monorepo, npm breaks on husky) and writes is_pro/pro_expires_at via service-role; native `StoreManager` now sets `appAccountToken = user UUID` so the webhook can map the transaction; **deferred** migration `20260603000004_lock_pro_columns.sql` (a trigger that blocks client writes to those columns) is run ONLY after sandbox-testing. Route fails closed (503) until Apple root certs + env + ASC webhook URL are set up, so the live purchase flow is unchanged. `next build` verified green before push. Setup steps in `apps/web/app/api/appstore/README.md`. (3) **#5 profile charts — already done** (full `ProfileDashboard` in ProfileCharts.swift wired at ProfileTab:73; the "deferred" note was stale). (4) **#6 persistence hygiene:** `GamePersistence.cleanupStaleDailyGames()` at launch purges past-date daily saves + their elapsed/hint keys; unlimited resume now only restores in-progress games <24h old. All committed/pushed.*

*2026-06-03 update (latest++++++++++++++++++++++++++++++++++++++++++++++): **BUILD 4 submitted to App Review** — bundled the entire parity batch (Gauntlet header/transition/stage-review, Six/Seven/ProperNoundle hints + scoring, Wikipedia clue, quadrant keyboard, Sequence masking, ProperNoundle post-game, medals + sweep/flawless XP, home icon box) + AdMob (real ad unit IDs + GDPR/UMP consent via UserMessagingPlatform 2.7.0) + web app-ads.txt. Bumped to build 4, archived/exported (cloud-sign)/uploaded via altool, cleared export compliance, removed build 3 from review, swapped build 4 onto the 1.0 version, resubmitted → "1.0 (4) Waiting for Review" (submission f98ff6a9). Build 4 auto-distributes to the TestFlight Internal Testers group. Manual release still set (nothing goes public until the user releases). Wave-2 parity audit (visual/edge/persistence/timezone/copy) found NO functional bugs — mostly false positives + platform idioms; one cosmetic fix (icon box). Tip: poll ASC build processing via a Ruby ES256 JWT to GET /v1/builds (script /tmp/asc_build_check.rb). Remaining: user TestFlight-tests build 4 (round-trip vs web), then manual release after approval; AdMob payment profile + app-ads.txt verification happen post-launch.*

*2026-06-03 update (latest+++++++++++++++++++++++++++++++++++++++++++++): **Adversarial parity sweep → Tiers 1-3 fixed (queued for build 4).** After the hint-buttons miss, ran a 5-agent adversarial web-vs-native audit (each agent owns a surface, assumes nothing is at parity). Found real gaps + ~30% false positives (so verify each in both codebases before coding). Fixed: **T1a** per-board QUADRANT keyboard for QuadWord/OctoWord/Deliverance (Sequence excluded); **T1b** Sequence locked-board masking (dim + bullets) + input gating + active-board-only keyboard (engine applyToAll already matched web — pure presentation); **T1c** ProperNoundle post-game (rank badge, ScoreBreakdownView, Share, VictoryOverlay; header timer+clue were already present — false positives); **T1d** Gauntlet auto-advancing StageTransition overlay (replaced manual Continue) + solution-word reveal in the stage review (already expandable — false positive); **T2a** new MedalService — streak(7/30/100) + perfect-game medals + Daily Sweep(+200)/Flawless(+400) XP (daily TOP-3 podium medals already awarded by the server cron api/cron/daily-medals). **T3 (web, deployed):** "8"→"9 modes" copy + removed the broken Facebook button (provider not enabled). All native items committed + build-clean, NOT yet uploaded — batching into build 4. Scoring configs/formula confirmed byte-for-byte at parity.*

*2026-06-03 update (latest++++++++++++++++++++++++++++++++++++++++++++): **"Simulate Pro" dev toggle gated behind is_admin (build 3).** Loose-end audit found the **"Simulate Pro"/"Disable Pro" button was ungated** on BOTH web (`app/profile/page.tsx`, live) and native (`ProfileTab.swift`, in submitted build 2) — visible to every user, granting free Pro by flipping `is_pro` client-side → App Review rejection risk + revenue leak. User wants to KEEP it for testing, so instead of removing: added `is_admin` boolean to `profiles` (migration `20260603000003_add_is_admin_to_profiles.sql`, flags `bterchin@gmail.com`) and gated the button on it (web `{profile.is_admin && …}`; native `if auth.profile?.isAdmin == true`, added `isAdmin: Bool?` + `is_admin` to Profile model/selectColumns). Now renders only for the developer's account; reviewers (AppDevTester) + real users never see it. **USER MUST RUN the migration** (else the button is hidden for everyone incl. dev, and the native build-3 profile fetch — which now selects `is_admin` — would 400 until the column exists). Built/uploaded **build 3**; bumped CURRENT_PROJECT_VERSION 2→3. **DONE: build 3 resubmitted to App Review** — cleared build-3 export compliance, "removed version from review" (App Review page link, since a build can't be swapped while Waiting for Review), deleted build 2 + added build 3 on the version, re-Added-for-Review, opened the draft via the "Today at …" button on /reviewsubmissions and clicked Submit → "1 Item Submitted", submission shows "1.0 (3) — App Version — Waiting for Review". Subscriptions stayed "Waiting for Review" independently (so the version page's IAP attach section was empty — nothing in Ready-to-Submit to attach). User flagged is_admin (ran migration), so the gated button now shows only for them. Note: `is_pro` is client-writable by design here (button + StoreKit fulfillment both client-write) — server-side receipt validation is a separate, larger hardening item, NOT done. Other non-blocking loose ends: web "Continue with Facebook" button but Facebook provider NOT enabled in Supabase (only apple/email/google) → broken; AdMob payment profile + app-ads.txt for revenue; web custom SMTP for email-signup volume.*

*2026-06-03 update (latest+++++++++++++++++++++++++++++++++++++++++++): **iOS v1.0 SUBMITTED to App Review** (build 2, manual release). Finished the App Store submission end-to-end via Chrome. (1) **Real AdMob** wired (replaced Google test IDs): App ID `ca-app-pub-3015627373086578~8393761846`, banner `…/4287985559`, rewarded-interstitial `…/6909445311` (AdsManager.swift + project.yml); bumped build 1→2, re-archived/exported (cloud-sign)/uploaded via altool, cleared export compliance ("None of the algorithms"). (2) **App Privacy** completed + published: 12 data types — app data (Email, User ID, Purchases, Photos, Gameplay Content, Product Interaction) = linked/App Functionality/no-tracking; AdMob data (Coarse Location, Device ID, Advertising Data, Product Interaction, Crash/Performance/Other Diagnostic) = tracking=Yes/Third-Party Advertising+Analytics; Privacy Policy URL `https://wordocious.com/privacy`. (3) Cleared remaining gates: **Content Rights** (no third-party content), **Category** (Games / Word / Puzzle), **App base price = Free**, App Review demo account (AppDevTester email login) + contact info, age 4+. (4) **Release set to MANUAL** (new STANDING INSTRUCTION — always manual; user TestFlight-tests then releases himself). Submitted as "1 Item" (app + 3 IAPs); ≤48h review. **ASC text-field gotcha:** programmatic value-set doesn't enable Save (Ember/React) — use `document.execCommand('insertText',...)` after focusing; radio/checkbox clicks work via dispatchEvent. **⚠️ OPEN RISK (must fix before reviewer taps it): Sign in with Apple** — the native AuthView shows a `SignInWithAppleButton` (Guideline 4.8, since Google is offered) that calls Supabase `signInWithIdToken(provider:.apple)`, but the **Supabase Apple provider is NOT configured** → the button fails. Fix is SERVER-SIDE (Supabase Auth → Providers → Apple → enable + add Client ID `com.wordocious.app`), which fixes the in-review build with NO resubmit.*

*2026-06-03 update (latest++++++++++++++++++++++++++++++++++++++++++): **Auth fixes + cross-account play-limit leak (web).** Two sign-up/login bugs surfaced while creating the App Review demo account. (1) **Email signup RLS** — `auth.signUp()` then a client `profiles` insert failed "new row violates row-level security policy" because with email-confirmation ON there's no session yet (still `anon`). Fixed with a SECURITY DEFINER trigger `handle_new_user()` on `auth.users` that creates the profile from signup metadata (migration `20260603000002_auto_create_profile_on_signup.sql`, USER APPLIED — also backfilled orphaned auth users incl. `bterchin+appdevtester`); both clients now pass `{data:{username}}` and no longer insert the row. (2) **"email rate limit exceeded"** — Supabase's built-in confirmation-email cap (~3-4/hr) burned by repeated attempts; not a code bug. Unblock for the demo account: `update auth.users set email_confirmed_at = now() where email = '…' and email_confirmed_at is null;` then sign IN (no new email). Launch note: keep Google/Facebook primary or add custom SMTP for real email-signup volume. (3) **Cross-account daily-lock leak (web only)** — a brand-new account showed every mode "Play again in …" (all locked). Root cause: `play-limit-service.ts` keyed its localStorage cache by DATE ONLY (`wordocious-plays-<date>`), so a prior account's completions on the same browser leaked in (and `syncPlayLimits` only MERGED DB rows into that stale cache). Fix: scope every key per user (`wordocious-plays-<uid>-<date>`) via a persisted `activeUserId` (module var hydrated from localStorage for correct first-render), new `setActivePlayUser()` called from the home `[user]` effect + inside `syncPlayLimits`, and `cleanupOldPlayData` now purges the legacy un-scoped keys. **Native is NOT affected** — its `isLocked` reads DB daily-completions for the current user (`completions.byMode[key]`), no device-level cache. Web tsc clean. **Also (this session): export-compliance answered ("None of the algorithms" — HTTPS-only) so the uploaded build flipped Missing Compliance → Ready to Submit.***

*2026-06-03 update (latest+++++++++++++++++++++++++++++++++++++++++): **iOS App Store Connect onboarded — app record + IAPs + FIRST BUILD UPLOADED.** Drove ASC end-to-end (via Chrome) + the local toolchain. (1) **App created**: Wordocious, App ID `6775966055`, bundle `com.wordocious.app` (App ID already had Sign in with Apple + In-App Purchase capabilities). (2) **IAPs created, priced ($6.99 / $59.99 / $0.99), localized**: subscription group "Wordocious Pro" + `pro_monthly` (auto-renew 1mo) + `pro_yearly` (auto-renew 1yr) + `pro_day` (Consumable) — product IDs match StoreManager.swift exactly. (3) **Build uploaded**: archived unsigned → exported app-store IPA via Xcode account cloud-signing → `xcrun altool` validate+upload using the ASC API key **C8FRS9T697** (the "ShowLoud build upload" key; Issuer ID `8bdd3f73-0d8b-427d-95c7-8097b77dfb7a`, read from ASC Users&Access→Integrations; the other key N3Q8F7JJ87 is the hobby account). Validation reject (iPad family + portrait-only) → fixed via **TARGETED_DEVICE_FAMILY=1 (iPhone-only)**, re-archived → `UPLOAD SUCCEEDED`. (4) **App Store screenshots** captured at the required 6.9" size (1320×2868) on an iPhone 17 Pro Max sim via `simctl io screenshot` (Home, solved board, leaderboard, records, profile, Pro), saved to ~/Desktop/wordocious_screenshots. Used "Simulate Pro" for ad-free shots then REVERTED it (account left clean/free). **Signing facts for next time:** Apple Distribution cert present (Q32F6GRDYG); account-based cloud signing works for export — the API key lacks create-profile permission ("Cloud signing permission error" if used for provisioning), so omit `-authenticationKey*` on export (rely on the logged-in Xcode account) and use the key only for altool upload. Also fixed native Pro page "8"→"9 game modes" (to match web). **Remaining for submission:** upload the screenshots + IAP review screenshots to the listing, fill listing metadata (description/keywords/support+privacy URLs/age rating), submit once the build finishes processing. **Unchanged blocker:** Apple OAuth (SIWA) still needs the Supabase provider enabled (dashboard).*

*2026-06-03 update (latest++++++++++++++++++++++++++++++++++++++++): **Parity sweep — 4 web→native gaps closed (account deletion, avatars, daily reminder, VS-limit modal).** A full web-vs-native feature audit found most things at parity; built the 4 real gaps. (1) **Account deletion** (App Store 5.1.1(v) requirement): Settings → Delete Account → confirm alert → `AuthService.deleteAccount()` POSTs the user's access token to the EXISTING web `/api/account/delete` (the native anon client can't delete the auth user; the web route holds the service-role key and does the cascade), then signs out. (2) **Avatars**: native previously ignored `avatar_url` everywhere (initials only). New `AvatarView` (renders `avatar_url` via AsyncImage, else gradient initial) on Profile, PublicProfile, Edit Profile; `AvatarUploader` resizes to 256² JPEG → uploads to the existing `avatars` bucket at `<uid>/avatar.jpg` (lowercased uid for the RLS folder policy); Edit Profile gains a PhotosPicker "Change Photo" that writes `profiles.avatar_url` + refreshes. (3) **Daily reminder**: `NotificationService` schedules a repeating local notification (`UNUserNotificationCenter`, 6pm local — no APNs/server, the native counterpart to the web push cron) with a Settings → Notifications → Daily Reminder toggle (requests permission; reverts + alert on denial). (4) **VS-limit modal**: `DailyResultsService.hasPlayedDailyVS()` (daily_results DUEL/vs today) gates the free daily VS; when used, the lobby shows a native `VSLimitModal` (swords, "Daily VS Used", local-midnight countdown, Go Pro) mirroring `vs-limit-modal.tsx`. Build clean; verified live (Settings Delete-Account row + Daily-Reminder toggle, Edit-Profile photo picker, avatar fallback). No new migration (reuses avatars bucket). **Not gaps (web-only by nature):** PWA install prompt, cookie/consent, admin tools, service worker. **Remaining known launch blocker (unchanged):** Apple OAuth not yet configured server-side in Supabase (SIWA).*

*2026-06-03 update (latest+++++++++++++++++++++++++++++++++++++++): **Native social share parity + a stale-install lesson.** (1) iOS Share now shows the puzzle on Facebook/X too: `ShareService` uploads the rendered PNG to the shared `share-images` bucket (path `<uid>/<ShareMode>-<date>.png`, uid LOWERCASED to satisfy the storage RLS folder policy) and includes the per-result `wordocious.com/s/…` URL in the iOS share sheet alongside the image. Messages/WhatsApp still get the file directly; Facebook/X scrape the URL's og:image (the same PNG). Verified live: share sheet shows the rich "Solved 4/6 · 0:27 · wordocious.com" preview, PNG confirmed uploaded (93.8 KB, public HTTP 200). supabase-swift 2.46 Storage `upload(_:data:options:FileOptions(contentType:upsert:))`. (2) **Lesson — stale-install false alarm:** the user reported the home header "regressed" (PRO badge, settings gear, and Daily/Unlimited toggle gone; completed dailies opening new puzzles; "old footer"). Root cause was NOT code — there are TWO DerivedData dirs for the project (`dnddssmb…` current, `dthbydbn…` from Jun 1), and the install step's `find … | head -1` is non-deterministic and had been installing YESTERDAY'S build. Confirmed via `xcrun simctl get_app_container` (installed binary mtime Jun 1 14:04 vs source/build Jun 2 13:09). Fix: install from `xcodebuild -showBuildSettings → TARGET_BUILD_DIR` (authoritative), never `find|head`. After reinstalling the correct build, ALL of it was present and correct (PRO badge, gear, toggle above the Flawless banner, and completed-daily→solved-review routing). **Going forward, always resolve the install path via TARGET_BUILD_DIR.***

*2026-06-03 update (latest++++++++++++++++++++++++++++++++++++++): **Social share — Facebook/X now show the actual puzzle image (per-result Open Graph).** Sharing a result to Facebook/X previously showed only a generic wordocious.com link card, because those platforms refuse pre-attached image files (Web Share API `files`) and only scrape `og:image` from the shared URL — a hard platform constraint, not a fixable bug in the share button. Fix: the Share button now uploads the EXACT PNG it already generates (`generateShareImage`) to a new public `share-images` Storage bucket, and shares a per-result URL `wordocious.com/s/<uid>/<mode>-<date>?…stats` instead of the bare site URL. New route `app/s/[...key]/page.tsx` emits `openGraph`/`twitter:card` meta whose `og:image` IS that uploaded PNG (overriding the root file-convention OG image for that route), plus a human-facing landing (image + Play CTA). Messages/WhatsApp/Mail still receive the attached file directly (best quality); only the URL-scraping platforms needed this. Implementation chose upload-the-client-PNG over server re-render (`@napi-rs/canvas` not installed, Next 13.5, only Nunito-Black font on hand → re-render wouldn't be pixel-exact); uploading the real bytes guarantees the identical image. One object per (user, mode, day) with upsert (bounded growth); result stats baked into the query so distinct results are distinct URLs (no stale FB scrape cache). `shareResult` signature unchanged (uid fetched internally); falls back to the bare site URL if not signed in / upload fails. **Migration `20260603000001_create_share_images_bucket.sql` (public bucket + authed-write/public-read policies, mirrors avatars) — USER CONFIRMED APPLIED; bucket verified public via REST.** Also fixed stale mode counts (9 daily modes now): the brand OG card `og-image-renderer.tsx` "7 game modes"→"9", welcome modal "7"→"9", Pro page "all 8 game modes"→"all 9" (×2). (`/about` + `/how-to-play` left at "10" — they enumerate all 10 incl. VS Battle.) `next build` clean (`λ /s/[...key]` in manifest, 47 pages); pushed. Native share already image-only to the iOS sheet (same platform constraint applies there); a native per-result-URL share could be a future parity follow-up.*

*2026-06-02 update (latest+++++++++++++++++++++++++++++++++++++): **Records — "Most Games Played" & "Most Gold Medals" were missing (web AND native); root-caused + fixed.** Both record types showed blank on the all-time Records page. Diagnosed against prod via the anon REST API: `all_time_records` had **1,441 rows** against PostgREST's **1000-row cap**, because GLOBAL records (game_mode/play_type = NULL) were duplicated into the hundreds (`highest_level` 496, `most_daily_completions` 475, `most_gold_medals` 430 — each should be 1). Cause: a plain `UNIQUE(record_type, game_mode, play_type)` treats NULLs as distinct, so the app's `upsert onConflict(...)` never matched a NULL-keyed row and **inserted a fresh duplicate every game**; `checkAndUpdateRecord`'s `.maybeSingle()` also errored on the many rows so it always thought the record was new. Ordered alphabetically by `record_type`, the 996 duplicate rows pushed the two alphabetically-last types (`most_games_played`, `most_gold_medals`) entirely past row 1000, so `fetchAllTimeRecords` never returned them. **Fix:** (1) rewrote `checkAndUpdateRecord` (`lib/daily-service.ts`) to be self-healing — selects ALL rows for the key (best-first), updates the canonical row, and DELETES duplicates on every write, with no reliance on NULL `onConflict`; (2) migration `20260602000002_dedupe_all_time_records.sql` collapses existing dupes (keeping the true holder per key: MIN for time/guesses, MAX otherwise) and adds a `NULLS NOT DISTINCT` unique index so global records can never re-duplicate. Native only READS `all_time_records` (it never wrote records), so it's fixed automatically once the table is clean. Even without running the migration, the deployed write fix self-heals within one game (the next game collapses `highest_level`'s dupes, dropping the table under 1000). Web tsc clean; pushed. **ACTION: run the migration in Supabase for instant cleanup + the protective index.***

*2026-06-02 update (latest++++++++++++++++++++++++++++++++++++): **iOS "Do 1-6" — six native engagement/parity gaps closed.** Built the remaining native ports of web features the iOS app was missing, each committed + pushed + (where possible) verified live in the sim. (1) **Victory / game-over overlay** (`PostGameEffects.VictoryOverlay` + pure-SwiftUI `ConfettiView`) — confetti, gradient "VICTORY!"/"GAME OVER", solution word(s) + definition, stats, tap-to-dismiss; fires on finish for non-Gauntlet modes. (2) **XP / level-up toast** (`XpToastView`) — purple gradient pill "+N XP" with streak/daily chips + "Level up! Lv.N", auto-dismiss 3s, driven by a new `GameResultsService.XpResult` returned up through `record()`. (3) **Incoming VS invites banner** on Home (`InviteService.fetchPending`/`inviterUsername`/`decline` + `pendingInvitesBanner`) — tap to accept→`VSGameView(inviteCode:)`. (4) **Contextual pro-prompt banner** on Home (shown once when a free user hits a 7-day login streak). (5) **Streak-shield "at risk" modal** (`StreakShieldModal` + `ShieldService`, ports `StreakShieldProvider`+`shield-service.ts`) — on app open, if `daily_login_streak>0` and the streak is at risk (>20h since last play AND local day rolled over), offer to spend a shield (bumps `last_played_at` to preserve it) or Let Streak Reset (→0); added `last_played_at` to the `Profile` model + select. (6) **Public player profiles** (`PublicProfileView` + `PublicProfileService`, ports `app/profile/[id]/page.tsx`) — tapping any leaderboard row pushes a read-only profile: gradient avatar/username, Level badge + XP bar, 4 overall-stat cards, Solo/VS toggle + mode picker + per-mode stats card, Top Words, and last-10 Recent Matches. **Verified live**: leaderboard → BMT profile renders end-to-end (Level 101, 342 wins/88%, QuadWord 39W/7L/Best 4/37s, top words GHOST/PLACE/DRINK, recent matches GAUNTLET/CLASSIC SEVEN/…). Builds clean; all six pushed to main.*

*2026-06-02 update (latest+++++++++++++++++++++++++++++++++++): **iOS Gauntlet results polish — Home/Share to top + total-time fix.** (1) Home/Share moved to the TOP (under the title) on both the native Gauntlet results and the web `GauntletResults` (web's bottom button row removed; now a compact text-link row + DailyRankBadge under the title) — matching the other completed screens. (2) Fixed the top "Time" showing a stale "6s" cross-device: the native derived it from `elapsedMs` (local `loadElapsed` / matches-row total), which is absent/stale for a run played on another device. Now the total time = **sum of the per-stage `timeMs`** from `stageResults` (the authoritative cross-device data: 9+36+94+152+234 = 525s = 8m 45s), with `elapsedMs` only as a fallback when stages carry no recorded times — applied to the Time stat card, the score-breakdown time bonus, `GauntletCompletedView`, and the leaderboard `CompletedDailyCard` summary. Verified live: GAUNTLET CLEARED! shows Time 8m 45s, 2,475 pts, and the full stage breakdown — matching wordocious.com. (Also: today's pre-migration run was backfilled to the server via the web revisit, confirming the cross-device round-trip works end-to-end.)*

*2026-06-02 update (latest++++++++++++++++++++++++++++++++++): **`matches.gauntlet_stages` migration applied (prod) + web backfill.** The user ran the migration in Supabase, so writes/reads are now live. Added a web backfill: whenever the daily Gauntlet results render from a LOCAL session (incl. a restored completed save), `gauntlet-game` pushes `{stages, stageResults}` to the server via the idempotent `recordGauntletStages` — so runs finished BEFORE the column existed become viewable cross-device by simply revisiting them once on the device that played them (no replay needed). Verify: revisit today's gauntlet on the web (its localStorage backfills the server row), then open it on the native sim — the full stage breakdown with per-stage times should appear. Future runs persist automatically at finish. Web tsc clean; pushed.*

*2026-06-02 update (latest+++++++++++++++++++++++++++++++++): **Gauntlet cross-device parity completed — web now READS `gauntlet_stages` too.** Finished the symmetry from the prior update: a Gauntlet played on one device shows its full results on the other, both directions. New `stats-service.fetchGauntletStages(userId, seed)` reads `matches.gauntlet_stages` (best-effort). The web `/gauntlet` page (`gauntlet-game`) now, when a daily has no local session, fetches the server breakdown and renders `GauntletResults` from it (`showPlayAgain=false`, and a new `recordOnMount=false` guard on `GauntletResults` so a cross-device revisit doesn't re-count the run in local gauntlet-stats); the leaderboard "Completed Today" card (`completed-daily-board`) gets the same fallback. Both reads are purely additive (only fire when no local session) so the on-device flow is unchanged. With native already reading + both clients writing, the rich Gauntlet results (incl. per-stage times) now render identically on web AND native regardless of where the run was played — once the `gauntlet_stages` migration is applied and a gauntlet is played after. Web tsc clean; committed + pushed.*

*2026-06-02 update (latest++++++++++++++++++++++++++++++++): **Gauntlet per-stage breakdown persisted server-side (cross-device parity).** The Gauntlet results' per-stage data (name, status, guesses, **per-stage timeMs**, and each stage's final `boardsSnapshot`) previously lived ONLY in the playing device's local storage; the server (`matches`) only had the run total — so a cross-device revisit couldn't reproduce it (per-stage time can't be split out of one total). Fix: new column **`matches.gauntlet_stages` (jsonb)** = `{ stages, stageResults }`, written by both clients as a SEPARATE best-effort update after the match insert (`GameResultsService.recordGauntletStages` / web `recordGauntletStages`) — best-effort so a not-yet-applied column never breaks recording (social_links lesson). The JSON decodes 1:1 on both platforms (shapes verified: `GauntletStageConfig/Result`, `BoardState`, `GuessResult`, `GameStatus` all identical). **Native reads** it (`MatchStatsService.gauntletStages(seed:)`) as a fallback in `SolvedPuzzleView` + leaderboard `CompletedDailyCard` when there's no local session, rebuilding a `GauntletProgress` so the rich stage breakdown shows cross-device. **Migration `20260602000001_add_matches_gauntlet_stages.sql` must be run in the Supabase SQL editor** (`alter table public.matches add column if not exists gauntlet_stages jsonb;`) for writes to persist — until then it no-ops harmlessly and cross-device gauntlet detail stays off. Web tsc clean; native builds; committed + pushed. **Remaining symmetric piece (offered, not yet done):** a server-read fallback on the WEB (so a native-played gauntlet shows the rich screen on web) — the web still reads its own localStorage on revisit, which covers the device-where-played (the common case).*

*2026-06-02 update (latest+++++++++++++++++++++++++++++++): **Gauntlet results — web drops all-time stats; native rebuilt to match.** (WEB `gauntlet-results.tsx`) Removed the All-Time Stats card (that data lives in Records); the composite Score Breakdown stays. (NATIVE `SolvedPuzzleView` gauntlet branch) Rebuilt to mirror the web `GauntletResults`: amber trophy + "GAUNTLET CLEARED!" gradient title (XCircle + "GAUNTLET FAILED" on loss), three boxed stat cards (Stages X/5 · Guesses · Time), `DailyRankBadge`, the shared `ScoreBreakdownView` for GAUNTLET (1000 + time + completion → matches the web's 2,475), a "STAGE BREAKDOWN" section (expandable per-stage mini-boards via `GauntletCompletedView(showSummary:false, showStageHeader:true)`), and bottom Share/Home buttons (corner Home hidden on this screen to avoid a duplicate). **Device-local caveat (same on web):** the rich stage-by-stage view needs the local saved `GameState.gauntlet` (per-stage `boardsSnapshot`) — exactly like the web reads its own `localStorage`. A gauntlet played on another device falls back to the generic board-grid reconstruction (the synced `matches` row has the 21 flat solutions but no per-stage breakdown/times). So on the sim a web-played gauntlet shows the fallback; playing the gauntlet natively shows the rich screen. Web tsc clean; native builds; committed + pushed.*

*2026-06-02 update (latest++++++++++++++++++++++++++++++): **FAQ now mentions Achievements (web + native).** Achievements existed (70 of them, shown on the profile) but were never surfaced in any help text, so players didn't know to chase them. Added an "Are there achievements?" item to the in-app FAQ (identical in `help-modal.tsx` + native `HelpView.swift`) and an "Are there achievements to earn?" item to the standalone web `/faq` page: 70 achievements across beginner/consistency/skill/social/collection, unlock automatically, full set (with progress) shown on the profile. Count + categories verified against `achievement-service.ts` (70 defs), which the native `AchievementService` mirrors 1:1 and the profile renders as an X/70 grid. Native FAQ verified live; web tsc clean; committed + pushed.*

*2026-06-02 update (latest+++++++++++++++++++++++++++++): **FAQ XP section now covers Daily Sweep + Flawless Victory (web + native).** Added to "How do XP and levels work?" (identical in `help-modal.tsx` + `HelpView.swift`) and the web `/how-to-play` XP list: play all 9 of the day's puzzles for a **Daily Sweep (+200 XP)**, win every one for a **Flawless Victory (+400 XP more, 600 total)** — to nudge players to clear every board daily. Values verified against `daily-service.ts` (`DAILY_SWEEP_XP=200`, `FLAWLESS_EXTRA_XP=400`, `DAILY_MODE_COUNT=9`). Native builds, web tsc clean; committed + pushed.*

*2026-06-02 update (latest++++++++++++++++++++++++++++): **FAQ / How-to-Play scoring rewritten for accuracy + hints (web AND native, identical).** The FAQ's "How are scores calculated?" was wrong — it described a general guess bonus and a 1,687-pt Classic example, but the engine (`DailyScoring.swift` ↔ `lib/daily-service.ts`) only awards a guess bonus to the hint modes (Six/Seven/ProperNoundle); a real Classic score is base 1,000 + speed (timeCap − seconds) + completion (≤200, scaled by boards solved), e.g. a 27s solve = 1,000 + 273 + 200 = 1,473 (matches the in-app Score Breakdown). Rewrote the FAQ with identical copy in `apps/web/components/modals/help-modal.tsx` and `apps/ios/.../HelpView.swift`, and fixed the web `/how-to-play` Scoring System section the same way: corrected the components + example, and added **hints** — each revealed hint subtracts 120 (ProperNoundle) / 150 (Six, Seven), floored at zero, other modes unaffected. Also made the section more robust: a new "Do hints affect my score?" item, a new "How do medals work?" (top-3 daily podium + streak/perfect milestones), and enriched streak (local-midnight reset), Streak-Shield (auto on first miss, header count), and PRO (ad-free + unlimited replays + Unlimited mode + Pro Insights + VS invites/rematches) answers. Native FAQ verified live; web tsc clean. Committed + pushed (Vercel auto-deploys the web copy).*

*2026-06-02 update (latest+++++++++++++++++++++++++++): **iOS home bottom section — LIVE players bar + Pro Invite + Sign Out + footer links (web parity).** Ported the web home-page bottom that native was missing. (1) **LIVE banner**: green pulsing dot + "LIVE" + connected-player count — new `LivePlayerCount` poller mirrors `useLivePlayerCount` (polls `VSConfig.serverURL/presence` every 10s, nil→"Players online" until first success, else "N players online"; the VS server is currently down so it shows "Players online"). (2) **Invite** button (Pro-only, pink gradient) opens a new `InviteSheet` porting the web `InviteModal`: 9-mode picker + Share Link / By Username tabs. Share Link → `InviteService.createInvite` → `https://wordocious.com/vs/join/<code>` with Copy/Share; By Username → targeted invite (extended `InviteService.createInvite(gameMode:inviteeUsername:)` to resolve the profile id with "User not found"/"can't invite yourself" errors and store `invitee_id` for the recipient's pending badge — 1:1 with the web `createInvite`). (3) **Sign Out** + an **About · How to Play · Privacy · Terms** footer routing to the existing `InfoPage`/`HelpView` — the same destinations Settings uses, so that content is a single source of truth and stays aligned with the web. **Verified live in the sim**: LIVE bar + Invite render for Pro, invite link generates (`wordocious.com/vs/join/D6D8PW4T`), Sign Out + footer present, About pushes with a working back button. Committed + pushed.*

*2026-06-02 update (latest++++++++++++++++++++++++++): **iOS single-board completed screens now populate the word + definition (Classic/Six/Seven), matching wordocious.com.** They showed no definition: `SolvedPuzzleView` gated it on the matches-row `d.solutions` (`d.solutions.count == 1` / `d.solutions[0]`), which could be empty/mismatched even though the board rendered fine from the local session. Fix: base the definition on the actual displayed board's solution (`displayBoards.first`), and rework `DefinitionCard` to mirror the web "Solution + Definition" block — the word in bold, then phonetic + part-of-speech pill + definition, or "No definition available for this word." when the dictionary has no entry (same `dictionaryapi.dev` source the web uses) so it always populates instead of a blank gap. The live `GameScreen` finish passes `showWord:false` (the full board already spells it). Verified live: Classic "CUTIE" → /ˈkjuːti/ · NOUN · "A cute person or animal." Six/Seven use the identical single-board path. Committed + pushed.*

*2026-06-02 update (latest+++++++++++++++++++++++++): **iOS Share on every completed screen + image-only share + Daily Rank badge confirmed + Gauntlet completed card.** (Gauntlet) Built a native `GauntletCompletedView` porting the web `GauntletCompletedCard`: 3-stat summary (Stages X/5 · Guesses · Time) + per-stage rows (✓/✗ badge, stage name, `Ng · time`, chevron) that expand to that stage's mini-boards from `boardsSnapshot` (1/2/4-col, 140/240/320 caps). Wired into `SolvedPuzzleView` (full-screen, with a GAUNTLET title) and the leaderboard `CompletedDailyCard` (summary label `cleared/5 · Ng · time` + stage rows when expanded), driven by the locally-persisted `GauntletProgress`. (Share) The live post-game already had a faithful Share; added it to `SolvedPuzzleView` for every layout (standard via `FinishedStatsHeader` onShare; Home|Share row on the Gauntlet branch), building the share `Kind` from displayed boards/stages and routing through the same `ShareService` so the image is identical (added a web-parity `boardToGrid` converter). Made share **image-only** — passing the wordocious.com URL alongside made iOS render a dominant OG/link card that hid the image (the image already has the footer). **Verified the rendered PNG** (written to the sandbox + inspected): matches `lib/share-image.ts` pixel-for-pixel — WORDOCIOUS gradient wordmark, mode accent, `4/6 · 0:27 · Jun 2` + green Win pill, green-framed color-only tile grid, footer. (Rank) `DailyRankBadge` "🏆 Top 1% · #1 of 2" (gold ≥75th pct) confirmed showing on the completed screen — the earlier miss was just the async rank `.task` not finished when first screenshotted. All committed + pushed.*

*2026-06-02 update (latest++++++++++++++++++++++++): **WEB fix — completed daily boards intermittently blew up & clipped on the leaderboard.** On wordocious.com's COMPLETED TODAY card (`completed-daily-board.tsx` `CompletedMiniBoard`), the first row of mini boards sometimes rendered oversized and clipped on the right while the rest stayed small, and a re-render (tab blur→focus) could flip a correct layout into the broken one. Root cause: CSS-grid blowout — grid items default to `min-width:auto` and the tiles use `aspectRatio:1`, so on some layout passes a tile sized from its stretched row height and overflowed its column, dragging the board past its cell. Fix: `min-w-0`/`min-h-0` at every grid level + `minmax(0,1fr)` columns and `minmax(0,auto)` rows so width stays authoritative and tiles shrink to their cell — deterministic now. Also covers Gauntlet's completed stage boards (reuse `CompletedMiniBoard`); ProperNoundle's mini board uses explicit pixel widths and was unaffected. Web typecheck clean; pushed to main (Vercel auto-deploy). (NOTE: this WEB bug is distinct from the native completed-board work below — the user was viewing wordocious.com.)*

*2026-06-02 update (latest+++++++++++++++++++++++): **iOS Succession (and all-mode) completed-board rendering fixed + audited.** The solved/completed views (`SolvedPuzzleView` + leaderboard `CompletedDailyCard`) reconstructed each board by replaying ONE shared flat guess list against every solution. Correct for Quordle/Octordle (all boards receive the same guesses simultaneously) but WRONG for **Succession** (`SEQUENCE`), which is played one board at a time — so a late-solved board replayed the whole list and ballooned to ~14 ragged, mismatched rows. Fix: (a) **prefer the local saved per-board `GameState`** (`GamePersistence.load(seed,mode)`), which is correct for every mode and present whenever this device played the daily; (b) when absent (cross-device, e.g. the daily was played on the web), reconstruct **mode-aware** via a new `CompletedBoardReconstruct` — SEQUENCE splits the flat list sequentially (advance to the next board when a guess matches the current solution), all other multi modes keep shared-guess semantics. Both views now render through one shared `CompletedMiniBoardView`, padded to the largest board's capacity for a uniform grid, framed green/red with a ✓ badge (single board unframed, like the web). **Audited every mode's completed screen vs wordocious.com and verified live in the sim**: Classic (single, unframed ✓), QuadWord (2×2 shared), OctoWord (4×2 shared ✓), Succession (2×2 sequential — each board its own 4/5/6/7-guess run ending on its solution ✓); Deliverance/Six/Seven flow through the same shared/single paths (not completable today to screenshot); ProperNoundle keeps its own restore path (`ProperNoundleView`). **Known follow-up:** Gauntlet's completed view uses the generic board grid; the web has a dedicated stage-by-stage `GauntletCompletedCard` (needs a completed Gauntlet daily to build+verify).*

*2026-06-02 update (latest++++++++++++++++++++++): **iOS daily/completed/unlimited + Word-of-the-Day fixes (all verified live in the sim).** Five issues the user hit on the running build. (1) **Word of the Day was stale** — Home showed yesterday's word (Scram) while web showed today's (Drift). The day-index math (`floor(ms/86400000)`) and the bundled `solutions.json` are byte-identical to the web, so the word was correct — the bug was caching: `WordOfTheDayView` used `.task { if info == nil }`, which fetches once and never refreshes; the Home tab stays alive in the TabView, so across the UTC-day rollover it kept the prior word. Now re-fetches keyed on the UTC day index (+ scenePhase so foregrounding re-checks). (2) **Completed daily boards didn't fit one screen** — OctoWord clipped on the leaderboard and the solved views were oversized/scrolly. Ported the web `completed-daily-board.tsx` sizing into a shared `CompletedBoardLayout`: a width-capped grid (4 cols + min(320pt) for >4 boards, 2 cols + 240pt for 2-4, 200pt single) with a solved-for responsive tile size so EVERY board fits with no scroll/clip. Applied to `CompletedDailyCard` (leaderboard, now also with filler rows + per-board green/red frame + ✓ badge) and `SolvedPuzzleView`. (3) **Tapping a completed daily replayed it instead of showing the review** — `isLocked` only gated free users, so Pro users fell through to a `NavigationLink` that started a fresh game on the same daily seed. Now a finished daily (Daily mode) routes to the solved-puzzle review for everyone (`solvedMode` → `SolvedPuzzleView`/`ProperNoundleView`); free users keep the upsell modal (which already links to View Solved). (4) **Games weren't full-screen** — the bottom tab nav (a `safeAreaInset` on the TabView) bled onto pushed game/solved screens, stealing the height that crushed boards tiny and cut off the keyboard. Added a shared `ChromeVisibility` + `.hidesBottomNav()` on `GameScreen`/`SolvedPuzzleView`/`ProperNoundleView` so they're full-screen like the web. (5) **OctoWord boards were tiny** — `BoardLayout` used 2 columns for 8 boards (→ 4 rows → fit-to-height crush); the web uses **4 columns** (`grid-cols-4`) for octordle. Native now matches (4 cols >4 boards, 2 for 2-4, 1 single). (6) **Unlimited didn't persist** — it minted a new timestamp seed every tap, so an in-progress puzzle never resumed; now the Unlimited seed resolves at tap time (resume the saved non-daily puzzle while it's still PLAYING, else mint+remember a fresh seed), so an Unlimited puzzle persists until finished and the completed-review path only fires in Daily mode — web parity. **Verified live**: Home shows 'Drift'; the OctoWord COMPLETED TODAY leaderboard card and the tap-through solved review both render all 8 boards in a clean 4×2 grid, full-screen, fully visible, with the score breakdown — no replay, no nav bar.*

*2026-06-01 update (latest+++++++++++++++++++++): **iOS parity batch — 6 functional items + 3 minor polishes (all built clean, pushed to main).** Closed the remaining web-parity gaps found in the last audit. (1) **Unlimited mode records stats** — non-daily (Unlimited) plays now write `user_stats` + a `matches` row (just not `daily_results`), so the Unlimited hero's "All stats count" is true; one-line guard change in `GameViewModel.recordResultIfNeeded` (only the daily-results write stays daily-gated, as before). (2) **Pro Insights depth** — extended `MatchStatsService.proInsights` with parallel sub-fetchers porting the rest of `pro-insights-card.tsx`: `fetchWordInsights` (nemesis word, lucky word, avg guesses, first-try rate), `fetchModeWinStreak` (current/best streak), `peakHourLabel`, `fetchHeadToHeadRecord` (VS W/L/win-rate). `ProInsightsCard` now shows new stat cells (Win Streak, Avg Guesses, First Try Rate, Peak Hour, Lucky Word) + a Nemesis row (≥2 losses) + a VS Record row. (3) **Post-game Daily Rank badge** — ported `daily-rank-badge.tsx` to a `DailyRankBadge` (Top X% · #rank of total, gold when ≥75th pct) shown on the finished `GameScreen` and `SolvedPuzzleView` for daily plays, fetched via `LeaderboardService.userRank`. (4) **Medals on Profile** — new `MedalsService` reads the `medals` table; Profile shows a gold/silver/bronze tally (from `profiles.*_medals`) + a recent-medals list (crown/medal/flame/star icons per `medal_type`). (5) **Edit Profile** — `EditProfileView` sheet (Edit-profile button in the Profile header) edits username (validated) + social links (twitter/instagram/tiktok/threads/discord/website), saved to `profiles`; links render as tappable chips. **`social_links` is fetched via a SEPARATE optional query (`ProfileExtras.socialLinks`) and is NOT in the main profile `select`** — same resilience lesson as the ProperNoundle constraint: a column missing from prod must never break profile loading. Avatar upload deferred. (6) **Onboarding** — `WelcomeView` first-run modal (ports `welcome-modal.tsx`): wordmark + 3 pillars + username field, shown via `fullScreenCover` when `profiles.has_onboarded == false`; Get Started / Skip both set `has_onboarded=true`. **Minors:** (a) Daily/Unlimited choice already persists via `@AppStorage("pref-play-mode")` (String enum) — confirmed, no change needed. (b) **ProperNoundle VS board now shows the Clue/Vowel/Consonant hint row** — extracted a shared `NoundleHints` view reused by the solo screen + `ProperNoundleVSBoard`. (c) **Gauntlet VS stage relay** — `GameViewModel` fires a new `onStageCompleted(stageIndex)` when a stage clears in a VS run; `VSMatchViewModel` emits `stage_completed` and handles `opponent_stage_completed` (the `VSMatchService` plumbing already existed), tracking `opponent.stagesCleared`; the opponent strip now shows the opponent's current Gauntlet stage. **Caveats:** Pro Insights / medals / rank badge depend on real recorded data and are not screenshot-verified end-to-end; VS stage relay needs the (down) socket server to verify live — code-complete, same caveat as all native VS. All changes build clean and are pushed to `main` across 4 commits.*

*2026-06-01 update (latest++++++++++++++++++++): **iOS: fixed a VS join crash (all modes).** Tapping a VS mode (reported on ProperNoundle, but mode-agnostic) hard-crashed in `VSMatchService.decode`: `JSONSerialization.data(withJSONObject:)` *raises an NSException* — not a Swift error, so `try?` cannot catch it — when the socket payload's first element isn't a valid top-level JSON object (a String/Number/NSNull error payload, an ack, etc.), which aborts the app (SIGABRT, the exact crash in the user's report). Added a `JSONSerialization.isValidJSONObject(first)` guard before serializing so an unexpected/malformed payload is skipped instead of crashing. **Verified in the sim**: ProperNoundle VS now reaches the "Pairing… / Position in queue" screen (previously crashed on tap). This was a pre-existing latent bug in the VS socket layer, surfaced now that all modes are tappable in the VS lobby; the fix protects every VS mode.*

*2026-06-01 update (latest+++++++++++++++++++): **iOS: VS for Gauntlet + ProperNoundle (both now populate under VS Battle).** Matches the web, which has dedicated `VsGauntlet` + `VsProperNoundle`. Added `.gauntlet` and `.propernoundle` to the VS lobby `modes` (quick-match + create-private-match grids) and to the Home Unlimited VS-swords overlay. **Gauntlet VS** reuses the shared board path (`GameViewModel(.gauntlet, isVersus:true)` — the core engine handles gauntlet); `VSGameView` now renders the stage-clear **Continue/Finish Gauntlet** button in a match (was keyboard-only, so a cleared stage couldn't advance). **ProperNoundle VS** is bespoke (own engine): `ProperNoundleVM` gained a seed-based init + VS hooks (`onGuessCommitted`/`onCompleted`, `isVersus` skips solo recording); `VSMatchViewModel` drives a `ProperNoundleVM` (new `@Published var proper`) for `.propernoundle` instead of a board `GameViewModel`; `VSGameView` renders a new `ProperNoundleVSBoard` (reuses `NoundleBoard`/`NoundleKeyboard`). Added `ProperNoundle.puzzle(forSeed:)` — a stable FNV-1a hash → list index so both players get the same puzzle (Swift's String.hashValue is per-launch randomized, so a fixed hash was required). **Verified in the sim**: both appear in the VS Battle lobby (quick + private). Live match play still needs the VS socket server (down per VS_SETUP), so this is code-complete but not live-verified — same caveat as all native VS. Builds clean.*

*2026-06-01 update (latest++++++++++++++++++): **iOS Home: Daily Challenge hero fills the Daily slot (no layout shift between Daily/Unlimited).** The Daily mode had no banner where Unlimited shows its `UnlimitedHero`, so toggling shifted the Word-of-the-Day + game-cards grid. Added `DailyChallengeHero` ("★ Daily Challenge ★ / N puzzles · Leaderboards & medals / Resets in HH:MM:SS", violet→indigo gradient, ports the web Daily hero), sized to match `UnlimitedHero` and the Sweep/Flawless `banner` (same py + 3 lines). The Home hero slot is now ALWAYS filled: Unlimited → `UnlimitedHero`; all dailies done → Sweep/Flawless `banner`; otherwise → `DailyChallengeHero`. So switching Daily⇄Unlimited (or completing all dailies) never shifts the cards. The Profile Today's-Dailies card already swaps to Flawless Victory / Daily Sweep when all 9 are complete (unchanged). Verified live in the sim (Daily Challenge banner + countdown shows; same slot/size as the Unlimited hero). Builds clean.*

*2026-06-01 update (latest+++++++++++++++++): **iOS Home Pro parity — Daily/Unlimited toggle + Unlimited hero, web-matching footer, PRO-badge clip fix.** (1) **Daily | Unlimited toggle** (`PlayModeToggle`, Pro-only, ports play-mode-toggle.tsx) + **`UnlimitedHero`** ("∞ Unlimited Play ∞ / Infinite puzzles · All stats count / Tap ⚔ on any game for VS"). In Unlimited mode each standard card launches a **fresh non-daily game** (seed `unlimited-<mode>-<ts>`, no daily gate) and shows a **VS swords overlay** (Pro+Unlimited, excludes VS/Gauntlet/ProperNoundle) that pushes the VS lobby via `navigationDestination` (system back button — first tried a fullScreenCover but it trapped the user with no back). Free users never see the toggle (forced to Daily). (2) **PRO badge was clipped** to an orange sliver next to the wordmark — the header was overflowing; the wordmark now uses `minimumScaleFactor(0.6)` instead of fixed-size so "WORDOCIOUS PRO" + the 4 controls fit one row. (3) **Footer now matches the web `BottomNav`**: replaced the system `UITabBar` with a custom `BottomNav` — outline icon + muted label when inactive, **filled icon + purple label + a 4px dot** when active. The system tab bar is hidden (`.toolbar(.hidden, for: .tabBar)`) and the custom nav is added via `safeAreaInset(.bottom)` with `background(…, ignoresSafeAreaEdges: .bottom)` so it fills the home-indicator zone; `TabView(selection:)` is kept so each tab preserves its state, and the per-tab ad banner still insets above the nav. **Verified live in the sim**: PRO badge fully visible, toggle switches Daily⇄Unlimited, hero + per-card swords render, swords→VS lobby, Unlimited card → fresh game (no ad for Pro), footer active/inactive states. Minor: `@AppStorage` play-mode persistence didn't survive a reinstall in testing (in-session works); not pursued.*

*2026-06-01 update (latest++++++++++++++++): **iOS: shared web-style header across all 4 tabs.** Ported the web `AppHeader` to a shared `AppHeaderView` now used on Home, Leaderboard, Profile, and Records (previously the header was Home-only, via a nav-bar toolbar, and Profile had a separate floating gear). It renders the **WORDOCIOUS gradient wordmark (no background/bubble)** + PRO badge when active, then **help** and **settings** circle buttons, then the **daily-streak** (🔥) and **streak-shield** (🛡) pills. Changes: (1) settings moved into the header — removed the floating Profile toolbar gear; (2) streak/shield pills show the real `profiles.daily_login_streak` / `streak_shields` and are now **tappable** → a current/best + explanation popover (ports the web `StatPopover`; uses `presentationCompactAdaptation(.popover)` guarded for iOS 16.4+); (3) the streak pill was wrapping its 2-digit count into a squished circle — added `fixedSize`/`lineLimit(1)` so it's the **same capsule shape/size as the shield**, and sized the wordmark to 20 (web `text-xl`) so all controls fit one row. Each tab root hides the system nav bar (`.toolbar(.hidden, for: .navigationBar)`); pushed detail views keep their own bars. **Verified live in the sim** across all four tabs incl. the working streak popover (Current 35 / Best 35). Note: streak + shields were already wired to real data on Home — this makes them consistent app-wide + interactive. Builds clean.*

*2026-06-01 update (latest+++++++++++++++): **iOS Profile "All"-view Pro Stats section added (completes Pro-stats web parity).** The remaining gap from the previous update: the web's All-view **Pro Stats** (`components/profile/pro-stats.tsx`) — two aggregate bar charts, **Win Rate by Mode** (#facc15 yellow, 0–100%) and **Avg Solve Time by Mode** (#a78bfa purple) — wasn't on native. Added `ProStatsCard` (Swift Charts), shown on the All view (`mode == nil`): free users get the locked "Pro Feature" teaser + gold Upgrade-to-Pro button (→ ProView); Pro users get the two charts, built from `user_stats` (combined solo+vs per mode, the native convention; games-weighted avg time; short mode labels Classic/Quad/Octo/Succ/Deliv/Six/Seven/Gaunt/Proper). Per-mode view still shows `ProInsightsCard`. **Verified live in the sim** — free shows the locked teaser; Simulate Pro shows both bar charts; reverted Simulate Pro after (writes `profiles.is_pro`). So the Profile Pro tiering now matches the web on BOTH the per-mode (Pro Insights) and All (Pro Stats) views. Builds clean.*

*2026-06-01 update (latest++++++++++++++): **iOS Profile settings gear pinned to a solid header.** The gear was a `topBarTrailing` toolbar item but the nav bar had no background (transparent), so the scrolling Profile content slid under the floating icon — it read as the button "hovering through the scroll." Added `.toolbarBackground(Theme.background, for: .navigationBar)` + `.toolbarBackground(.visible, …)` so the gear sits on a pinned, solid (theme-aware) header bar and content scrolls beneath it. Verified in the sim. Builds clean.*

*2026-06-01 update (latest+++++++++++++): **iOS Profile: Pro Insights now shows a locked "Upgrade to Pro" teaser to free users (web parity).** Audit of how the web gates Pro stats (`components/profile/pro-insights-card.tsx`): when NOT Pro it renders a **locked overlay** (frosted card + `Lock` #c4b5fd + "Deep Insights" + a gold `linear-gradient(#f59e0b,#d97706)` Crown "Upgrade to Pro" → /pro), and when Pro it shows the real per-mode insights. Native was *hiding* the per-mode `ProInsightsCard` entirely for free users (`if let mode, isProActive`). Fixed: the card now always renders for a selected mode and self-gates — free users get the locked teaser (frosted placeholder + lock + "Deep Insights" + gold "Upgrade to Pro" button that presents `ProView`), Pro users get the real stats (Fastest Win, Fewest Guesses, Perfect Games, Consistency, trend); the proInsights query only runs when Pro. **Verified live in the sim**: Classic mode selected → free shows the locked teaser; "Simulate Pro" on → real per-game insights (8s fastest, 3 fewest, trend). Reverted Simulate Pro after (it writes `profiles.is_pro`). Builds clean. Remaining web-parity gap (minor, not requested): the web's **"All"-view "Pro Stats"** section (two aggregate bar charts — Win Rate by Mode, Avg Solve Time by Mode, also locked for free) isn't on native's All view yet — native's All view shows the free charts only. Per-mode Pro Insights (the reported behavior) is now matched.*

*2026-06-01 update (latest++++++++++++): **iOS: ProperNoundle now selectable in the Leaderboard + Records mode pickers.** ProperNoundle was the only daily mode missing from the leaderboard's top mode selector (and the Records by-mode pickers). Root cause (same family as the earlier Profile Today's-Dailies grid miss): those pickers filtered/matched on `HomeMode.mode` — a `GameMode` — which is `nil` for ProperNoundle (it runs its own engine, so its HomeMode has `dbKey:"PROPERNOUNDLE"` but `mode:nil`). Fix: the leaderboard picker, the shared `HModePicker` (All-Time + Daily records), and the All-Time/Daily title-accent lookups now key off `dbKey` instead — pickers include every dbKey-bearing mode (9, VS excluded) and map the chosen `dbKey` to a `GameMode` via `GameMode(rawValue:)` (`"PROPERNOUNDLE"` == `GameMode.propernoundle`, rawValue confirmed in packages/core). The leaderboard fetch already used `mode.rawValue`, so selecting ProperNoundle queries `daily_results.game_mode = 'PROPERNOUNDLE'` correctly. Verified in the sim: ProperNoundle appears in the picker and loads its daily leaderboard (currently "No results yet" — expected, since ProperNoundle daily recording only started working after today's constraint fix). Builds clean.*

*2026-06-01 update (latest+++++++++++): **iOS: ad banner no longer blocks bottom content on Profile / Leaderboard / Records.** The bottom banner was applied via `.adBanner()` *outside* each tab's `NavigationStack` (in `RootTabView`), so the inner `ScrollView` never inset for the 50pt banner and the last items — notably Profile's **Sign out** button — sat underneath it, unreachable. Moved `.adBanner()` *inside* each of those three `NavigationStack` roots (matching the earlier Home fix), so the ScrollView insets correctly and bottom content scrolls fully above the banner; this also keeps the banner off pushed detail views (e.g. a solved board opened from those tabs). Verified in the sim (Sign out now reachable above the banner). Builds clean.*

*2026-06-01 update (latest++++++++++): **Accessibility + sound settings made functional (both platforms).** Audit found that of the six Settings toggles, only **web sound** actually did anything — the other five were dead (saved but never read), the same pattern as themes. Fixes: **Colorblind mode** — now swaps tiles/keyboard to the high-contrast palette (orange=correct #f5793a, blue=present #85c0f9) for red-green color blindness, on native (`Theme.correct/present/keyCorrect/keyPresent` read `ThemeManager.colorblind`) and web (`[data-colorblind]` overrides the hardcoded green/yellow tile/key classes 500+600 — the old `--correct/--present` vars were never referenced, so the prior approach was a no-op). **Verified live in the sim** (solved board tiles render orange/blue). **Reduced motion** — now actually cuts animations: native adds `Theme.animation()` (nil when reduced) gating the app's `.animation`/`withAnimation` sites; web sets `[data-reduced-motion]` driving a global CSS kill-switch (the old `--transition-duration` var was unused). **Native sound effects** — native had **no audio at all** (haptics only); added `SoundManager` that synthesizes the web `lib/sounds.ts` tones via AVAudioEngine (key tap + win/loss/invalid jingles, `.ambient` session so it respects the mute switch), gated by `pref-sound`, wired into KeyboardView / GameScreen win-loss / GameViewModel invalid / ProperNoundle. Web sound already worked (Web Audio). `ThemeManager` now owns colorblind + reducedMotion (published + persisted); Settings toggles drive it. Native builds clean, web tsc clean. Notes: native colorblind/reduced-motion apply on the next screen render (not forced root rebuild, so toggling doesn't dismiss Settings); the colorblind hex palette matches across platforms. (Sound/reduced-motion aren't screenshot-verifiable; verified by code + build. The colorblind toggle was left ON in the test sim — a harmless local device preference.)*

*2026-06-01 update (latest+++++++++): **Themes made real on both platforms (Default / Dark / Ocean / Forest).** Themes were half-built everywhere: native saved `pref-theme` but `Theme` was static colors (no-op picker); web's `SettingsDialog` (theme + sound + colorblind + reduced-motion) was **never rendered anywhere** (no settings button at all — `ThemeProvider` mounted but unreachable); and Ocean/Forest only overrode `--correct/--present/--absent`, which **aren't referenced anywhere**, so even on web they were no-ops (only Dark did a real repaint). Fixes — owner chose "ship it properly (both)": (1) **Native** — new `ThemeManager` (ObservableObject) + a `Palette` per theme; `Theme`'s surface/text/border tokens now read the active palette and the app root rebuilds on change (`preferredColorScheme` + `.id`) so every screen recolors live (tiles/keyboard/brand stay constant, matching web Dark). Settings picker writes through `ThemeManager`. **Verified live in the sim** — Dark = full dark-navy repaint, Ocean = blue tint; changing theme returns you to Home (the root rebuild that repaints also resets nav — acceptable for a set-once choice). (2) **Web** — added a gear button to the shared `AppHeader` (next to Help) that opens the orphaned `SettingsDialog`; theming applies via the already-mounted `ThemeProvider`/`[data-theme]`. (3) **Ocean/Forest given full real palettes** on both — native `ThemeManager` + matching `[data-theme="ocean"|"forest"]` `--color-*` overrides in globals.css (blue/teal + green/earth light tints). Ocean/Forest hexes are freshly designed (web never had real ones) — tweakable. tsc clean; native builds clean. Known follow-up: the native `.id`-rebuild kicks you back to Home on theme change (a full no-refactor recolor; in-place would need every view to observe the theme).*

*2026-06-01 update (latest++++++++): **Native iOS app merged to `main` + ProperNoundle Today's-Dailies grid fix.** (1) **Merged the full native SwiftUI iOS app to `main`** via PR #38 (`Native SwiftUI iOS app (Wordocious)`, merge commit `e776f81`) — 54 commits, all of `apps/ios`. It had been living on the feature branch `claude/eloquent-hodgkin-383095`; it's now on `main` alongside the web app. The merge preserved the two web-only fixes already on `main` (single-board completion-bonus row + the ProperNoundle constraint migration) since the branch never touched those files. Note: merging `apps/ios` does NOT affect the Vercel web deploy (nothing under `apps/web` changed in the PR). (2) **Fixed a native-only UI bug:** ProperNoundle wasn't rendering in the Profile "Today's Dailies" grid (you'd see "/9" with only 8 tiles). The grid filtered modes on `dbKey != nil && mode != nil`, but ProperNoundle has a `dbKey` and no `GameMode` enum (its own engine), so it was dropped. Added a dedicated `dailyTiles` list (`dbKey != nil`, 9 modes, VS excluded) for the grid — web parity 5+4 — and taught `dailyBadge` to route ProperNoundle to `ProperNoundleView` (which restores the solved daily when already played); the GameMode-backed mode-picker list is unchanged. This is a UI-list fix independent of the constraint fix below — it does not "heal" over time. Verified live in the sim (crown ProperNoundle tile now in row 2). Builds clean; committed `c236146`.*
*2026-06-01 update (latest+++++++): **ProperNoundle daily-recording bug fixed (prod DB constraint).** Symptom: finishing the daily ProperNoundle greyed/locked the Home card (freemium `play_limits` write succeeded) but showed no W/L badge and wasn't counted in the Profile "Today's Dailies" — on web AND native (same tables). Root cause: `recordGameResult` writes `game_mode='PROPERNOUNDLE'` into `user_stats` + `daily_results`, but the **production** `game_mode` CHECK constraints predated the mode (only DUEL/QUORDLE/OCTORDLE/SEQUENCE/RESCUE/GAUNTLET) — the repo constraint migrations (20260414000001 / 20260521000001) had never been applied to prod. supabase-js returns `{error}` (doesn't throw) on a constraint violation and the result wasn't inspected → silent no-row, while the lock (separate unconstrained `play_limits` table) still wrote. Fix: (1) new idempotent migration `20260601000001_reapply_game_mode_constraints.sql` re-states every constraint (daily_seeds/daily_results/matches/user_stats) with the full mode list incl. PROPERNOUNDLE/DUEL_6/DUEL_7; (2) **owner applied it to prod via the Supabase SQL editor** (project `eniiqqsxpmuyrspvepiw`) — confirmed success; (3) hardened `recordDailyResult` to inspect the insert/update `.error` so a rejected daily write can never fail silently again. Records correctly from the next fresh play onward (today's already-played row was rejected pre-fix and isn't backfilled). Also clears the same latent issue for Six/Seven. (CLI link/push not used — entering the Supabase access token / DB password is a hard no, so the owner ran the SQL in-dashboard.)*

*2026-06-01 update (latest++++++): **iOS game-screen layout + finished-screen parity fixes (verified live in the sim).** (1) **Boards now fit the screen** — `BoardLayout` sizes tiles to the smaller of the width and (in-play) height budget via a GeometryReader instead of fixed per-mode sizes in a ScrollView, so Deliverance/QuadWord/OctoWord (and wide Six/Seven single boards) never get clipped by the keyboard; post-game keeps width-fit + scroll. Applied to `GameScreen` + `VSGameView`. (2) **Ad banner no longer covers the keyboard or tab bar** — the `.adBanner()` inset was leaking onto pushed game screens (and earlier was on the TabView, covering the footer); moved inside the Home `NavigationStack` root so it shows on the mode grid only, off all pushed game screens. (3) **Finished/solved screens now match wordocious.com** — added a shared `SolvedBoardFrame` (green frame #4ade80 / bg #f0fdf4 + green ✓ badge on each solved board; red frame on unsolved-at-loss) applied in `BoardView` (live, the moment a board is won) and `SolvedPuzzleView`, plus a shared `FinishedStatsHeader` (amber-trophy boards-solved · guesses · blue-clock time + green/red summary + Home/Share) used by both the live post-game `GameScreen` and the reconstructed solved view; solved boards are now equal-height with empty white filler rows (was trimming). Verified live in the sim across single-board (Classic — no frame, no trophy), 4-board (QuadWord loss — 3 green + 1 red frame, red "3/4 solved"), and 8-board (OctoWord — 8 green frames + ✓, "All 8 solved") solved views, matching the user-supplied web reference.*
*2026-06-01 update (latest+++++): **Full aesthetic polish pass (web-parity).** Ran a multi-agent audit workflow (11 screens audited in parallel, each finding adversarially verified) → 77 verified pixel/copy/color discrepancies vs wordocious.com. Implemented ~75 across every screen, building + committing per area: shared Theme tokens (gray-300 empty border, borderLight/divider, darker 600-weight keyboard palette); Home (split Sweep/Flawless banners w/ icons+gradient text, flush gradient accent bar, W/L badge, exact card/grid sizing); Game (persistent corner Home button, keyboard palette, post-game status line, underlined actions, rounded toast, black tile weight); Pro (floating BEST VALUE badge, crown/title sizes, bold weights); Auth (purple 3D submit gradient, styled error box, card glow/radius); Help; Settings (two sections) + Info (section cards); ProperNoundle (flat red title, per-category pill, per-button hint colors, clock/delete icons, ENTER-right); VS (per-mode 3-stop title gradients, result headline hexes, sizes); Leaderboard (gradient header + live countdown, compact yesterday rows); Records (inner gold/surface cards, custom Solo|VS toggle); Profile (amber PRO badge, banner, daily badges). Then completed the two largest deferred items too: the **Profile mode-picker rebuild** (leading "All" chip + vertical chips with games-count badges, nil-selection = global "All" dashboard) and the **Records daily-leaderboard card** (accent bar + mode header + inline Solo|VS toggle + player-count/rank row). So all ~77 audit findings are now implemented. Also added the remaining Profile dashboard extras — **Top Words** (most-guessed words + win%, from `matches.player1_guesses`) and **Pro Insights** (per-mode personal bests, perfect games, consistency score, improvement trend; Pro-gated) — so the Profile data-viz is complete. All builds clean; signed-out Auth polish visually verified in the simulator (purple btn-3d gradient, #c4b5fd card border + glow). **Visual sweep done on a real signed-in account** (native Google OAuth now wired live — owner added the `com.wordocious.app://auth-callback` redirect to Supabase, so native Google sign-in is verified end-to-end): confirmed Home (accent bars + W/L badges), Profile (rebuilt mode picker w/ All chip + games counts, all charts, Top Words, calendar), Records Daily (card restructure) + All-Time (Hall of Fame + by-mode inner cards), Game (corner Home button, light tile borders), and Auth (purple btn-3d). The sweep caught + fixed a real bug: `session.user.id.uuidString` is uppercase but Postgres returns uuids lowercase, so client-side `winner_id`/`player1_id` string compares failed → Top Words showed 0% win and the calendar rendered no green days; fixed by lowercasing the uid in `MatchStatsService`. **Freemium daily-play gating added + verified live** (ports page.tsx + mode-limit-modal.tsx): free users see completed dailies greyed/locked on Home; tapping shows the "Played Today" modal (countdown + Upgrade to Pro + View Solved Puzzle); Pro users replay unlimited. Added a TEST-ONLY "Simulate Pro"/"Disable Pro" toggle in the Profile header (flips `profiles.is_pro`, mirrors the web dev toggle) — **remove before launch.** Verified the full free↔Pro cycle on a real account in the sim. **Four more parity items (all verified live):** (1) **Leaderboard completed-board dropdown** — `CompletedDailyCard`, a collapsible "Completed/Attempted Today" card that expands to the user's solved board (from the matches row), ports completed-daily-board.tsx. (2) **Today's Dailies badges are tappable** — played → solved board, not-played → play the daily. (3) **Profile equal sizing** — summary cells + mode-picker chips now reserve their optional lines so all are uniform height. (4) **Achievements** — `AchievementService` (all 70 defs 1:1 with achievement-service.ts) + a collapsible "ACHIEVEMENTS X/70" grid in Profile (unlocked purple+✓, locked faded+?), reading the shared `achievements` table. Collapsible headers made fully tappable via contentShape.
**checkAchievements ported to native** — `AchievementService.checkAchievements` is a 1:1 port of the web's ~580-line unlock logic (all 70 achievements), wired into every completion path (solo `GameViewModel`, `VSMatchViewModel`, `ProperNoundleVM`), inserting newly-unlocked keys into the shared `achievements` table. ProperNoundle now also writes a `recordSoloMatch` row (with `hints_used`) so the `pure_proper` hintless ladder + charts work. So native plays now unlock achievements the same as web. Combined the web's per-column profile reads into single fetches; uses local `today` to match native `daily_results.day`. Builds clean; queries are `try?`-guarded (graceful, no crash on schema drift).
**"View Solved Puzzle" now reconstructs from the server `matches` row** (`MatchStatsService.solvedDaily` + read-only `SolvedPuzzleView`) so it works cross-device / on a fresh install, not just from the local snapshot. Verified live: a locked Classic → View Solved Puzzle rebuilt the exact GHOST/DRINK/PLACE/MOLDY/MOULD board with the 1,469 breakdown (Win 1000 + Time 269 + Completion 200), re-confirming the completion-bonus scoring fix on native. (Verification: code-level only — computer-use was disconnected, so populated screens need a signed-in run to eyeball.)*
*2026-06-01 update (adsense rejection fix): AdSense rejected wordocious.com for **"low value content"** — root cause was the **login wall**: only privacy/terms/support were public, so the crawler/reviewer only ever saw the login form. Fixes (web, deployed): (1) added `/how-to-play` + `/about` to AuthGate `PUBLIC_PATHS` (exposing 400+ existing lines) + sitemap; (2) built a **public marketing landing** (`components/auth/landing.tsx`) shown to signed-out visitors instead of the bare login — hero, the 10 modes, how-to-play summary, FAQ, footer, "Sign in to play" reveals the existing LoginScreen (gameplay stays gated); matches brand tokens; (3) new public **`/faq`** page with ~13 Q&As + word-game strategy. After it deploys + Google re-crawls, re-request the AdSense review. Login-required-to-PLAY is preserved.*
*2026-06-01 update (ads): **Ads wired on both clients.** WEB (AdSense, pub-3015627373086578): the interstitial-on-game-start (`AdGate`, 5s countdown → Continue, Pro-skip) + `ads.txt` + loader were already built; added the missing bottom `AdBanner` (free users, dormant until env set). Activation is config-only — set Vercel `NEXT_PUBLIC_ADS_ENABLED=true` + `NEXT_PUBLIC_ADSENSE_CLIENT_ID` + interstitial/banner slot IDs, and get the site AdSense-approved. NATIVE (AdMob, GoogleMobileAds 11.13): new `AdsManager` (SDK start + ATT + game-start interstitial) + `AdBannerContainer` (bottom banner via RootTabView safeAreaInset), gated to non-Pro; the game-start ad is a **rewarded-video interstitial** (`GADRewardedInterstitialAd` — the full skippable-video format) firing on **solo GameScreen, VSGameView (pre-matchmaking), and ProperNoundleView**, with the game timer starting only after dismissal. Uses Google TEST unit IDs — **verified live in the sim** (ATT prompt, test banner, rewarded video w/ "Reward in Ns → granted → close", then board at 0:00). Swap real AdMob app/unit IDs per `apps/ios/ADMOB_SETUP.md`. Note: web AdSense can't do a true 30s-skippable video (that's the native AdMob interstitial/rewarded path).*
*2026-06-01 update (latest++++): **Scoring-breakdown fix + Profile chart dashboard + app icon.** (1) Fixed a display bug where single-board score breakdowns hid the +200 Completion bonus (it was always in the total but the row only rendered for multi-board) — so a Classic win showed Win 1000 + Time 269 but totalled 1469; now the row shows ("puzzle solved") and the math reconciles. Fixed on web (deployed to prod) + native; formula/leaderboard unchanged. (2) Built the deferred Profile data-viz dashboard (`ProfileDashboard` + `MatchStatsService`): per-mode **guess distribution, 90-day activity calendar, solve-time line, time-of-day heatmap** (SwiftUI Charts + custom grids, querying `matches`). Native solo games now also write a `matches` row (`recordSoloMatch`, 1:1 with the web) so the charts populate from native play. Deferred: top words, Pro insights, global "All" view. (3) Added the App Store **app icon** (gradient + white "W" tile, opaque 1024 PNG). Remaining: server/store CONFIG (OAuth providers, StoreKit products, VS server URL) + submission screenshots + App Store Connect record.*
*2026-06-01 update (latest+++): **VS multiplayer (Phase 3) built.** The native VS client is now implemented end-to-end on socket.io-client-swift, mirroring the web's `vs-game.tsx`: the full protocol (21 events, `VSProtocol`/`VSMatchService`), a state machine (`VSMatchViewModel`: queue → match-found countdown → match → waiting → result → rematch, with opponent-left/error handling) that drives a child engine `GameViewModel` (new guarded `isVersus` path — relays guesses/board-solves/completion, skips solo persistence/recording), all match screens (`VSGameView` — reuses the solo board+keyboard + a live opponent strip), a lobby (`VSLobbyView` — free: one daily Classic VS with an already-played upsell; Pro: all standard modes + private-match invites + rematches), invites (`InviteService`, 1:1 with `invite-service.ts`), and vs-result recording (`play_type: 'vs'`). Supports the 7 standard board modes; **Gauntlet/ProperNoundle VS deferred** (bespoke flows). Builds clean & app launches. Two things gate live testing: the **socket server URL** (`VSConfig` is set to the render.yaml-derived `wordocious-server.onrender.com` but a probe returned Render `no-server` — confirm in the dashboard) and needing **two players** — see `apps/ios/VS_SETUP.md`. With this, all 10 modes + all consumer surfaces exist natively; remaining work is server/store CONFIG (OAuth providers, StoreKit products, VS server URL) + submission prep.*
*2026-06-01 update (latest++): **StoreKit 2 IAP.** ProView now drives real purchases via a new `StoreManager` (StoreKit 2) — live prices, per-plan purchasing spinners, Restore Purchases, and the App-Store-required auto-renew disclosure + Terms/Privacy links. A verified transaction writes the entitlement to the same `profiles` columns the web uses (`is_pro`/`pro_expires_at`/`streak_shields`), 1:1 with `fulfillSubscription` (monthly +30d, yearly +365d via StoreKit's expirationDate, day pass +24h stacking; +4 shields on monthly/yearly only). Product IDs `com.wordocious.app.pro_{monthly,yearly,day}`; ships a local `.storekit` test config wired into the scheme. Fulfillment is client-side for now (RLS permits own-row writes, same trust model as the web) — `STOREKIT_SETUP.md` documents App Store Connect product setup + the server-side-verification (App Store Server Notifications) hardening path. Only **VS multiplayer (Phase 3)** remains.*
*2026-06-01 update (latest): **Native login gate + native OAuth.** (1) Gated the whole iOS app behind auth, mirroring the web's `auth-gate.tsx` — signed-out users see the login screen (with a branded loading skeleton during session restore), never the app; closes the signed-out-play parity gap. (2) Wired **Sign in with Apple + Continue with Google** (replacing the social placeholders). Apple replaces Facebook because App Store Guideline 4.8 mandates Sign in with Apple alongside any third-party social login (product decision). All three paths (Apple/Google/email) are functional in code; OAuth first sign-in auto-creates a profile row like the web. Apple/Google need server-side config to activate the round-trip — exact steps in `apps/ios/OAUTH_SETUP.md` (Apple Developer Services ID + key, Supabase providers, redirect allow-list). Builds clean, auth screen verified in sim. Remaining big items: StoreKit 2 IAP, VS multiplayer (Phase 3).*
*2026-06-01 update (later): Built **ProperNoundle** — the 9th solo game (guess-the-name with word-group board, hints, category rotation; ported its own engine + bundled puzzle data). 9 of 10 modes now playable on iOS; only VS Battle (Phase 3 socket.io) remains.*
*2026-06-01 update (audit pass complete): Finished the web→native parity audit — #20 **Pro page** (`/pro` benefits/plans/day-pass; Subscribe pending StoreKit), #21 **Auth** (matched login-screen.tsx — Google/Facebook buttons, email/password, copy), #22 **Settings + info pages** (theme picker + toggles + About/Privacy/Terms/Support). Remaining deferred: Profile chart dashboard (VS-data, revisit in Phase 3), StoreKit IAP, native login gate, native OAuth, full theming, VS multiplayer, ProperNoundle. Earlier: Parity audit #19 (pass 1) — rebuilt the native **Profile** core to match `/profile` (avatar + level tier + XP bar, Today's Dailies 5+4 grid, GlobalSummaryRow, mode picker + per-mode user_stats). Heavy bits deferred to a later pass (activity calendar, charts, top words, time-of-day heatmap, Pro insights, edit modal, social links). #18 — rebuilt the native **Records** tab to match `/records` (RECORDS header, Daily|All-Time toggle, Hall of Fame + By-Game-Mode mode picker, StatCell with current-user highlight, Daily solo/VS leaderboard). Earlier same day:*
*Fixed a production bug where **first-time/signed-out users were stuck on an infinite loading spinner** — `AppLoaderDismiss` was nested inside `AuthGate`, so the signed-out (LoginScreen) path never dismissed the static `#app-loader` overlay; moved it outside the gate (deployed, verified live). Product decision: site **keeps login-required** (native app currently plays signed-out → TODO: add a matching auth gate to native for parity). Also audited the 2,611-word 5-letter solutions list and **removed 17 questionable answers** (BARRY/PUBES + vulgar + name-first words; kept legit common-word names + NAKED) on web + iOS, regenerated parity fixtures (17/17 pass). Removing answers shifts the daily seed→word mapping.*

### Historical snapshot (May 29, 2026)

*Preserved from the original §7 — the state of the world when the native iOS effort was mid-Phase-1. Everything below has since been superseded by the change log above (all tabs/modes complete, VS live, builds shipped from `main`).*

#### Native iOS — Phase 1 (as of May 29, 2026)

**Engine port (Phase 0 — done & verified).** Ported all of `packages/core` to Swift in `apps/ios/Sources/Core/` (Types, Evaluator, Dictionary, Seed, Scoring, Prefill, Reducer). Wrote `generate-fixtures.mjs` to emit JSON fixtures from the live TS engine, and `EngineParityTests` (XCTest) that replays them against the Swift engine — **16/16 passing**, including the critical `simpleHash` (Int32-truncated to match JS 32-bit bitwise overflow) and seed-generation cases. Confirms a seed → identical words on iOS and web.

**App scaffold (Phase 0 — done).** XcodeGen `project.yml` generates `Wordocious.xcodeproj` (bundle `com.wordocious.app`, iOS 16+) linking the `WordociousCore` SwiftPM package. The `.xcodeproj` is gitignored (regenerate via `xcodegen generate`). Verified live in the iPhone 17 Pro simulator generating that day's real daily puzzle.

**Game UI (Phase 1 — done).** `GameViewModel` drives any solo mode (1..N boards) off the pure engine. `BoardView`/`TileView` render the grid (green/yellow/gray on `#f8f7ff` lavender, matched to web); `BoardLayout` lays out 1 board full-size or 2/4/8 in a scrollable grid. `KeyboardView` is a QWERTY with per-key state coloring + native haptics. `HomeView` is a mode picker. 7 solo modes playable: Classic, Six, Seven, QuadWord, OctoWord, Sequence, Deliverance. `GamePersistence` saves/loads `GameState` per seed+mode to resume mid-game.

**Auth (Phase 1 — done, email).** `supabase-swift` integrated. `AuthService` mirrors the web `auth-context` (session restore, `authStateChanges`, profile fetch, expiry-aware `isProActive`). `Profile` model + `isProActive()` ported 1:1 from `lib/pro.ts`. `AuthView` does email sign-in/sign-up; Sign in with Apple button scaffolded (activates in Phase 2). Anon key wired and **live-verified** (REST 200; email + Google enabled on the project, Apple OAuth not yet configured).

**Code signing (done).** Apple Distribution team `Q32F6GRDYG` (same as ShowLoud) baked into `project.yml` (`DEVELOPMENT_TEAM` + automatic signing) so it survives XcodeGen regeneration. Apple recognizes it and resolves `com.wordocious.app`; simulator + distribution archive are unblocked, device builds just need a registered iPhone.

**Aesthetic match (done).** Audited the web design system and mirrored it natively: bundled the **Nunito** typeface (`UIAppFonts`), pulled exact tokens from `globals.css` (bg `#f8f7ff`, text `#1a1a2e`, border `#ede9f6`, primary `#7c3aed`), the **WORDOCIOUS** gradient wordmark (`#a78bfa→#ec4899`), the gold flame streak pill, and a 4-tab shell (`RootTabView`: Home/Leaderboard/Profile/Records) matching `bottom-nav.tsx` icons + purple tint. `ProfileTab` renders the real Supabase profile (avatar, Pro badge, level/wins/streak, medals).

**Live leaderboard (done).** `LeaderboardService` mirrors `lib/daily-service.ts` `getDailyLeaderboard()` exactly — `daily_results` with a `profiles!inner(username, avatar_url)` join, filtered by device-local day + game_mode + `play_type=solo`, ordered `composite_score desc, created_at asc`. `LeaderboardTab` has a mode picker + ranked rows. **Note:** `daily_results` SELECT policy is `TO authenticated`, so the leaderboard returns data only to signed-in users — the tab shows a sign-in prompt when signed out (verified: anon REST read returns `[]`). Records tab still a placeholder.

**Result writing (done).** `DailyScoring` ports the composite-score formula 1:1 from `lib/daily-service.ts` (`MODE_SCORE_CONFIG` + `computeScoreBreakdown`). `DailyResultsService.record` mirrors `recordDailyResult` — keyed by user+day+mode+play_type, insert-new or update-only-if-better, gated to an authenticated session (RLS). `GameViewModel` posts once when a daily game finishes (elapsed from `state.startTime`, boards solved, rows used; no-ops for non-daily/restored-finished/signed-out). So iOS daily play now produces the same rows the web does and appears on the shared leaderboard. Not yet exercised end-to-end (avoided writing test data to prod). Time is wall-clock from game start (web uses active-play time) — a minor scoring nuance to refine later.

**Records tab (done).** `RecordsService` mirrors `fetchAllTimeRecords` (`all_time_records` + `profiles!inner`, ordered by type); `RecordCatalog` ports `RECORD_LABELS` + the global/per-mode type lists + per-type value formatting from `app/records/page.tsx`. `RecordsTab` shows Global Records + Classic per-mode records as holder cards. Like the leaderboard, `all_time_records` SELECT is `TO authenticated`, so the tab is sign-in-gated. All four tabs (Home/Leaderboard/Profile/Records) now have real content.

**Gauntlet (done).** The 5-stage run (Duel→Quordle→Sequence→Rescue→Octordle, 1/4/4/4/8 boards). `GameViewModel` detects stage clears, advances via `NEXT_STAGE` (with elapsed timing), and finishes on the last stage; `GameScreen` shows the stage label + a Continue/Finish banner between stages. Gauntlet result recording computes cumulative guesses + boards-solved from `stageResults`, mirroring `gauntlet-game.tsx`. New engine test `testGauntletFullRunReachesWon` plays a complete run to WON (17/17 engine tests pass). 8 of the ~11 modes are now playable on iOS (remaining: ProperNoundle — needs its data; VS modes — Phase 3).

**Profile progression (done).** `GameResultsService` ports the core of `recordGameResult` (`lib/stats-service.ts`): `user_stats` upsert (wins/losses/games/best_score/average_time/fastest_time, same running-avg + min logic), profile `total_wins/losses`, win-streak + best, **XP** (100 win/25 loss + 50 streak + 50 daily bonus), **level** (xp/1000+1), daily-login-streak (local-day compare) + best, and the 7-day free streak-shield grant. Then calls the daily-results row and refreshes the in-memory profile. `GameViewModel` records via this on finish. **Still deferred** (fire-and-forget in web): `all_time_records` `checkAndUpdateRecord` writes (so iOS play doesn't yet *set* hall-of-records entries) and the daily "sweep" bonus.

**Visual verification (done).** Confirmed live in the iPhone 17 Pro simulator: Home (8 mode cards, gradient WORDOCIOUS wordmark, 4-tab bar), and a full Classic game loop — typed a guess, correct green/yellow/gray tile reveal, keyboard state coloring, guess counter advance. Aesthetic matches the web (Nunito, brand palette). Fixed a wordmark bug (overlay+mask → `foregroundStyle` gradient + `fixedSize`). The authenticated write loop (sign-in → play → XP/leaderboard row) is not yet E2E-tested (would require entering credentials / writing to prod).

All native work lives on branch `claude/eloquent-hodgkin-383095` (off `main` — never triggers a Vercel deploy).

#### Web — stable, untouched (as of May 29, 2026)
Latest `main` is `abef5de` (#37, hints on daily leaderboard). Recent merges: #34 multi-board Gauntlet fix, #35 hintless scoring + Pure achievements, #36 Gauntlet footer + RLS, #37 hints on leaderboard. No web files changed by the iOS effort.

---

## 8. Engine parity — implementation notes

The single most important invariant in this project: **the Swift, Kotlin, and TS engines must compute identically.** If they drift, daily seeds and VS matches desync between platforms.

- **The contract** is `apps/ios/Tests/Fixtures/*.json` — generated from the live TS engine by `apps/ios/generate-fixtures.mjs` and replayed by `EngineParityTests`. Covers hashing, evaluation, seed generation (5/6/7-letter), scoring, prefill, and daily-seed helpers. The Android Kotlin engine in `apps/android/core` is the same port discipline.
- **The risk line** is `simpleHash`. JavaScript bitwise ops are 32-bit; Swift `Int` is 64-bit (and Kotlin `Long` likewise). The native ports truncate to 32-bit so overflow matches exactly — this drives which words a seed selects, so an off-by-one there would diverge every puzzle. Verified bit-identical.
- **The rule:** never change game logic on one platform without porting it to the others and re-running the fixtures. Treat `packages/core` as canonical; regenerate fixtures after any change to it.
- **Dictionaries** are the same JSON word lists (`allowed`, `solutions`, and 6/7-letter variants) bundled into the web app, the iOS app bundle, and the Android app. Keep one canonical copy.

---

## 9. Build cadence

**Web/server:** commit + push to `main` → Vercel auto-deploys the web app (project `spellstrike`); the socket server auto-deploys to Railway from `main` too. The user's standing preference is auto-commit + push to `main` after every change.

**Native iOS — development loop** (driven from the command line; Xcode is sandboxed to click-only for the agent):
1. Edit Swift sources / `project.yml`.
2. After adding files **or** packages, run `xcodegen generate` (regenerates the `.xcodeproj`).
3. `swift test` for engine parity; `xcodebuild -scheme Wordocious -destination 'generic/platform=iOS Simulator' build` for the app.
4. `xcrun simctl install/launch` to run in the simulator.

**Native iOS — release flow** (proven across builds 2→20):
1. Bump `CURRENT_PROJECT_VERSION` in `project.yml` → `xcodegen generate`.
2. Archive **UNSIGNED** (`CODE_SIGNING_ALLOWED=NO`).
3. `xcodebuild -exportArchive` with the export plist at `~/.appstoreconnect/wr_export.plist` — **`signingStyle=automatic`** + `-allowProvisioningUpdates` + the ASC API key `C8FRS9T697`, so xcodebuild fetches the App Store provisioning profile and embeds `embedded.mobileprovision`. (Manual signing with no profile → 409 "Missing Provisioning Profile". The plist lived only in `/tmp` and got cleaned out once — now backed up under `~/.appstoreconnect/`.)
4. **Re-sign the exported .app to restore `com.apple.developer.applesignin`** — the export strips this entitlement every time. Dump the COMPLETE exported entitlements first (`codesign -d --entitlements - --xml`, which includes `application-identifier` + `team-identifier`), add applesignin to that full set, then `codesign -f`. Re-signing from a partial entitlements file drops `application-identifier` → 409.
5. Upload via `altool`.

App Store Connect operations (attach build, cancel/resubmit review submissions, check processing) can be driven via the **ASC REST API** (ES256 JWT). Durable helper at **`~/.appstoreconnect/asc.rb`** (`builds` / `version` / `get` / `patch` / `post` / `delete`) — replaces the old `/tmp/asc_*.rb` scripts that got cleaned out. Release is always **Manual** (standing instruction). **KNOWN API LIMITATION (hit 2026-06-13):** the build-swap flow that works for an IN_REVIEW/WAITING version (cancel submission → PATCH `appStoreVersions/{id}/relationships/build` → POST reviewSubmission + item → submit) does **NOT** work once a version is **PENDING_DEVELOPER_RELEASE** (already approved). The API returns `INVALID_STATE` / `STATE_NOT_SUITABLE_TO_SUBMIT` for both the build PATCH and the submission-item add, and exposes no "developer-reject" transition to return the version to an editable state. Swapping a build onto an approved-pending-release version must be done in the **ASC web UI** (which performs the implicit un-approval), or by releasing the approved build and shipping the new one as a subsequent update.

**Native Android:** `./gradlew assembleDebug` + emulator for development (no physical Android device — emulator-only testing); `./gradlew bundleRelease` for a **signed release AAB** (signing via the gitignored `apps/android/key.properties` → keystore `~/.android-keys/wordocious-upload.jks`).

Native work (iOS + Android) now commits to `main` alongside everything else — the web deploy is unaffected because native directories don't change web build output.

---

## 10. Domains, accounts, infra

- **Web production domain**: `wordocious.com` (Vercel)
- **Vercel projects**: the **live project is `spellstrike`** (the `wordrush` project is stale — do not deploy there)
- **GitHub repo**: `BTBTBTBTBTBTBT/WordRush`, default branch `main`
- **Supabase project**: `eniiqqsxpmuyrspvepiw` (Postgres + Auth + Storage); 13 public tables, RLS enabled + correct
- **Realtime server**: `socket.io` (`apps/server`) on **Railway** behind `server.wordocious.com` (deploys from `main`); DNS managed at **GoDaddy**, not Vercel
- **iOS bundle ID**: `com.wordocious.app`
- **Brand typeface**: Nunito (bundled in both native apps; Google font, same as web)
- **Apple Developer Team ID**: `Q32F6GRDYG` (Apple Distribution; same team as ShowLoud) — baked into `project.yml`. (`CG6Q3FCB7B` is the personal/individual team, sim/personal-device only.)
- **App Store Connect app ID**: `6775966055` (Wordocious). Version **1.0 (build 34) SUBMITTED** as of 2026-06-17 (manual release). History: build 10 approval canceled → build 31 → 32 (rejected) → 33 (guest-mode fix) → **build 34** (guest "Sign In" discoverability: persistent header button on web + both natives, profile prompt offers Sign In not just Go Home). **Build 32 REJECTED 2026-06-17** on **2.3.6** (Age Rating must declare Advertising=Yes — FIXED in ASC metadata, no build) and **5.1.1(v)** (app forced registration before single-player — FIXED via guest mode on all 3 platforms). To swap a build while in review: "remove this version from review" → Developer Rejected → swap build → Add for Review → Submit. **MARKETING_VERSION must stay `1.0`** to match the ASC version record, else the build-picker won't offer the build (builds 11–30 were `1.0.0` and were invisible to the v1.0 picker). ASC API key for build automation: key ID `C8FRS9T697`, issuer `8bdd3f73-0d8b-427d-95c7-8097b77dfb7a`, `.p8` at `~/.appstoreconnect/private_keys/`; build-state poll script at `~/.appstoreconnect/asc_builds.rb`. **Build-export plist** backed up at `~/.appstoreconnect/wr_export.plist` (was previously only in `/tmp`, which got cleaned out — it must use `signingStyle=automatic` so `-allowProvisioningUpdates` fetches the App Store profile; the applesignin re-sign must dump the COMPLETE exported entitlements first, then add applesignin, or `application-identifier` is lost → 409).
- **AdMob**: publisher `pub-3015627373086578`. iOS app approved (App ID `…~8393761846`, banner `…/4287985559`, rewarded-interstitial `…/6909445311`); GDPR (UMP) consent message published 2026-06-10. **No Android AdMob app yet** (Android runs Google test IDs).
- **Google Play Console**: _not yet created_ ($25 one-time) — the gating item for Android submission (app record, the 3 billing products, internal-testing track, license testers, store listing/privacy form).
- **Android signing**: upload keystore at `~/.android-keys/wordocious-upload.jks`, referenced by the gitignored `apps/android/key.properties` (storepass inside). **MUST BE BACKED UP** — losing it loses the Play signing identity unless Play App Signing is enrolled.
- **Android deep links**: `https://wordocious.com` app links; `/.well-known/assetlinks.json` is published with the upload-cert SHA-256 so verification is automatic.
- **Android Google sign-in**: needs an **Android OAuth client** in the Google Cloud project (package `com.wordocious.app`, SHA-1 `7A:2C:E8:AC:82:B7:F7:F6:10:64:E1:9D:CC:88:23:0A:8E:63:79:B0`) before ID tokens vend.
- **Local repo location**: primary working copy at **`~/Developer/WordRush`** (NON-iCloud) — moved off `~/Desktop/Projects/WordRush` because Desktop is iCloud-synced with "Optimize Mac Storage", which evicted file contents at high disk usage and made `git`/`xcodebuild`/`find` hang forever (`fileproviderd` pegged). Both copies share the same GitHub remote; commit from `~/Developer`. Web deploy is unaffected (Vercel builds from GitHub `main`, not local).
- **Vercel env vars** (Production + Preview, Sensitive): `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET`
- **Supabase anon key**: public/publishable, shipped in the web bundle and both native apps (`SupabaseConfig.swift` / the Android config), gated by RLS
- **RLS note**: `daily_results` AND `all_time_records` SELECT are both `TO authenticated` (despite a "publicly readable" comment) — leaderboards + records require a signed-in session; anon reads return `[]`. `profiles` SELECT allows the anon REST ping (HTTP 200).
- **Cron**: `/api/cron/daily-medals` (12:05 UTC — moved from 05:05 in the audit sweep so west-of-UTC players' local-day rows finalize before podium grading; now insert-only/idempotent), `/api/cron/daily-reminder` (14:00 UTC)

---

## 11. Critical risks

1. **Engine parity drift.** If the Swift, Kotlin, and TS engines diverge, cross-platform dailies and VS break. Guarded by the fixture suite — but only if it's re-run after every `packages/core` change.
2. **Android upload-keystore loss.** `~/.android-keys/wordocious-upload.jks` is the Play signing identity — losing it means losing the ability to update the app (unless Play App Signing is enrolled at submission, which it should be). **Back it up.**
3. **Pro entitlement is still client-writable.** BOTH server webhooks now exist fail-closed — Apple App Store Server Notifications V2 (`apps/web/app/api/appstore/`) and the Google Play RTDN webhook (`apps/web/app/api/playstore/`) — but neither is **configured/enforced** yet. The migration that blocks client `is_pro` writes (`20260603000004_lock_pro_columns.sql`) is deferred until both are set up (certs/service-account, env vars, ASC + Pub/Sub registration) and sandbox-tested; running it before the Play webhook is live would break Android purchases. Until then a malicious client could self-grant Pro. See each route's README.
4. **VS cross-platform pairing fix not yet live-verified.** The (mode, dailySeed|random) matchmaking bucketing that fixes the iOS↔web pairing failure is deployed but still needs a live two-player cross-platform test.
5. **AdMob Android still on Google test IDs.** Shipping to Play without creating the Android AdMob app + swapping real IDs means zero ad revenue on Android.
6. **Android Google sign-in blocked on config.** Built end-to-end, but no tokens vend until the Android OAuth client (package + SHA-1) exists in Google Cloud.
7. **ATT prompt reliability (App Review 5.1.2i).** Build 8 was rejected because the reviewer never saw the ATT prompt — it was requested at the tail of the Google UMP network chain and while the app could still be `.inactive` (iOS silently denies without showing it). FIXED in build 9+ (`AdsManager.requestATTOnce` gates on `.active` + 3s watchdog) and validated by build 10's approval. If privacy labels keep declaring tracking, every build MUST reliably present ATT on first launch or it will be re-rejected.
8. **Subscription metadata must carry an EULA link (App Review 3.1.2c).** Build 8 was also rejected for a missing Terms of Use (EULA) link in the App Store Description. The Description now includes subscription names/prices + the standard Apple EULA link + Privacy Policy — keep these whenever editing the Description.
9. **No real payments collected yet.** iOS StoreKit 2 is live in the build but revenue starts only after release; web still runs `DemoProvider` (Stripe not wired); Android Billing awaits Play Console products.
10. **Single-developer bus factor.** All web, native iOS, native Android, infra, and store negotiation sit with one person.
11. **iCloud eviction breaks builds (environmental).** Keeping the repo under an iCloud-synced folder (`~/Desktop`) with "Optimize Mac Storage" ON silently offloads file contents at high disk usage → all reads hang, `fileproviderd` pegs CPU. Mitigated by working from `~/Developer` (non-synced) and keeping disk < 85%. Do NOT toggle off "Desktop & Documents" sync (current macOS deletes local files with no keep-a-copy option).
12. **Apple Guideline 4.2 (minimum functionality).** Was the #1 review risk for a puzzle app; mitigated by going fully native (the lesson from ShowLoud's repeated WebView rejections) and now validated — build 10 passed App Review.
13. **AdSense web ads blocked.** wordocious.com was denied (low value content) because the login gate hides the game from Google's crawler. Public `/guides` content shipped as the fix; if a re-review fails again, the escalation is anonymous daily play (a product decision). No web ad revenue until approved. Does NOT affect AdMob app ads.

---

## 12. Pending non-engineering items

- **iOS release — swap build 20 onto 1.0 (BLOCKED via API, needs ASC web UI):** the API can't swap a build on a PENDING_DEVELOPER_RELEASE version (see §9). To put build 20 through: in the ASC web UI, on the 1.0 version, **remove/replace the build** (this developer-rejects the approved binary and returns 1.0 to "Prepare for Submission"), select **build 20**, keep release = **Manual**, and resubmit for review. (Once the version is back in an editable state, the API swap *would* also work — offer to finish it then.) Alternatively release build 10 now and ship build 20 as a later update. An inert empty draft reviewSubmission (`a176542b`, 0 items, never submitted) exists from the API attempt — harmless; discard it in the UI if it shows.
- **AdSense re-review**: request review on the wordocious.com site in the AdSense console after the ~48h crawl window now that `/guides` content is live.
- **Google Play Console**: create the $25 developer account + the Wordocious app record; create the 3 billing products (`pro_monthly`, `pro_yearly`, `pro_day`); set up an internal-testing track + license testers; complete the store listing + privacy/data-safety form.
- **Android AdMob app**: create it in the AdMob console and swap the real IDs into `AndroidManifest` + `AdsManager.kt`.
- **Android OAuth client**: create in Google Cloud (package `com.wordocious.app` + the upload SHA-1) to activate native Google sign-in.
- **AdMob payments**: set up the payment profile (banking); after the iOS app is publicly released, link the App Store listing and verify `app-ads.txt`.
- **Pro entitlement enforcement**: configure + sandbox-test the App Store Server Notifications webhook, then run the deferred `lock_pro_columns` migration (see §11.3).
- **Stripe (web Pro)**: decide whether to wire real web payments now or after the stores are live.
- **Business entity / tax**: whether to ship under an individual or LLC (payment processors accept individuals; an entity is a liability/tax question, not a technical blocker).
- **Keystore backup**: copy `~/.android-keys/wordocious-upload.jks` (+ `key.properties` passwords) somewhere safe off-machine.
- **Push infra for native**: APNs key (one-shot download — back it up) + FCM project, if server-pushed notifications are ever wanted (both apps currently use local/scheduled reminders).

---

## 13. What to read next (if you want to dig deeper)

- `apps/ios/README.md` — native iOS build setup + parity contract.
- `apps/android/` — the native Android app (`:app` Compose UI + services, `:core` Kotlin engine port; `key.properties` for release signing).
- `packages/core/src/` — canonical game engine (TS). The Swift port mirrors it file-for-file in `apps/ios/Sources/Core/`; the Kotlin port lives in `apps/android/core`.
- `apps/web/lib/payment/` — the monetization abstraction (interface, factory, fulfillment).
- `apps/web/app/api/appstore/README.md` — the StoreKit server-notifications webhook + the steps to enforce server-side Pro entitlement.
- `apps/web/lib/pro.ts` + `lib/auth-context.tsx` — the Pro entitlement read path.
- `apps/server/src/` — socket.io matchmaking + VS protocol (`types.ts` has the event contract).
- `supabase/migrations/` — the data model + RLS policies.
- The `.claude/` memory under the project — accumulated session notes (native build plan, ShowLoud architecture reference).

---

## Glossary

- **Daily seed**: `daily-YYYY-MM-DD-<MODE>` (device-local date). Deterministically selects the day's puzzle words; identical across web + iOS + Android.
- **Mode**: a puzzle variant (Classic/Quordle/Octordle/Sequence/Rescue/Gauntlet/VS/etc.), differing in board count, word length, and guess budget.
- **Gauntlet**: a single 5-stage run chaining modes; one bust ends it.
- **VS**: real-time head-to-head match on a shared seed via the socket server; matchmaking is bucketed by (mode, dailySeed|random).
- **Pro**: the single paid subscription (`pro_day` / `pro_monthly` / `pro_yearly`); entitlement = `is_pro` + `pro_expires_at`, read via `isProActive()`.
- **Streak shield**: a consumable that protects a daily-login streak; granted with monthly/yearly Pro.
- **Engine parity**: the guarantee that the Swift, Kotlin, and TS engines compute identically, enforced by JSON fixtures.
- **simpleHash**: the 32-bit string hash that drives seed→word selection; the parity linchpin.
- **PaymentProvider**: the interface/factory in `lib/payment/` that is the single swap point between Demo / Stripe / StoreKit fulfillment.
