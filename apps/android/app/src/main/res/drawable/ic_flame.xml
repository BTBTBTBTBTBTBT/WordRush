<!--
  Streak flame — a direct transcription of the iOS asset
  (Resources/Assets.xcassets/flame.imageset/flame.svg), same as ic_shield.

  Android was using Material's LocalFireDepartment, a SOLID filled flame, next
  to our stroked outline shield — so the two header pills didn't even match
  each other, let alone iOS. Stroke geometry, width, and round caps/joins are
  copied verbatim; the color is supplied by Icon(tint = …) at the call site.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:strokeColor="#000000"
        android:strokeWidth="2"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:fillColor="@android:color/transparent"
        android:pathData="M12,3 q1,4 4,6.5 t3,5.5 a1,1 0 0 1 -14,0 5,5 0 0 1 1,-3 1,1 0 0 0 5,0 c0,-2 -1.5,-3 -1.5,-5 q0,-2 2.5,-4 z" />
</vector>
